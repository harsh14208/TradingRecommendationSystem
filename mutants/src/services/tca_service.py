"""
services/tca_service.py

QENG-3b: Transaction Cost Analysis service.
QENG-3c: Capacity and participation limits checker.
Computes realized slippage, NBBO spread capture, implementation shortfall,
and checks trading capacity limits before executing orders.
"""

import logging
import math
from datetime import datetime
from typing import Optional, Tuple
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from models import BrokerOrder, Fill, Instrument

log = logging.getLogger("signal.trade.tca")

# Default values if market stats are unavailable
DEFAULT_SPREAD_PCT = 0.0010  # 10 bps
DEFAULT_DAILY_VOL = 0.020   # 2.0%
DEFAULT_ADV = 1_000_000     # 1M shares


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_record_fill_tca__mutmut: MutantDict = {}  # type: ignore

@_mutmut_mutated(mutants_x_record_fill_tca__mutmut)
async def record_fill_tca(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_orig(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_1(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = None
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_2(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(None)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_3(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") and 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_4(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") and broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_5(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get(None) or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_6(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("XXfilled_qtyXX") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_7(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("FILLED_QTY") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_8(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get(None) or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_9(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("XXfilledQtyXX") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_10(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledqty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_11(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("FILLEDQTY") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_12(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 1.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_13(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = None
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_14(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(None)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_15(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty and filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_16(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") and order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_17(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") and broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_18(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get(None) or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_19(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("XXqtyXX") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_20(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("QTY") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_21(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get(None) or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_22(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("XXrequested_qtyXX") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_23(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("REQUESTED_QTY") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_24(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = None
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_25(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(None)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_26(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") and 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_27(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") and broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_28(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") and broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_29(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get(None) or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_30(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("XXfilled_avg_priceXX") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_31(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("FILLED_AVG_PRICE") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_32(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get(None) or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_33(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("XXavg_fill_priceXX") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_34(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("AVG_FILL_PRICE") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_35(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get(None) or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_36(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("XXavgPriceXX") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_37(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgprice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_38(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("AVGPRICE") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_39(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 1.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_40(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = None
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_41(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = None
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_42(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_43(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price >= 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_44(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 1 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_45(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = None
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_46(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") and "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_47(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") and broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_48(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get(None) or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_49(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("XXtypeXX") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_50(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("TYPE") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_51(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get(None) or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_52(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("XXorder_typeXX") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_53(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("ORDER_TYPE") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_54(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "XXmarketXX"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_55(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "MARKET"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_56(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = None
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_57(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get(None)
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_58(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("XXstatusXX")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_59(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("STATUS")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_60(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_61(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = None  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_62(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = None
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_63(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid and not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_64(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_65(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_66(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = None
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_67(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 1.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_68(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol not in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_69(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["XXAAPLXX", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_70(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["aapl", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_71(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "XXMSFTXX", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_72(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "msft", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_73(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "XXNVDAXX", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_74(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "nvda", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_75(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "XXGOOGXX", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_76(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "goog", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_77(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "XXAMZNXX"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_78(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "amzn"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_79(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 1.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_80(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = None
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_81(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(None, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_82(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, None)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_83(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_84(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, )
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_85(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival / spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_86(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 5)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_87(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = None
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_88(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(None, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_89(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, None)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_90(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_91(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, )
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_92(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 5)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_93(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = None
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_94(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 1.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_95(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker != "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_96(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "XXibkrXX" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_97(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "IBKR" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_98(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 1.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_99(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = None
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_100(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(None, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_101(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, None)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_102(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_103(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, )
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_104(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty / fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_105(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 3)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_106(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 or arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_107(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price >= 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_108(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 1 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_109(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival >= 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_110(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 1:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_111(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = None
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_112(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price + arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_113(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.upper() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_114(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() != "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_115(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "XXbuyXX" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_116(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "BUY" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_117(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival + avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_118(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = None
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_119(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) / 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_120(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff * arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_121(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10001.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_122(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = None
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_123(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(None, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_124(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, None)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_125(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_126(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, )
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_127(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread and 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_128(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 1.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_129(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 1.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_130(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = None
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_131(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid + avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_132(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.upper() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_133(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() != "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_134(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "XXbuyXX" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_135(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "BUY" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_136(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price + order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_137(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = None
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_138(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) / 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_139(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff * order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_140(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 101.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_141(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                None
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_142(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = None
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_143(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                None
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_144(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(None)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_145(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(None).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_146(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id != order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_147(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = None
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_148(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill or filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_149(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_150(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty >= 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_151(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 1:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_152(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = None
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_153(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    None
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_154(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(None)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_155(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(None).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_156(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker != order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_157(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = None
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_158(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = None
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_159(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=None,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_160(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=None,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_161(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_162(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=None,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_163(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=None,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_164(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=None,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_165(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=None,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_166(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=None
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_167(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_168(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_169(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_170(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_171(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_172(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_173(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_174(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_175(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees and 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_176(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 1.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_177(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(None)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_178(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning(None, order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_179(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", None, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_180(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, None, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_181(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=None)
        
    return order

async def x_record_fill_tca__mutmut_182(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning(order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_183(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_184(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_185(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, )
        
    return order

async def x_record_fill_tca__mutmut_186(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("XXError recording TCA for order %s: %sXX", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_187(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("error recording tca for order %s: %s", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_188(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("ERROR RECORDING TCA FOR ORDER %S: %S", order.id, e, exc_info=True)
        
    return order

async def x_record_fill_tca__mutmut_189(
    db: AsyncSession,
    order: BrokerOrder,
    broker_order_data: dict
) -> BrokerOrder:
    """
    QENG-3b: Extract fill details from broker response and calculate realized TCA.
    Populates NBBO mid, spread, avg_fill_price, fees, slippage_bps, and shortfall.
    """
    try:
        # Extract fields from broker data
        filled_qty = float(broker_order_data.get("filled_qty") or broker_order_data.get("filledQty") or 0.0)
        qty = float(broker_order_data.get("qty") or broker_order_data.get("requested_qty") or order.requested_qty or filled_qty)
        avg_fill_price = float(broker_order_data.get("filled_avg_price") or broker_order_data.get("avg_fill_price") or broker_order_data.get("avgPrice") or 0.0)
        
        # Populate ledger columns
        order.requested_qty = qty
        order.filled_qty = filled_qty
        order.avg_fill_price = avg_fill_price if avg_fill_price > 0 else None
        order.route_order_type = broker_order_data.get("type") or broker_order_data.get("order_type") or "market"
        order.final_execution_status = broker_order_data.get("status")
        
        # Estimate or fetch arrival price (fall back to signal target price or order target)
        if not order.arrival_price:
            order.arrival_price = avg_fill_price  # Fallback
            
        arrival = order.arrival_price
        
        # Calculate NBBO Mid and Spread estimate at arrival if not set
        if not order.nbbo_mid or not order.spread:
            # Estimate spread as 5 bps for liquid tech, 15 bps for others
            spread_pct = 0.0005 if order.symbol in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN"] else 0.0015
            order.spread = round(arrival * spread_pct, 4)
            # Center NBBO mid around arrival price
            order.nbbo_mid = round(arrival, 4)
            
        # Estimate commission/fees (Alpaca paper is free, live has minimal fee, IBKR is ~0.005/share)
        fee_per_share = 0.005 if order.broker == "ibkr" else 0.0001
        order.fees = round(filled_qty * fee_per_share, 2)
        
        if avg_fill_price > 0 and arrival > 0:
            # Slippage = Execution Price - Arrival Price (for BUY)
            # Slippage = Arrival Price - Execution Price (for SELL)
            price_diff = (avg_fill_price - arrival) if order.side.lower() == "buy" else (arrival - avg_fill_price)
            slippage_bps = (price_diff / arrival) * 10000.0
            order.spread = max(order.spread or 0.01, 0.01)
            
            # Spread capture: percentage of bid-ask spread captured/saved.
            # positive = executing better than mid, negative = paying spread.
            # Mid-to-execution diff relative to spread
            mid_diff = (order.nbbo_mid - avg_fill_price) if order.side.lower() == "buy" else (avg_fill_price - order.nbbo_mid)
            order.spread_pct = (mid_diff / order.spread) * 100.0
            
            # Save TCA into BrokerOrder record
            log.info(
                f"TCA recorded for order {order.id} ({order.symbol}): "
                f"Arrival: {arrival:.2f} | Fill: {avg_fill_price:.2f} | "
                f"Slippage: {slippage_bps:.1f} bps | Fees: ${order.fees:.2f}"
            )
            
            # Record individual fill tracking row if missing
            res_fill = await db.execute(
                select(Fill).where(Fill.broker_order_id == order.id)
            )
            existing_fill = res_fill.scalar_one_or_none()
            if not existing_fill and filled_qty > 0:
                res_inst = await db.execute(
                    select(Instrument).where(Instrument.ticker == order.symbol)
                )
                inst = res_inst.scalar_one_or_none()
                new_fill = Fill(
                    broker_order_id=order.id,
                    user_id=order.user_id,
                    instrument_id=inst.id if inst else None,
                    side=order.side,
                    qty=filled_qty,
                    price=avg_fill_price,
                    commission=order.fees or 0.0,
                    slippage_bps=slippage_bps
                )
                db.add(new_fill)
                
    except Exception as e:
        log.warning("Error recording TCA for order %s: %s", order.id, e, exc_info=False)
        
    return order

mutants_x_record_fill_tca__mutmut['_mutmut_orig'] = x_record_fill_tca__mutmut_orig # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_1'] = x_record_fill_tca__mutmut_1 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_2'] = x_record_fill_tca__mutmut_2 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_3'] = x_record_fill_tca__mutmut_3 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_4'] = x_record_fill_tca__mutmut_4 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_5'] = x_record_fill_tca__mutmut_5 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_6'] = x_record_fill_tca__mutmut_6 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_7'] = x_record_fill_tca__mutmut_7 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_8'] = x_record_fill_tca__mutmut_8 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_9'] = x_record_fill_tca__mutmut_9 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_10'] = x_record_fill_tca__mutmut_10 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_11'] = x_record_fill_tca__mutmut_11 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_12'] = x_record_fill_tca__mutmut_12 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_13'] = x_record_fill_tca__mutmut_13 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_14'] = x_record_fill_tca__mutmut_14 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_15'] = x_record_fill_tca__mutmut_15 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_16'] = x_record_fill_tca__mutmut_16 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_17'] = x_record_fill_tca__mutmut_17 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_18'] = x_record_fill_tca__mutmut_18 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_19'] = x_record_fill_tca__mutmut_19 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_20'] = x_record_fill_tca__mutmut_20 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_21'] = x_record_fill_tca__mutmut_21 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_22'] = x_record_fill_tca__mutmut_22 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_23'] = x_record_fill_tca__mutmut_23 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_24'] = x_record_fill_tca__mutmut_24 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_25'] = x_record_fill_tca__mutmut_25 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_26'] = x_record_fill_tca__mutmut_26 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_27'] = x_record_fill_tca__mutmut_27 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_28'] = x_record_fill_tca__mutmut_28 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_29'] = x_record_fill_tca__mutmut_29 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_30'] = x_record_fill_tca__mutmut_30 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_31'] = x_record_fill_tca__mutmut_31 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_32'] = x_record_fill_tca__mutmut_32 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_33'] = x_record_fill_tca__mutmut_33 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_34'] = x_record_fill_tca__mutmut_34 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_35'] = x_record_fill_tca__mutmut_35 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_36'] = x_record_fill_tca__mutmut_36 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_37'] = x_record_fill_tca__mutmut_37 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_38'] = x_record_fill_tca__mutmut_38 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_39'] = x_record_fill_tca__mutmut_39 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_40'] = x_record_fill_tca__mutmut_40 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_41'] = x_record_fill_tca__mutmut_41 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_42'] = x_record_fill_tca__mutmut_42 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_43'] = x_record_fill_tca__mutmut_43 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_44'] = x_record_fill_tca__mutmut_44 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_45'] = x_record_fill_tca__mutmut_45 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_46'] = x_record_fill_tca__mutmut_46 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_47'] = x_record_fill_tca__mutmut_47 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_48'] = x_record_fill_tca__mutmut_48 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_49'] = x_record_fill_tca__mutmut_49 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_50'] = x_record_fill_tca__mutmut_50 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_51'] = x_record_fill_tca__mutmut_51 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_52'] = x_record_fill_tca__mutmut_52 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_53'] = x_record_fill_tca__mutmut_53 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_54'] = x_record_fill_tca__mutmut_54 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_55'] = x_record_fill_tca__mutmut_55 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_56'] = x_record_fill_tca__mutmut_56 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_57'] = x_record_fill_tca__mutmut_57 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_58'] = x_record_fill_tca__mutmut_58 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_59'] = x_record_fill_tca__mutmut_59 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_60'] = x_record_fill_tca__mutmut_60 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_61'] = x_record_fill_tca__mutmut_61 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_62'] = x_record_fill_tca__mutmut_62 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_63'] = x_record_fill_tca__mutmut_63 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_64'] = x_record_fill_tca__mutmut_64 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_65'] = x_record_fill_tca__mutmut_65 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_66'] = x_record_fill_tca__mutmut_66 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_67'] = x_record_fill_tca__mutmut_67 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_68'] = x_record_fill_tca__mutmut_68 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_69'] = x_record_fill_tca__mutmut_69 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_70'] = x_record_fill_tca__mutmut_70 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_71'] = x_record_fill_tca__mutmut_71 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_72'] = x_record_fill_tca__mutmut_72 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_73'] = x_record_fill_tca__mutmut_73 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_74'] = x_record_fill_tca__mutmut_74 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_75'] = x_record_fill_tca__mutmut_75 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_76'] = x_record_fill_tca__mutmut_76 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_77'] = x_record_fill_tca__mutmut_77 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_78'] = x_record_fill_tca__mutmut_78 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_79'] = x_record_fill_tca__mutmut_79 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_80'] = x_record_fill_tca__mutmut_80 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_81'] = x_record_fill_tca__mutmut_81 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_82'] = x_record_fill_tca__mutmut_82 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_83'] = x_record_fill_tca__mutmut_83 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_84'] = x_record_fill_tca__mutmut_84 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_85'] = x_record_fill_tca__mutmut_85 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_86'] = x_record_fill_tca__mutmut_86 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_87'] = x_record_fill_tca__mutmut_87 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_88'] = x_record_fill_tca__mutmut_88 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_89'] = x_record_fill_tca__mutmut_89 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_90'] = x_record_fill_tca__mutmut_90 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_91'] = x_record_fill_tca__mutmut_91 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_92'] = x_record_fill_tca__mutmut_92 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_93'] = x_record_fill_tca__mutmut_93 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_94'] = x_record_fill_tca__mutmut_94 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_95'] = x_record_fill_tca__mutmut_95 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_96'] = x_record_fill_tca__mutmut_96 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_97'] = x_record_fill_tca__mutmut_97 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_98'] = x_record_fill_tca__mutmut_98 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_99'] = x_record_fill_tca__mutmut_99 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_100'] = x_record_fill_tca__mutmut_100 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_101'] = x_record_fill_tca__mutmut_101 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_102'] = x_record_fill_tca__mutmut_102 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_103'] = x_record_fill_tca__mutmut_103 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_104'] = x_record_fill_tca__mutmut_104 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_105'] = x_record_fill_tca__mutmut_105 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_106'] = x_record_fill_tca__mutmut_106 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_107'] = x_record_fill_tca__mutmut_107 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_108'] = x_record_fill_tca__mutmut_108 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_109'] = x_record_fill_tca__mutmut_109 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_110'] = x_record_fill_tca__mutmut_110 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_111'] = x_record_fill_tca__mutmut_111 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_112'] = x_record_fill_tca__mutmut_112 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_113'] = x_record_fill_tca__mutmut_113 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_114'] = x_record_fill_tca__mutmut_114 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_115'] = x_record_fill_tca__mutmut_115 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_116'] = x_record_fill_tca__mutmut_116 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_117'] = x_record_fill_tca__mutmut_117 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_118'] = x_record_fill_tca__mutmut_118 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_119'] = x_record_fill_tca__mutmut_119 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_120'] = x_record_fill_tca__mutmut_120 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_121'] = x_record_fill_tca__mutmut_121 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_122'] = x_record_fill_tca__mutmut_122 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_123'] = x_record_fill_tca__mutmut_123 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_124'] = x_record_fill_tca__mutmut_124 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_125'] = x_record_fill_tca__mutmut_125 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_126'] = x_record_fill_tca__mutmut_126 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_127'] = x_record_fill_tca__mutmut_127 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_128'] = x_record_fill_tca__mutmut_128 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_129'] = x_record_fill_tca__mutmut_129 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_130'] = x_record_fill_tca__mutmut_130 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_131'] = x_record_fill_tca__mutmut_131 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_132'] = x_record_fill_tca__mutmut_132 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_133'] = x_record_fill_tca__mutmut_133 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_134'] = x_record_fill_tca__mutmut_134 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_135'] = x_record_fill_tca__mutmut_135 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_136'] = x_record_fill_tca__mutmut_136 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_137'] = x_record_fill_tca__mutmut_137 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_138'] = x_record_fill_tca__mutmut_138 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_139'] = x_record_fill_tca__mutmut_139 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_140'] = x_record_fill_tca__mutmut_140 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_141'] = x_record_fill_tca__mutmut_141 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_142'] = x_record_fill_tca__mutmut_142 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_143'] = x_record_fill_tca__mutmut_143 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_144'] = x_record_fill_tca__mutmut_144 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_145'] = x_record_fill_tca__mutmut_145 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_146'] = x_record_fill_tca__mutmut_146 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_147'] = x_record_fill_tca__mutmut_147 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_148'] = x_record_fill_tca__mutmut_148 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_149'] = x_record_fill_tca__mutmut_149 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_150'] = x_record_fill_tca__mutmut_150 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_151'] = x_record_fill_tca__mutmut_151 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_152'] = x_record_fill_tca__mutmut_152 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_153'] = x_record_fill_tca__mutmut_153 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_154'] = x_record_fill_tca__mutmut_154 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_155'] = x_record_fill_tca__mutmut_155 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_156'] = x_record_fill_tca__mutmut_156 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_157'] = x_record_fill_tca__mutmut_157 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_158'] = x_record_fill_tca__mutmut_158 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_159'] = x_record_fill_tca__mutmut_159 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_160'] = x_record_fill_tca__mutmut_160 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_161'] = x_record_fill_tca__mutmut_161 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_162'] = x_record_fill_tca__mutmut_162 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_163'] = x_record_fill_tca__mutmut_163 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_164'] = x_record_fill_tca__mutmut_164 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_165'] = x_record_fill_tca__mutmut_165 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_166'] = x_record_fill_tca__mutmut_166 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_167'] = x_record_fill_tca__mutmut_167 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_168'] = x_record_fill_tca__mutmut_168 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_169'] = x_record_fill_tca__mutmut_169 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_170'] = x_record_fill_tca__mutmut_170 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_171'] = x_record_fill_tca__mutmut_171 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_172'] = x_record_fill_tca__mutmut_172 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_173'] = x_record_fill_tca__mutmut_173 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_174'] = x_record_fill_tca__mutmut_174 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_175'] = x_record_fill_tca__mutmut_175 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_176'] = x_record_fill_tca__mutmut_176 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_177'] = x_record_fill_tca__mutmut_177 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_178'] = x_record_fill_tca__mutmut_178 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_179'] = x_record_fill_tca__mutmut_179 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_180'] = x_record_fill_tca__mutmut_180 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_181'] = x_record_fill_tca__mutmut_181 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_182'] = x_record_fill_tca__mutmut_182 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_183'] = x_record_fill_tca__mutmut_183 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_184'] = x_record_fill_tca__mutmut_184 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_185'] = x_record_fill_tca__mutmut_185 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_186'] = x_record_fill_tca__mutmut_186 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_187'] = x_record_fill_tca__mutmut_187 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_188'] = x_record_fill_tca__mutmut_188 # type: ignore # mutmut generated
mutants_x_record_fill_tca__mutmut['x_record_fill_tca__mutmut_189'] = x_record_fill_tca__mutmut_189 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut: MutantDict = {}  # type: ignore

@_mutmut_mutated(mutants_x_calculate_expected_slippage_bps__mutmut)
def calculate_expected_slippage_bps(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_orig(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_1(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv < 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_2(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 1:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_3(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = None
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_4(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = None
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_5(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 11.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_6(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = None
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_7(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty * adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_8(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = None
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_9(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct / 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_10(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 / spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_11(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 1.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_12(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10001.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_13(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = None
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_14(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) / 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_15(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol / math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_16(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef / daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_17(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(None) * 10000.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_18(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10001.0
    
    return half_spread_bps + impact_bps

def x_calculate_expected_slippage_bps__mutmut_19(
    ticker: str,
    qty: float,
    price: float,
    adv: float = DEFAULT_ADV,
    daily_vol: float = DEFAULT_DAILY_VOL,
    spread_pct: float = DEFAULT_SPREAD_PCT
) -> float:
    """
    QENG-3c: Almgren-Chriss Market Impact Model.
    Expected Slippage (bps) = 0.5 * Spread (bps) + Volatility * (Quantity / ADV)^0.5
    """
    if adv <= 0:
        adv = DEFAULT_ADV
        
    # Constant coefficient for temporary/permanent impact scale
    impact_coef = 10.0
    
    # Participation rate
    participation = qty / adv
    
    # Half-spread term + market impact term
    half_spread_bps = 0.5 * spread_pct * 10000.0
    impact_bps = impact_coef * daily_vol * math.sqrt(participation) * 10000.0
    
    return half_spread_bps - impact_bps

mutants_x_calculate_expected_slippage_bps__mutmut['_mutmut_orig'] = x_calculate_expected_slippage_bps__mutmut_orig # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_1'] = x_calculate_expected_slippage_bps__mutmut_1 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_2'] = x_calculate_expected_slippage_bps__mutmut_2 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_3'] = x_calculate_expected_slippage_bps__mutmut_3 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_4'] = x_calculate_expected_slippage_bps__mutmut_4 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_5'] = x_calculate_expected_slippage_bps__mutmut_5 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_6'] = x_calculate_expected_slippage_bps__mutmut_6 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_7'] = x_calculate_expected_slippage_bps__mutmut_7 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_8'] = x_calculate_expected_slippage_bps__mutmut_8 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_9'] = x_calculate_expected_slippage_bps__mutmut_9 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_10'] = x_calculate_expected_slippage_bps__mutmut_10 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_11'] = x_calculate_expected_slippage_bps__mutmut_11 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_12'] = x_calculate_expected_slippage_bps__mutmut_12 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_13'] = x_calculate_expected_slippage_bps__mutmut_13 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_14'] = x_calculate_expected_slippage_bps__mutmut_14 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_15'] = x_calculate_expected_slippage_bps__mutmut_15 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_16'] = x_calculate_expected_slippage_bps__mutmut_16 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_17'] = x_calculate_expected_slippage_bps__mutmut_17 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_18'] = x_calculate_expected_slippage_bps__mutmut_18 # type: ignore # mutmut generated
mutants_x_calculate_expected_slippage_bps__mutmut['x_calculate_expected_slippage_bps__mutmut_19'] = x_calculate_expected_slippage_bps__mutmut_19 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut: MutantDict = {}  # type: ignore

@_mutmut_mutated(mutants_x_check_capacity_limits__mutmut)
async def check_capacity_limits(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_orig(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_1(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = None
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_2(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional * arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_3(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = None
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_4(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker not in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_5(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["XXAAPLXX", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_6(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["aapl", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_7(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "XXMSFTXX", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_8(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "msft", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_9(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "XXNVDAXX", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_10(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "nvda", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_11(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "XXGOOGXX", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_12(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "goog", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_13(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "XXAMZNXX", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_14(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "amzn", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_15(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "XXMETAXX", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_16(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "meta", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_17(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "XXTSLAXX"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_18(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "tsla"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_19(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = None
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_20(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10000001 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_21(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500001
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_22(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = None
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_23(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 1.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_24(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 1.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_25(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = None
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_26(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 1.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_27(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 1.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_28(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = None
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_29(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(None, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_30(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, None, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_31(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, None, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_32(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, None, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_33(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, None, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_34(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, None)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_35(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_36(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_37(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_38(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_39(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_40(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, )
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_41(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = None
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_42(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 121.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_43(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = None  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_44(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 / edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_45(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 1.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_46(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = None
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_47(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv / 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_48(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 1.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_49(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = None
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_50(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = None
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_51(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = True
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_52(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty >= max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_53(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = None
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_54(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty / arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_55(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = None
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_56(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(None)
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_57(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = None
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_58(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(None, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_59(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, None, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_60(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, None, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_61(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, None, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_62(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, None, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_63(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, None)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_64(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_65(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_66(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_67(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_68(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_69(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, )
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_70(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip >= slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_71(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = None
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_72(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv / ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_73(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) * 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_74(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) * (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_75(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold + 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_76(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct / 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_77(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 / spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_78(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 1.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_79(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10001.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_80(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol / 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_81(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 / daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_82(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (11.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_83(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10001.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_84(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 3
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_85(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 and (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_86(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty <= 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_87(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 2.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_88(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty / arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_89(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) <= (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_90(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 / notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_91(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (1.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_92(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = None
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_93(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = False
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_94(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = None
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_95(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 1.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_96(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(None)
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_97(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = None
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_98(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(None, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_99(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, None)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_100(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_101(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, )
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_102(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty / arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_103(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 3)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_104(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(None)
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_105(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(None, exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_106(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=None)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_107(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(exc_info=True)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_108(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", )
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_109(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=False)
        return False, notional, 10.0

async def x_check_capacity_limits__mutmut_110(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return True, notional, 10.0

async def x_check_capacity_limits__mutmut_111(
    db: AsyncSession,
    ticker: str,
    notional: float,
    arrival_price: float
) -> Tuple[bool, float, float]:
    """
    QENG-3c: Capacity and participation limits.
    Blocks or shrinks order size when expected slippage consumes signal edge.
    Expected edge of signal is estimated at 120 bps (typical mean-reversion gain).
    
    Returns:
      (blocked: bool, suggested_notional: float, expected_slippage_bps: float)
    """
    try:
        qty = notional / arrival_price
        
        # Simple lookup for ADV and volatility (can be extended to live fetch from database or daily stats)
        # For this implementation, we use conservative defaults or lookups for liquid tech.
        is_high_liquid = ticker in ["AAPL", "MSFT", "NVDA", "GOOG", "AMZN", "META", "TSLA"]
        adv = 10_000_000 if is_high_liquid else 500_000
        daily_vol = 0.018 if is_high_liquid else 0.035
        spread_pct = 0.0003 if is_high_liquid else 0.0018
        
        # Calculate expected slippage
        expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
        
        # Signal edge is roughly 120 bps
        edge_bps = 120.0
        slippage_threshold = 0.35 * edge_bps  # Maximum allowed slippage is 35% of the edge (42 bps)
        
        # Check participation rate limit: max 2% of ADV for any single order
        max_qty = adv * 0.02
        suggested_notional = notional
        blocked = False
        
        if qty > max_qty:
            suggested_notional = max_qty * arrival_price
            qty = max_qty
            log.warning(f"Capacity check: {ticker} order qty {qty:.0f} exceeds 2% ADV limit. Sizing down to {max_qty:.0f} shares.")
            expected_slip = calculate_expected_slippage_bps(ticker, qty, arrival_price, adv, daily_vol, spread_pct)
            
        if expected_slip > slippage_threshold:
            # Expected slippage consumes too much edge. Shrink further or block.
            # Scale down so expected slippage equals the threshold
            allowed_qty = adv * ((slippage_threshold - 0.5 * spread_pct * 10000.0) / (10.0 * daily_vol * 10000.0)) ** 2
            
            if allowed_qty < 1.0 or (allowed_qty * arrival_price) < (0.25 * notional):
                # If suggest sizing down by more than 75%, block trade entirely
                blocked = True
                suggested_notional = 0.0
                log.warning(f"Capacity check: {ticker} BLOCKED. Expected slippage ({expected_slip:.1f} bps) exceeds limit.")
            else:
                suggested_notional = round(allowed_qty * arrival_price, 2)
                log.info(f"Capacity check: {ticker} sized down from ${notional:.2f} to ${suggested_notional:.2f} due to expected slippage ({expected_slip:.1f} bps).")
                
        return blocked, suggested_notional, expected_slip
        
    except Exception as e:
        log.error(f"Error in TCA capacity check: {e}", exc_info=True)
        return False, notional, 11.0

mutants_x_check_capacity_limits__mutmut['_mutmut_orig'] = x_check_capacity_limits__mutmut_orig # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_1'] = x_check_capacity_limits__mutmut_1 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_2'] = x_check_capacity_limits__mutmut_2 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_3'] = x_check_capacity_limits__mutmut_3 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_4'] = x_check_capacity_limits__mutmut_4 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_5'] = x_check_capacity_limits__mutmut_5 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_6'] = x_check_capacity_limits__mutmut_6 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_7'] = x_check_capacity_limits__mutmut_7 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_8'] = x_check_capacity_limits__mutmut_8 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_9'] = x_check_capacity_limits__mutmut_9 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_10'] = x_check_capacity_limits__mutmut_10 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_11'] = x_check_capacity_limits__mutmut_11 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_12'] = x_check_capacity_limits__mutmut_12 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_13'] = x_check_capacity_limits__mutmut_13 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_14'] = x_check_capacity_limits__mutmut_14 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_15'] = x_check_capacity_limits__mutmut_15 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_16'] = x_check_capacity_limits__mutmut_16 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_17'] = x_check_capacity_limits__mutmut_17 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_18'] = x_check_capacity_limits__mutmut_18 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_19'] = x_check_capacity_limits__mutmut_19 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_20'] = x_check_capacity_limits__mutmut_20 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_21'] = x_check_capacity_limits__mutmut_21 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_22'] = x_check_capacity_limits__mutmut_22 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_23'] = x_check_capacity_limits__mutmut_23 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_24'] = x_check_capacity_limits__mutmut_24 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_25'] = x_check_capacity_limits__mutmut_25 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_26'] = x_check_capacity_limits__mutmut_26 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_27'] = x_check_capacity_limits__mutmut_27 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_28'] = x_check_capacity_limits__mutmut_28 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_29'] = x_check_capacity_limits__mutmut_29 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_30'] = x_check_capacity_limits__mutmut_30 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_31'] = x_check_capacity_limits__mutmut_31 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_32'] = x_check_capacity_limits__mutmut_32 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_33'] = x_check_capacity_limits__mutmut_33 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_34'] = x_check_capacity_limits__mutmut_34 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_35'] = x_check_capacity_limits__mutmut_35 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_36'] = x_check_capacity_limits__mutmut_36 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_37'] = x_check_capacity_limits__mutmut_37 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_38'] = x_check_capacity_limits__mutmut_38 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_39'] = x_check_capacity_limits__mutmut_39 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_40'] = x_check_capacity_limits__mutmut_40 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_41'] = x_check_capacity_limits__mutmut_41 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_42'] = x_check_capacity_limits__mutmut_42 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_43'] = x_check_capacity_limits__mutmut_43 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_44'] = x_check_capacity_limits__mutmut_44 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_45'] = x_check_capacity_limits__mutmut_45 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_46'] = x_check_capacity_limits__mutmut_46 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_47'] = x_check_capacity_limits__mutmut_47 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_48'] = x_check_capacity_limits__mutmut_48 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_49'] = x_check_capacity_limits__mutmut_49 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_50'] = x_check_capacity_limits__mutmut_50 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_51'] = x_check_capacity_limits__mutmut_51 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_52'] = x_check_capacity_limits__mutmut_52 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_53'] = x_check_capacity_limits__mutmut_53 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_54'] = x_check_capacity_limits__mutmut_54 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_55'] = x_check_capacity_limits__mutmut_55 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_56'] = x_check_capacity_limits__mutmut_56 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_57'] = x_check_capacity_limits__mutmut_57 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_58'] = x_check_capacity_limits__mutmut_58 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_59'] = x_check_capacity_limits__mutmut_59 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_60'] = x_check_capacity_limits__mutmut_60 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_61'] = x_check_capacity_limits__mutmut_61 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_62'] = x_check_capacity_limits__mutmut_62 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_63'] = x_check_capacity_limits__mutmut_63 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_64'] = x_check_capacity_limits__mutmut_64 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_65'] = x_check_capacity_limits__mutmut_65 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_66'] = x_check_capacity_limits__mutmut_66 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_67'] = x_check_capacity_limits__mutmut_67 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_68'] = x_check_capacity_limits__mutmut_68 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_69'] = x_check_capacity_limits__mutmut_69 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_70'] = x_check_capacity_limits__mutmut_70 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_71'] = x_check_capacity_limits__mutmut_71 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_72'] = x_check_capacity_limits__mutmut_72 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_73'] = x_check_capacity_limits__mutmut_73 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_74'] = x_check_capacity_limits__mutmut_74 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_75'] = x_check_capacity_limits__mutmut_75 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_76'] = x_check_capacity_limits__mutmut_76 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_77'] = x_check_capacity_limits__mutmut_77 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_78'] = x_check_capacity_limits__mutmut_78 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_79'] = x_check_capacity_limits__mutmut_79 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_80'] = x_check_capacity_limits__mutmut_80 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_81'] = x_check_capacity_limits__mutmut_81 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_82'] = x_check_capacity_limits__mutmut_82 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_83'] = x_check_capacity_limits__mutmut_83 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_84'] = x_check_capacity_limits__mutmut_84 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_85'] = x_check_capacity_limits__mutmut_85 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_86'] = x_check_capacity_limits__mutmut_86 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_87'] = x_check_capacity_limits__mutmut_87 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_88'] = x_check_capacity_limits__mutmut_88 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_89'] = x_check_capacity_limits__mutmut_89 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_90'] = x_check_capacity_limits__mutmut_90 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_91'] = x_check_capacity_limits__mutmut_91 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_92'] = x_check_capacity_limits__mutmut_92 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_93'] = x_check_capacity_limits__mutmut_93 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_94'] = x_check_capacity_limits__mutmut_94 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_95'] = x_check_capacity_limits__mutmut_95 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_96'] = x_check_capacity_limits__mutmut_96 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_97'] = x_check_capacity_limits__mutmut_97 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_98'] = x_check_capacity_limits__mutmut_98 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_99'] = x_check_capacity_limits__mutmut_99 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_100'] = x_check_capacity_limits__mutmut_100 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_101'] = x_check_capacity_limits__mutmut_101 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_102'] = x_check_capacity_limits__mutmut_102 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_103'] = x_check_capacity_limits__mutmut_103 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_104'] = x_check_capacity_limits__mutmut_104 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_105'] = x_check_capacity_limits__mutmut_105 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_106'] = x_check_capacity_limits__mutmut_106 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_107'] = x_check_capacity_limits__mutmut_107 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_108'] = x_check_capacity_limits__mutmut_108 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_109'] = x_check_capacity_limits__mutmut_109 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_110'] = x_check_capacity_limits__mutmut_110 # type: ignore # mutmut generated
mutants_x_check_capacity_limits__mutmut['x_check_capacity_limits__mutmut_111'] = x_check_capacity_limits__mutmut_111 # type: ignore # mutmut generated
