"""
Dark Pool / Off-Exchange Block Trades.
Powered by Massive API (massive.com)

Layer 1 — Live WebSocket stream: accumulate raw notional flow from
  off-exchange prints (Exchange ID 4 = FINRA TRF).

Layer 2 — Block Trade Reconstruction: group fragmented tape prints
  by time/price proximity and infer institutional order direction
  using the Lee-Ready tick rule (buy-initiated vs sell-initiated).
"""

import asyncio
import logging
import os
import time
from collections import defaultdict, deque
from dataclasses import dataclass
from services.http_client import get_ssl_context, shared_session

log = logging.getLogger("signal.trade.dark_pool")

# ── Raw flow accumulator ($ millions per ticker) ──────────────────────────────
_flow_data: dict[str, float] = {}

# Liveness heartbeat for the stream watchdog. handle_messages() stamps this on
# every batch the Massive client delivers; the watchdog in start_dark_pool_stream
# treats "no heartbeat for _STALL_SEC" as a dead connection. (Previously the
# watchdog read a local that nothing ever updated, so it tripped unconditionally
# every 60s and restarted the stream forever.)
_last_msg_monotonic: list[float] = [time.monotonic()]

# ── Rolling print buffer for reconstruction (last 30 minutes of prints) ───────
_MAX_BUFFER_AGE_S = 1800  # 30 minutes
_print_buffer: deque = deque(maxlen=100_000)  # (ts, symbol, price, size, notional)
_last_price: dict[str, float] = {}  # for tick rule direction inference


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


@dataclass
class _Print:
    ts: float
    symbol: str
    price: float
    size: int
    notional: float
mutants_x_handle_messages__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_handle_messages__mutmut)
def handle_messages(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_orig(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_1(messages: list):
    now = None
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_2(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = None
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_3(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[1] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_4(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = None
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_5(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(None, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_6(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, None, 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_7(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", None) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_8(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr("exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_9(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_10(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", ) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_11(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "XXexchangeXX", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_12(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "EXCHANGE", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_13(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 1) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_14(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) != 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_15(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 5
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_16(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = None
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_17(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(None, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_18(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, None, 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_19(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", None)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_20(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr("size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_21(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_22(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", )
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_23(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "XXsizeXX", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_24(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "SIZE", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_25(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 1)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_26(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = None
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_27(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(None, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_28(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, None, 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_29(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", None)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_30(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr("price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_31(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_32(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", )
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_33(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "XXpriceXX", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_34(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "PRICE", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_35(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 1.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_36(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = None

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_37(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size / price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_38(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange or (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_39(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 and notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_40(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size > 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_41(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10001 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_42(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional > 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_43(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250001):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_44(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = None
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_45(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(None, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_46(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, None, "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_47(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", None)
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_48(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr("symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_49(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_50(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", )
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_51(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "XXsymbolXX", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_52(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "SYMBOL", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_53(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "XXUNKNOWNXX")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_54(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "unknown")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_55(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = None
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_56(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) - notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_57(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(None, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_58(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, None) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_59(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_60(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, ) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_61(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 1.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_62(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional * 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_63(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1000001.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_64(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(None)
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_65(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(None, symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_66(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, None, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_67(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, None, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_68(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, None, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_69(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, None))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_70(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(symbol, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_71(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, price, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_72(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, size, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_73(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, notional))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_74(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, ))
                    _last_price[symbol] = price
    except Exception:
        pass


def x_handle_messages__mutmut_75(messages: list):
    now = time.time()
    # Watchdog heartbeat — any delivered batch means the connection is alive,
    # even if no print clears the off-exchange size/notional filter below.
    _last_msg_monotonic[0] = time.monotonic()
    try:
        from massive.websocket.models import EquityTrade

        for msg in messages:
            if isinstance(msg, EquityTrade):
                is_off_exchange = getattr(msg, "exchange", 0) == 4
                size = getattr(msg, "size", 0)
                price = getattr(msg, "price", 0.0)
                notional = size * price

                if is_off_exchange and (size >= 10_000 or notional >= 250_000):
                    symbol = getattr(msg, "symbol", "UNKNOWN")
                    _flow_data[symbol] = _flow_data.get(symbol, 0.0) + notional / 1_000_000.0
                    _print_buffer.append(_Print(now, symbol, price, size, notional))
                    _last_price[symbol] = None
    except Exception:
        pass

mutants_x_handle_messages__mutmut['_mutmut_orig'] = x_handle_messages__mutmut_orig # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_1'] = x_handle_messages__mutmut_1 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_2'] = x_handle_messages__mutmut_2 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_3'] = x_handle_messages__mutmut_3 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_4'] = x_handle_messages__mutmut_4 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_5'] = x_handle_messages__mutmut_5 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_6'] = x_handle_messages__mutmut_6 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_7'] = x_handle_messages__mutmut_7 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_8'] = x_handle_messages__mutmut_8 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_9'] = x_handle_messages__mutmut_9 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_10'] = x_handle_messages__mutmut_10 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_11'] = x_handle_messages__mutmut_11 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_12'] = x_handle_messages__mutmut_12 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_13'] = x_handle_messages__mutmut_13 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_14'] = x_handle_messages__mutmut_14 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_15'] = x_handle_messages__mutmut_15 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_16'] = x_handle_messages__mutmut_16 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_17'] = x_handle_messages__mutmut_17 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_18'] = x_handle_messages__mutmut_18 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_19'] = x_handle_messages__mutmut_19 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_20'] = x_handle_messages__mutmut_20 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_21'] = x_handle_messages__mutmut_21 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_22'] = x_handle_messages__mutmut_22 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_23'] = x_handle_messages__mutmut_23 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_24'] = x_handle_messages__mutmut_24 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_25'] = x_handle_messages__mutmut_25 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_26'] = x_handle_messages__mutmut_26 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_27'] = x_handle_messages__mutmut_27 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_28'] = x_handle_messages__mutmut_28 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_29'] = x_handle_messages__mutmut_29 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_30'] = x_handle_messages__mutmut_30 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_31'] = x_handle_messages__mutmut_31 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_32'] = x_handle_messages__mutmut_32 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_33'] = x_handle_messages__mutmut_33 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_34'] = x_handle_messages__mutmut_34 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_35'] = x_handle_messages__mutmut_35 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_36'] = x_handle_messages__mutmut_36 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_37'] = x_handle_messages__mutmut_37 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_38'] = x_handle_messages__mutmut_38 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_39'] = x_handle_messages__mutmut_39 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_40'] = x_handle_messages__mutmut_40 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_41'] = x_handle_messages__mutmut_41 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_42'] = x_handle_messages__mutmut_42 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_43'] = x_handle_messages__mutmut_43 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_44'] = x_handle_messages__mutmut_44 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_45'] = x_handle_messages__mutmut_45 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_46'] = x_handle_messages__mutmut_46 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_47'] = x_handle_messages__mutmut_47 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_48'] = x_handle_messages__mutmut_48 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_49'] = x_handle_messages__mutmut_49 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_50'] = x_handle_messages__mutmut_50 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_51'] = x_handle_messages__mutmut_51 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_52'] = x_handle_messages__mutmut_52 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_53'] = x_handle_messages__mutmut_53 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_54'] = x_handle_messages__mutmut_54 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_55'] = x_handle_messages__mutmut_55 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_56'] = x_handle_messages__mutmut_56 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_57'] = x_handle_messages__mutmut_57 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_58'] = x_handle_messages__mutmut_58 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_59'] = x_handle_messages__mutmut_59 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_60'] = x_handle_messages__mutmut_60 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_61'] = x_handle_messages__mutmut_61 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_62'] = x_handle_messages__mutmut_62 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_63'] = x_handle_messages__mutmut_63 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_64'] = x_handle_messages__mutmut_64 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_65'] = x_handle_messages__mutmut_65 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_66'] = x_handle_messages__mutmut_66 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_67'] = x_handle_messages__mutmut_67 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_68'] = x_handle_messages__mutmut_68 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_69'] = x_handle_messages__mutmut_69 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_70'] = x_handle_messages__mutmut_70 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_71'] = x_handle_messages__mutmut_71 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_72'] = x_handle_messages__mutmut_72 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_73'] = x_handle_messages__mutmut_73 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_74'] = x_handle_messages__mutmut_74 # type: ignore # mutmut generated
mutants_x_handle_messages__mutmut['x_handle_messages__mutmut_75'] = x_handle_messages__mutmut_75 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__run_darkpool_scanner__mutmut)
def _run_darkpool_scanner():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_orig():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_1():
    api_key = None
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_2():
    api_key = os.getenv(None)
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_3():
    api_key = os.getenv("XXMASSIVE_API_KEYXX")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_4():
    api_key = os.getenv("massive_api_key")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_5():
    api_key = os.getenv("MASSIVE_API_KEY")
    if api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_6():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = None
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_7():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=None, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_8():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market=None)
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_9():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_10():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, )
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_11():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="XXstocksXX")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_12():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="STOCKS")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_13():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe(None)
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_14():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("XXT.*XX")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_15():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("t.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_16():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info(None)
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_17():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("XX[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…XX")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_18():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] starting massive websocket stream for dark pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_19():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[DARK_POOL] STARTING MASSIVE WEBSOCKET STREAM FOR DARK POOL PRINTS…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_20():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(None)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_21():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = None
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_22():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).upper()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_23():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(None).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_24():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(None):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_25():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k not in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_26():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("XXplanXX", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_27():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("PLAN", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_28():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "XXupgradeXX", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_29():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "UPGRADE", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_30():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "XXsubscriptionXX", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_31():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "SUBSCRIPTION", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_32():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "XX1008XX", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_33():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "XXpolicy violationXX")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_34():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "POLICY VIOLATION")):
            raise  # let outer loop apply long backoff
        log.warning(f"[dark_pool] Massive WebSocket error: {e}")


def x__run_darkpool_scanner__mutmut_35():
    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return
    try:
        from massive import WebSocketClient

        client = WebSocketClient(api_key=api_key, market="stocks")
        client.subscribe("T.*")
        log.info("[dark_pool] Starting Massive WebSocket stream for Dark Pool prints…")
        client.run(handle_messages)
    except Exception as e:
        err = str(e).lower()
        # 1008 (policy violation) = server rejecting the subscription/entitlement —
        # same class as a plan limit, so propagate it to the outer long-backoff loop
        # instead of letting it fall through to the 10s hammer-retry.
        if any(k in err for k in ("plan", "upgrade", "subscription", "1008", "policy violation")):
            raise  # let outer loop apply long backoff
        log.warning(None)

mutants_x__run_darkpool_scanner__mutmut['_mutmut_orig'] = x__run_darkpool_scanner__mutmut_orig # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_1'] = x__run_darkpool_scanner__mutmut_1 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_2'] = x__run_darkpool_scanner__mutmut_2 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_3'] = x__run_darkpool_scanner__mutmut_3 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_4'] = x__run_darkpool_scanner__mutmut_4 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_5'] = x__run_darkpool_scanner__mutmut_5 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_6'] = x__run_darkpool_scanner__mutmut_6 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_7'] = x__run_darkpool_scanner__mutmut_7 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_8'] = x__run_darkpool_scanner__mutmut_8 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_9'] = x__run_darkpool_scanner__mutmut_9 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_10'] = x__run_darkpool_scanner__mutmut_10 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_11'] = x__run_darkpool_scanner__mutmut_11 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_12'] = x__run_darkpool_scanner__mutmut_12 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_13'] = x__run_darkpool_scanner__mutmut_13 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_14'] = x__run_darkpool_scanner__mutmut_14 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_15'] = x__run_darkpool_scanner__mutmut_15 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_16'] = x__run_darkpool_scanner__mutmut_16 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_17'] = x__run_darkpool_scanner__mutmut_17 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_18'] = x__run_darkpool_scanner__mutmut_18 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_19'] = x__run_darkpool_scanner__mutmut_19 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_20'] = x__run_darkpool_scanner__mutmut_20 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_21'] = x__run_darkpool_scanner__mutmut_21 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_22'] = x__run_darkpool_scanner__mutmut_22 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_23'] = x__run_darkpool_scanner__mutmut_23 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_24'] = x__run_darkpool_scanner__mutmut_24 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_25'] = x__run_darkpool_scanner__mutmut_25 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_26'] = x__run_darkpool_scanner__mutmut_26 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_27'] = x__run_darkpool_scanner__mutmut_27 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_28'] = x__run_darkpool_scanner__mutmut_28 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_29'] = x__run_darkpool_scanner__mutmut_29 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_30'] = x__run_darkpool_scanner__mutmut_30 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_31'] = x__run_darkpool_scanner__mutmut_31 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_32'] = x__run_darkpool_scanner__mutmut_32 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_33'] = x__run_darkpool_scanner__mutmut_33 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_34'] = x__run_darkpool_scanner__mutmut_34 # type: ignore # mutmut generated
mutants_x__run_darkpool_scanner__mutmut['x__run_darkpool_scanner__mutmut_35'] = x__run_darkpool_scanner__mutmut_35 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_start_dark_pool_stream__mutmut)
async def start_dark_pool_stream():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_orig():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_1():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_2():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv(None):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_3():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("XXMASSIVE_API_KEYXX"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_4():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("massive_api_key"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_5():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = None
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_6():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 61
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_7():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = None  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_8():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 / 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_9():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 7 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_10():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3601  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_11():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = None  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_12():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 4  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_13():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = None

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_14():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 1

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_15():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while False:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_16():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = None
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_17():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[1] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_18():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = None
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_19():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(None)
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_20():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(None))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_21():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = None
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_22():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = True
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_23():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_24():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(None)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_25():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(11)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_26():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() + _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_27():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[1] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_28():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] >= _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_29():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = None
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_30():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = False
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_31():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    return

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_32():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls = 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_33():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls -= 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_34():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 2
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_35():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls > _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_36():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        None,
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_37():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        None,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_38():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_39():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_40():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "XX[dark_pool] %d consecutive stalls with no messages — feed not XX"
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_41():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[DARK_POOL] %D CONSECUTIVE STALLS WITH NO MESSAGES — FEED NOT "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_42():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "XXdelivering (likely plan/entitlement); pausing 6hXX",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_43():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "DELIVERING (LIKELY PLAN/ENTITLEMENT); PAUSING 6H",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_44():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = None
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_45():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 1
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_46():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(None)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_47():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        None,
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_48():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        None,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_49():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        None,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_50():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        None,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_51():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_52():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_53():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_54():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_55():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "XX[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)XX",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_56():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[DARK_POOL] STREAM STALLED (%DS NO MESSAGES) — RESTARTING (%D/%D)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_57():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(None)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_58():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(6)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_59():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = None  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_60():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 1  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_61():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(None)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_62():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(6)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_63():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = None
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_64():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).upper()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_65():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(None).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_66():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(None):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_67():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k not in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_68():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("XXplanXX", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_69():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("PLAN", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_70():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "XXupgradeXX", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_71():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "UPGRADE", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_72():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "XXsubscriptionXX", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_73():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "SUBSCRIPTION", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_74():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "XXauthXX", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_75():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "AUTH", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_76():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "XX1008XX", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_77():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "XXpolicy violationXX")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_78():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "POLICY VIOLATION")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_79():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    None
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_80():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "XX[dark_pool] WebSocket access rejected (plan/entitlement or 1008 XX"
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_81():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] websocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_82():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[DARK_POOL] WEBSOCKET ACCESS REJECTED (PLAN/ENTITLEMENT OR 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_83():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "XXpolicy violation) — pausing for 6h (upgrade at massive.com/pricing)XX"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_84():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "POLICY VIOLATION) — PAUSING FOR 6H (UPGRADE AT MASSIVE.COM/PRICING)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_85():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(None)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_86():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning(None, e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_87():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", None)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_88():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning(e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_89():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", )
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_90():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("XX[dark_pool] stream error: %s — retrying in 10sXX", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_91():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[DARK_POOL] STREAM ERROR: %S — RETRYING IN 10S", e)
                await asyncio.sleep(10)


async def x_start_dark_pool_stream__mutmut_92():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(None)


async def x_start_dark_pool_stream__mutmut_93():
    """
    Start the Massive WebSocket client with a reconnect watchdog.
    If the stream silently dies (no messages for 60s), restart it automatically.
    Plan-limit errors back off for 6 hours instead of hammering every 15s.
    """
    if not os.getenv("MASSIVE_API_KEY"):
        return

    _STALL_SEC = 60
    _PLAN_LIMIT_BACKOFF = 6 * 3600  # 6 hours — plan/entitlement won't change sooner
    _MAX_CONSEC_STALLS = 3  # after this many empty stalls, treat feed as dead → long backoff
    consec_stalls = 0

    while True:
        try:
            # Fresh liveness window per attempt — otherwise a stale heartbeat makes
            # the next connection "stall" within one 10s tick instead of _STALL_SEC.
            _last_msg_monotonic[0] = time.monotonic()
            stream_task = asyncio.create_task(asyncio.to_thread(_run_darkpool_scanner))
            stalled = False
            while not stream_task.done():
                await asyncio.sleep(10)
                if time.monotonic() - _last_msg_monotonic[0] > _STALL_SEC:
                    stream_task.cancel()
                    stalled = True
                    break

            if stalled:
                consec_stalls += 1
                if consec_stalls >= _MAX_CONSEC_STALLS:
                    # Connects but never delivers — almost always a plan/entitlement
                    # gap (the firehose just isn't sent). Stop hammering every ~15s.
                    log.warning(
                        "[dark_pool] %d consecutive stalls with no messages — feed not "
                        "delivering (likely plan/entitlement); pausing 6h",
                        consec_stalls,
                    )
                    consec_stalls = 0
                    await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
                else:
                    log.warning(
                        "[dark_pool] stream stalled (%ds no messages) — restarting (%d/%d)",
                        _STALL_SEC,
                        consec_stalls,
                        _MAX_CONSEC_STALLS,
                    )
                    await asyncio.sleep(5)
            else:
                await stream_task  # surface any exception (e.g. plan-limit AuthError)
                consec_stalls = 0  # clean exit without stall → reset the counter
                await asyncio.sleep(5)
        except asyncio.CancelledError:
            return
        except Exception as e:
            err = str(e).lower()
            if any(k in err for k in ("plan", "upgrade", "subscription", "auth", "1008", "policy violation")):
                log.warning(
                    "[dark_pool] WebSocket access rejected (plan/entitlement or 1008 "
                    "policy violation) — pausing for 6h (upgrade at massive.com/pricing)"
                )
                await asyncio.sleep(_PLAN_LIMIT_BACKOFF)
            else:
                log.warning("[dark_pool] stream error: %s — retrying in 10s", e)
                await asyncio.sleep(11)

mutants_x_start_dark_pool_stream__mutmut['_mutmut_orig'] = x_start_dark_pool_stream__mutmut_orig # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_1'] = x_start_dark_pool_stream__mutmut_1 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_2'] = x_start_dark_pool_stream__mutmut_2 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_3'] = x_start_dark_pool_stream__mutmut_3 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_4'] = x_start_dark_pool_stream__mutmut_4 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_5'] = x_start_dark_pool_stream__mutmut_5 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_6'] = x_start_dark_pool_stream__mutmut_6 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_7'] = x_start_dark_pool_stream__mutmut_7 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_8'] = x_start_dark_pool_stream__mutmut_8 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_9'] = x_start_dark_pool_stream__mutmut_9 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_10'] = x_start_dark_pool_stream__mutmut_10 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_11'] = x_start_dark_pool_stream__mutmut_11 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_12'] = x_start_dark_pool_stream__mutmut_12 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_13'] = x_start_dark_pool_stream__mutmut_13 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_14'] = x_start_dark_pool_stream__mutmut_14 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_15'] = x_start_dark_pool_stream__mutmut_15 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_16'] = x_start_dark_pool_stream__mutmut_16 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_17'] = x_start_dark_pool_stream__mutmut_17 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_18'] = x_start_dark_pool_stream__mutmut_18 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_19'] = x_start_dark_pool_stream__mutmut_19 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_20'] = x_start_dark_pool_stream__mutmut_20 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_21'] = x_start_dark_pool_stream__mutmut_21 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_22'] = x_start_dark_pool_stream__mutmut_22 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_23'] = x_start_dark_pool_stream__mutmut_23 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_24'] = x_start_dark_pool_stream__mutmut_24 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_25'] = x_start_dark_pool_stream__mutmut_25 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_26'] = x_start_dark_pool_stream__mutmut_26 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_27'] = x_start_dark_pool_stream__mutmut_27 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_28'] = x_start_dark_pool_stream__mutmut_28 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_29'] = x_start_dark_pool_stream__mutmut_29 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_30'] = x_start_dark_pool_stream__mutmut_30 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_31'] = x_start_dark_pool_stream__mutmut_31 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_32'] = x_start_dark_pool_stream__mutmut_32 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_33'] = x_start_dark_pool_stream__mutmut_33 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_34'] = x_start_dark_pool_stream__mutmut_34 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_35'] = x_start_dark_pool_stream__mutmut_35 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_36'] = x_start_dark_pool_stream__mutmut_36 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_37'] = x_start_dark_pool_stream__mutmut_37 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_38'] = x_start_dark_pool_stream__mutmut_38 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_39'] = x_start_dark_pool_stream__mutmut_39 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_40'] = x_start_dark_pool_stream__mutmut_40 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_41'] = x_start_dark_pool_stream__mutmut_41 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_42'] = x_start_dark_pool_stream__mutmut_42 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_43'] = x_start_dark_pool_stream__mutmut_43 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_44'] = x_start_dark_pool_stream__mutmut_44 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_45'] = x_start_dark_pool_stream__mutmut_45 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_46'] = x_start_dark_pool_stream__mutmut_46 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_47'] = x_start_dark_pool_stream__mutmut_47 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_48'] = x_start_dark_pool_stream__mutmut_48 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_49'] = x_start_dark_pool_stream__mutmut_49 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_50'] = x_start_dark_pool_stream__mutmut_50 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_51'] = x_start_dark_pool_stream__mutmut_51 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_52'] = x_start_dark_pool_stream__mutmut_52 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_53'] = x_start_dark_pool_stream__mutmut_53 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_54'] = x_start_dark_pool_stream__mutmut_54 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_55'] = x_start_dark_pool_stream__mutmut_55 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_56'] = x_start_dark_pool_stream__mutmut_56 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_57'] = x_start_dark_pool_stream__mutmut_57 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_58'] = x_start_dark_pool_stream__mutmut_58 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_59'] = x_start_dark_pool_stream__mutmut_59 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_60'] = x_start_dark_pool_stream__mutmut_60 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_61'] = x_start_dark_pool_stream__mutmut_61 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_62'] = x_start_dark_pool_stream__mutmut_62 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_63'] = x_start_dark_pool_stream__mutmut_63 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_64'] = x_start_dark_pool_stream__mutmut_64 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_65'] = x_start_dark_pool_stream__mutmut_65 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_66'] = x_start_dark_pool_stream__mutmut_66 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_67'] = x_start_dark_pool_stream__mutmut_67 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_68'] = x_start_dark_pool_stream__mutmut_68 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_69'] = x_start_dark_pool_stream__mutmut_69 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_70'] = x_start_dark_pool_stream__mutmut_70 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_71'] = x_start_dark_pool_stream__mutmut_71 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_72'] = x_start_dark_pool_stream__mutmut_72 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_73'] = x_start_dark_pool_stream__mutmut_73 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_74'] = x_start_dark_pool_stream__mutmut_74 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_75'] = x_start_dark_pool_stream__mutmut_75 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_76'] = x_start_dark_pool_stream__mutmut_76 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_77'] = x_start_dark_pool_stream__mutmut_77 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_78'] = x_start_dark_pool_stream__mutmut_78 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_79'] = x_start_dark_pool_stream__mutmut_79 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_80'] = x_start_dark_pool_stream__mutmut_80 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_81'] = x_start_dark_pool_stream__mutmut_81 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_82'] = x_start_dark_pool_stream__mutmut_82 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_83'] = x_start_dark_pool_stream__mutmut_83 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_84'] = x_start_dark_pool_stream__mutmut_84 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_85'] = x_start_dark_pool_stream__mutmut_85 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_86'] = x_start_dark_pool_stream__mutmut_86 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_87'] = x_start_dark_pool_stream__mutmut_87 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_88'] = x_start_dark_pool_stream__mutmut_88 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_89'] = x_start_dark_pool_stream__mutmut_89 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_90'] = x_start_dark_pool_stream__mutmut_90 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_91'] = x_start_dark_pool_stream__mutmut_91 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_92'] = x_start_dark_pool_stream__mutmut_92 # type: ignore # mutmut generated
mutants_x_start_dark_pool_stream__mutmut['x_start_dark_pool_stream__mutmut_93'] = x_start_dark_pool_stream__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_dark_pool_flow__mutmut)
async def get_dark_pool_flow(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_orig(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_1(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"XXnet_flow_mXX": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_2(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"NET_FLOW_M": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_3(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(None, 2)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_4(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], None)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_5(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(2)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_6(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], )} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_7(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 3)} for t in tickers if _flow_data.get(t, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_8(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(None, 0.0) > 0}


async def x_get_dark_pool_flow__mutmut_9(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, None) > 0}


async def x_get_dark_pool_flow__mutmut_10(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(0.0) > 0}


async def x_get_dark_pool_flow__mutmut_11(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, ) > 0}


async def x_get_dark_pool_flow__mutmut_12(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, 1.0) > 0}


async def x_get_dark_pool_flow__mutmut_13(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, 0.0) >= 0}


async def x_get_dark_pool_flow__mutmut_14(tickers: list[str]) -> dict:
    """
    Return accumulated off-exchange (dark pool) block flow.
    Returns: { ticker: {"net_flow_m": float_millions} }
    """
    return {t: {"net_flow_m": round(_flow_data[t], 2)} for t in tickers if _flow_data.get(t, 0.0) > 1}

mutants_x_get_dark_pool_flow__mutmut['_mutmut_orig'] = x_get_dark_pool_flow__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_1'] = x_get_dark_pool_flow__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_2'] = x_get_dark_pool_flow__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_3'] = x_get_dark_pool_flow__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_4'] = x_get_dark_pool_flow__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_5'] = x_get_dark_pool_flow__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_6'] = x_get_dark_pool_flow__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_7'] = x_get_dark_pool_flow__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_8'] = x_get_dark_pool_flow__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_9'] = x_get_dark_pool_flow__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_10'] = x_get_dark_pool_flow__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_11'] = x_get_dark_pool_flow__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_12'] = x_get_dark_pool_flow__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_13'] = x_get_dark_pool_flow__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_dark_pool_flow__mutmut['x_get_dark_pool_flow__mutmut_14'] = x_get_dark_pool_flow__mutmut_14 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut: MutantDict = {}  # type: ignore


# ── Block Trade Reconstruction ────────────────────────────────────────────────


@_mutmut_mutated(mutants_x__tick_rule__mutmut)
def _tick_rule(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "buy"
    elif current_price < prev_price:
        return "sell"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_orig(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "buy"
    elif current_price < prev_price:
        return "sell"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_1(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price >= prev_price:
        return "buy"
    elif current_price < prev_price:
        return "sell"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_2(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "XXbuyXX"
    elif current_price < prev_price:
        return "sell"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_3(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "BUY"
    elif current_price < prev_price:
        return "sell"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_4(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "buy"
    elif current_price <= prev_price:
        return "sell"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_5(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "buy"
    elif current_price < prev_price:
        return "XXsellXX"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_6(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "buy"
    elif current_price < prev_price:
        return "SELL"
    return "unknown"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_7(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "buy"
    elif current_price < prev_price:
        return "sell"
    return "XXunknownXX"


# ── Block Trade Reconstruction ────────────────────────────────────────────────


def x__tick_rule__mutmut_8(current_price: float, prev_price: float) -> str:
    """
    Lee-Ready tick rule: infer trade direction from price movement.
      uptick   (price > prev)  → buy-initiated
      downtick (price < prev)  → sell-initiated
      zero tick → carry forward previous direction
    """
    if current_price > prev_price:
        return "buy"
    elif current_price < prev_price:
        return "sell"
    return "UNKNOWN"

mutants_x__tick_rule__mutmut['_mutmut_orig'] = x__tick_rule__mutmut_orig # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_1'] = x__tick_rule__mutmut_1 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_2'] = x__tick_rule__mutmut_2 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_3'] = x__tick_rule__mutmut_3 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_4'] = x__tick_rule__mutmut_4 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_5'] = x__tick_rule__mutmut_5 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_6'] = x__tick_rule__mutmut_6 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_7'] = x__tick_rule__mutmut_7 # type: ignore # mutmut generated
mutants_x__tick_rule__mutmut['x__tick_rule__mutmut_8'] = x__tick_rule__mutmut_8 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__reconstruct_orders__mutmut)
def _reconstruct_orders(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_orig(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_1(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_2(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = None
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_3(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(None)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_4(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(None)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_5(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = None
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_6(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = None  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_7(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 31.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_8(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = None  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_9(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 1.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_10(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=None)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_11(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: None)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_12(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = None
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_13(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[1].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_14(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = None
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_15(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = None
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_16(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[1]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_17(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = None

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_18(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[1].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_19(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[2:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_20(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = None
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_21(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts + current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_22(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[+1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_23(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-2].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_24(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = None
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_25(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) * (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_26(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(None) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_27(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price + ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_28(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price and 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_29(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 2)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_30(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW or price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_31(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap < TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_32(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap < PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_33(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(None)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_34(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(None)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_35(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = None
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_36(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = None
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_37(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(None)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_38(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = None
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_39(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(None)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_40(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional <= 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_41(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500001:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_42(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                break

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_43(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = None
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_44(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(None)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_45(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = None

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_46(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional * total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_47(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 1.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_48(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = None
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_49(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = None
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_50(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[1].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_51(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = None
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_52(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(None, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_53(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, None)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_54(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_55(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, )
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_56(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d == "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_57(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "XXunknownXX":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_58(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "UNKNOWN":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_59(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(None)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_60(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = None

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_61(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = None
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_62(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count(None)
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_63(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("XXbuyXX")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_64(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("BUY")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_65(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = None
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_66(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count(None)
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_67(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("XXsellXX")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_68(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("SELL")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_69(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count >= sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_70(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = None
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_71(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "XXbuyXX"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_72(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "BUY"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_73(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = None
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_74(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count * len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_75(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 1.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_76(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count >= buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_77(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = None
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_78(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "XXsellXX"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_79(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "SELL"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_80(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = None
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_81(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count * len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_82(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 1.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_83(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = None
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_84(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "XXunknownXX"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_85(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "UNKNOWN"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_86(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = None

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_87(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 1.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_88(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                None
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_89(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "XXsymbolXX": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_90(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "SYMBOL": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_91(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "XXdirectionXX": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_92(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "DIRECTION": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_93(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "XXdirection_confXX": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_94(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "DIRECTION_CONF": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_95(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(None, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_96(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, None),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_97(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_98(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, ),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_99(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 3),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_100(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "XXtotal_notional_mXX": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_101(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "TOTAL_NOTIONAL_M": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_102(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(None, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_103(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, None),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_104(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_105(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, ),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_106(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional * 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_107(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1000001, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_108(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 3),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_109(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "XXtotal_sizeXX": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_110(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "TOTAL_SIZE": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_111(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "XXvwapXX": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_112(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "VWAP": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_113(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(None, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_114(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, None),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_115(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_116(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, ),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_117(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 3),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_118(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "XXprint_countXX": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_119(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "PRINT_COUNT": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_120(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "XXtime_startXX": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_121(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "TIME_START": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_122(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(None),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_123(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[1].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_124(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "XXtime_endXX": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_125(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "TIME_END": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_126(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(None),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_127(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[+1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_128(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-2].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_129(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "XXduration_sXX": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_130(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "DURATION_S": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_131(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(None, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_132(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, None),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_133(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_134(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, ),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_135(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts + group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_136(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[+1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_137(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-2].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_138(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[1].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_139(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 2),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_140(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=None, reverse=True)
    return orders


def x__reconstruct_orders__mutmut_141(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=None)
    return orders


def x__reconstruct_orders__mutmut_142(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(reverse=True)
    return orders


def x__reconstruct_orders__mutmut_143(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], )
    return orders


def x__reconstruct_orders__mutmut_144(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: None, reverse=True)
    return orders


def x__reconstruct_orders__mutmut_145(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["XXtotal_notional_mXX"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_146(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["TOTAL_NOTIONAL_M"], reverse=True)
    return orders


def x__reconstruct_orders__mutmut_147(prints: list[_Print]) -> list[dict]:
    """
    Group fragmented dark pool tape prints into reconstructed institutional orders.

    Grouping criteria:
      - Same symbol
      - Within 30 seconds of each other
      - Price within 0.5% of group reference price

    Direction: Lee-Ready tick rule applied per print, then majority vote
    on the group. Ambiguous groups (50/50) are labelled 'unknown'.

    Aggregation: sum size, sum notional, VWAP, time range.
    """
    if not prints:
        return []

    # Sort chronologically per symbol
    by_sym: dict[str, list[_Print]] = defaultdict(list)
    for p in prints:
        by_sym[p.symbol].append(p)

    orders = []
    TIME_WINDOW = 30.0  # seconds
    PRICE_WINDOW = 0.005  # 0.5%

    for sym, sym_prints in by_sym.items():
        sym_prints.sort(key=lambda p: p.ts)
        prev_price = sym_prints[0].price
        groups: list[list[_Print]] = []
        current_group: list[_Print] = [sym_prints[0]]
        ref_price = sym_prints[0].price

        for p in sym_prints[1:]:
            time_gap = p.ts - current_group[-1].ts
            price_gap = abs(p.price - ref_price) / (ref_price or 1)
            if time_gap <= TIME_WINDOW and price_gap <= PRICE_WINDOW:
                current_group.append(p)
            else:
                groups.append(current_group)
                current_group = [p]
                ref_price = p.price
        groups.append(current_group)

        # Filter: only materialise groups with ≥ $500k notional
        for group in groups:
            total_notional = sum(p.notional for p in group)
            if total_notional < 500_000:
                continue

            total_size = sum(p.size for p in group)
            vwap = total_notional / total_size if total_size else 0.0

            # Direction via tick rule on each print
            directions = []
            lp = group[0].price
            for p in group:
                d = _tick_rule(p.price, lp)
                if d != "unknown":
                    directions.append(d)
                lp = p.price

            buy_count = directions.count("buy")
            sell_count = directions.count("sell")
            if buy_count > sell_count:
                direction = "buy"
                confidence = buy_count / len(directions) if directions else 0.5
            elif sell_count > buy_count:
                direction = "sell"
                confidence = sell_count / len(directions) if directions else 0.5
            else:
                direction = "unknown"
                confidence = 0.5

            orders.append(
                {
                    "symbol": sym,
                    "direction": direction,
                    "direction_conf": round(confidence, 2),
                    "total_notional_m": round(total_notional / 1_000_000, 2),
                    "total_size": total_size,
                    "vwap": round(vwap, 2),
                    "print_count": len(group),
                    "time_start": int(group[0].ts),
                    "time_end": int(group[-1].ts),
                    "duration_s": round(group[-1].ts - group[0].ts, 1),
                }
            )

    orders.sort(key=lambda o: o["total_notional_m"], reverse=False)
    return orders

mutants_x__reconstruct_orders__mutmut['_mutmut_orig'] = x__reconstruct_orders__mutmut_orig # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut
# type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_126'] = x__reconstruct_orders__mutmut_126 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_127'] = x__reconstruct_orders__mutmut_127 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_128'] = x__reconstruct_orders__mutmut_128 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_129'] = x__reconstruct_orders__mutmut_129 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_130'] = x__reconstruct_orders__mutmut_130 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_131'] = x__reconstruct_orders__mutmut_131 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_132'] = x__reconstruct_orders__mutmut_132 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_133'] = x__reconstruct_orders__mutmut_133 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_134'] = x__reconstruct_orders__mutmut_134 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_135'] = x__reconstruct_orders__mutmut_135 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_136'] = x__reconstruct_orders__mutmut_136 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_137'] = x__reconstruct_orders__mutmut_137 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_138'] = x__reconstruct_orders__mutmut_138 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_139'] = x__reconstruct_orders__mutmut_139 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_140'] = x__reconstruct_orders__mutmut_140 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_141'] = x__reconstruct_orders__mutmut_141 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_142'] = x__reconstruct_orders__mutmut_142 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_143'] = x__reconstruct_orders__mutmut_143 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_144'] = x__reconstruct_orders__mutmut_144 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_145'] = x__reconstruct_orders__mutmut_145 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_146'] = x__reconstruct_orders__mutmut_146 # type: ignore # mutmut generated
mutants_x__reconstruct_orders__mutmut['x__reconstruct_orders__mutmut_147'] = x__reconstruct_orders__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_reconstructed_orders__mutmut)
async def get_reconstructed_orders(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_orig(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_1(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = None
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_2(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = None

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_3(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now + _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_4(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = None
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_5(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts > cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_6(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = None

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_7(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol != ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_8(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = None

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_9(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(None, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_10(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, None)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_11(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_12(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, )

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_13(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = None
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_14(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(None)
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_15(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["XXtotal_notional_mXX"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_16(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["TOTAL_NOTIONAL_M"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_17(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["XXdirectionXX"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_18(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["DIRECTION"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_19(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] != "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_20(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "XXbuyXX")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_21(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "BUY")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_22(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = None
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_23(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(None)
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_24(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["XXtotal_notional_mXX"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_25(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["TOTAL_NOTIONAL_M"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_26(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["XXdirectionXX"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_27(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["DIRECTION"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_28(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] != "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_29(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "XXsellXX")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_30(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "SELL")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_31(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = None

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_32(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total + sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_33(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "XXtickerXX": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_34(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "TICKER": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_35(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker and "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_36(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "XXALLXX",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_37(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "all",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_38(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "XXwindow_minutesXX": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_39(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "WINDOW_MINUTES": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_40(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S / 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_41(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 61,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_42(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "XXprint_countXX": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_43(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "PRINT_COUNT": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_44(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "XXordersXX": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_45(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "ORDERS": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_46(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:51],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_47(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "XXsummaryXX": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_48(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "SUMMARY": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_49(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "XXbuy_flow_mXX": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_50(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "BUY_FLOW_M": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_51(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(None, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_52(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, None),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_53(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_54(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, ),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_55(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 3),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_56(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "XXsell_flow_mXX": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_57(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "SELL_FLOW_M": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_58(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(None, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_59(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, None),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_60(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_61(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, ),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_62(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 3),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_63(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "XXnet_flow_mXX": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_64(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "NET_FLOW_M": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_65(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(None, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_66(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, None),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_67(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_68(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, ),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_69(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 3),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_70(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "XXdirectionXX": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_71(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "DIRECTION": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_72(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "XXbuyXX" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_73(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "BUY" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_74(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow >= 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_75(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 2 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_76(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "XXsellXX" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_77(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "SELL" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_78(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow <= -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_79(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < +1 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_80(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -2 else "neutral",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_81(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "XXneutralXX",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_82(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "NEUTRAL",
        },
        "has_live_data": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_83(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "XXhas_live_dataXX": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_84(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "HAS_LIVE_DATA": len(active_prints) > 0,
    }


async def x_get_reconstructed_orders__mutmut_85(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) >= 0,
    }


async def x_get_reconstructed_orders__mutmut_86(ticker: str | None = None) -> dict:
    """
    Public entrypoint: reconstruct institutional orders from buffered prints.
    If ticker is provided, filter to that symbol only.
    Returns aggregated view + list of reconstructed orders (largest first).
    """
    now = time.time()
    cutoff = now - _MAX_BUFFER_AGE_S

    # Drain stale prints from buffer
    active_prints = [p for p in _print_buffer if p.ts >= cutoff]
    if ticker:
        active_prints = [p for p in active_prints if p.symbol == ticker]

    orders = await asyncio.to_thread(_reconstruct_orders, active_prints)

    # Net flow summary: buy $ - sell $
    buy_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "buy")
    sell_total = sum(o["total_notional_m"] for o in orders if o["direction"] == "sell")
    net_flow = buy_total - sell_total

    return {
        "ticker": ticker or "ALL",
        "window_minutes": _MAX_BUFFER_AGE_S // 60,
        "print_count": len(active_prints),
        "orders": orders[:50],  # top 50 by notional
        "summary": {
            "buy_flow_m": round(buy_total, 2),
            "sell_flow_m": round(sell_total, 2),
            "net_flow_m": round(net_flow, 2),
            "direction": "buy" if net_flow > 1 else "sell" if net_flow < -1 else "neutral",
        },
        "has_live_data": len(active_prints) > 1,
    }

mutants_x_get_reconstructed_orders__mutmut['_mutmut_orig'] = x_get_reconstructed_orders__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_1'] = x_get_reconstructed_orders__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_2'] = x_get_reconstructed_orders__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_3'] = x_get_reconstructed_orders__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_4'] = x_get_reconstructed_orders__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_5'] = x_get_reconstructed_orders__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_6'] = x_get_reconstructed_orders__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_7'] = x_get_reconstructed_orders__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_8'] = x_get_reconstructed_orders__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_9'] = x_get_reconstructed_orders__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_10'] = x_get_reconstructed_orders__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_11'] = x_get_reconstructed_orders__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_12'] = x_get_reconstructed_orders__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_13'] = x_get_reconstructed_orders__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_14'] = x_get_reconstructed_orders__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_15'] = x_get_reconstructed_orders__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_16'] = x_get_reconstructed_orders__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_17'] = x_get_reconstructed_orders__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_18'] = x_get_reconstructed_orders__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_19'] = x_get_reconstructed_orders__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_20'] = x_get_reconstructed_orders__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_21'] = x_get_reconstructed_orders__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_22'] = x_get_reconstructed_orders__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_23'] = x_get_reconstructed_orders__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_24'] = x_get_reconstructed_orders__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_25'] = x_get_reconstructed_orders__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_26'] = x_get_reconstructed_orders__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_27'] = x_get_reconstructed_orders__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_28'] = x_get_reconstructed_orders__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_29'] = x_get_reconstructed_orders__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_30'] = x_get_reconstructed_orders__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_31'] = x_get_reconstructed_orders__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_32'] = x_get_reconstructed_orders__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_33'] = x_get_reconstructed_orders__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_34'] = x_get_reconstructed_orders__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_35'] = x_get_reconstructed_orders__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_36'] = x_get_reconstructed_orders__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_37'] = x_get_reconstructed_orders__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_38'] = x_get_reconstructed_orders__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_39'] = x_get_reconstructed_orders__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_40'] = x_get_reconstructed_orders__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_41'] = x_get_reconstructed_orders__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_42'] = x_get_reconstructed_orders__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_43'] = x_get_reconstructed_orders__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_44'] = x_get_reconstructed_orders__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_45'] = x_get_reconstructed_orders__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_46'] = x_get_reconstructed_orders__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_47'] = x_get_reconstructed_orders__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_48'] = x_get_reconstructed_orders__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_49'] = x_get_reconstructed_orders__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_50'] = x_get_reconstructed_orders__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_51'] = x_get_reconstructed_orders__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_52'] = x_get_reconstructed_orders__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_53'] = x_get_reconstructed_orders__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_54'] = x_get_reconstructed_orders__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_55'] = x_get_reconstructed_orders__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_56'] = x_get_reconstructed_orders__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_57'] = x_get_reconstructed_orders__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_58'] = x_get_reconstructed_orders__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_59'] = x_get_reconstructed_orders__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_60'] = x_get_reconstructed_orders__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_61'] = x_get_reconstructed_orders__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_62'] = x_get_reconstructed_orders__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_63'] = x_get_reconstructed_orders__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_64'] = x_get_reconstructed_orders__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_65'] = x_get_reconstructed_orders__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_66'] = x_get_reconstructed_orders__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_67'] = x_get_reconstructed_orders__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_68'] = x_get_reconstructed_orders__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_69'] = x_get_reconstructed_orders__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_70'] = x_get_reconstructed_orders__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_71'] = x_get_reconstructed_orders__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_72'] = x_get_reconstructed_orders__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_73'] = x_get_reconstructed_orders__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_74'] = x_get_reconstructed_orders__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_75'] = x_get_reconstructed_orders__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_76'] = x_get_reconstructed_orders__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_77'] = x_get_reconstructed_orders__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_78'] = x_get_reconstructed_orders__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_79'] = x_get_reconstructed_orders__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_80'] = x_get_reconstructed_orders__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_81'] = x_get_reconstructed_orders__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_82'] = x_get_reconstructed_orders__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_83'] = x_get_reconstructed_orders__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_84'] = x_get_reconstructed_orders__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_85'] = x_get_reconstructed_orders__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_reconstructed_orders__mutmut['x_get_reconstructed_orders__mutmut_86'] = x_get_reconstructed_orders__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_massive_advanced_signals__mutmut)
async def get_massive_advanced_signals(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_orig(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_1(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = None
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_2(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") and os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_3(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv(None) or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_4(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("XXPOLYGON_API_KEYXX") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_5(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("polygon_api_key") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_6(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv(None)
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_7(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("XXMASSIVE_API_KEYXX")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_8(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("massive_api_key")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_9(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_10(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = None

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_11(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = None

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_12(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "XXftdXX": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_13(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "FTD": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_14(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"XXis_reg_shoXX": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_15(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"IS_REG_SHO": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_16(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": True, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_17(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "XXspike_pctXX": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_18(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "SPIKE_PCT": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_19(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 1.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_20(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "XXcorp_actionsXX": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_21(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "CORP_ACTIONS": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_22(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"XXex_div_soonXX": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_23(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"EX_DIV_SOON": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_24(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": True, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_25(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "XXex_div_dateXX": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_26(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "EX_DIV_DATE": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_27(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "XXsplit_soonXX": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_28(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "SPLIT_SOON": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_29(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": True},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_30(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "XXdark_pool_flowXX": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_31(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "DARK_POOL_FLOW": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_32(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(None, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_33(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, None),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_34(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_35(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, ),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_36(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 1.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_37(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = None
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_38(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = None
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_39(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today - timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_40(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=None)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_41(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=15)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_42(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = None

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_43(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today - timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_44(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=None)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_45(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=8)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_46(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = None
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_47(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["XXapiKeyXX"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_48(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apikey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_49(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["APIKEY"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_50(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(None, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_51(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=None, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_52(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=None, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_53(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=None) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_54(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_55(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_56(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_57(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_58(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=7, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_59(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status != 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_60(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 201:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_61(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = None
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_62(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                None,
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_63(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                None,
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_64(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_65(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_66(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "XXhttps://api.polygon.io/v3/reference/dividendsXX",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_67(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "HTTPS://API.POLYGON.IO/V3/REFERENCE/DIVIDENDS",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_68(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"XXtickerXX": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_69(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"TICKER": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_70(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "XXex_dividend_date.lteXX": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_71(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "EX_DIVIDEND_DATE.LTE": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_72(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "XXorderXX": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_73(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "ORDER": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_74(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "XXascXX", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_75(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "ASC", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_76(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "XXlimitXX": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_77(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "LIMIT": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_78(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 6},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_79(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") and []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_80(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get(None) or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_81(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("XXresultsXX") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_82(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("RESULTS") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_83(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = None
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_84(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get(None, "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_85(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", None)
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_86(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_87(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", )
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_88(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("XXex_dividend_dateXX", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_89(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("EX_DIVIDEND_DATE", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_90(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "XXXX")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_91(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date or ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_92(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date > today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_93(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = None
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_94(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["XXcorp_actionsXX"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_95(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["CORP_ACTIONS"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_96(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["XXex_div_soonXX"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_97(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["EX_DIV_SOON"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_98(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = False
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_99(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = None
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_100(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["XXcorp_actionsXX"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_101(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["CORP_ACTIONS"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_102(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["XXex_div_dateXX"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_103(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["EX_DIV_DATE"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_104(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    return

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_105(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = None
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_106(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                None,
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_107(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                None,
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_108(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_109(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_110(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "XXhttps://api.polygon.io/v3/reference/splitsXX",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_111(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "HTTPS://API.POLYGON.IO/V3/REFERENCE/SPLITS",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_112(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"XXtickerXX": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_113(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"TICKER": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_114(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "XXexecution_date.lteXX": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_115(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "EXECUTION_DATE.LTE": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_116(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "XXorderXX": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_117(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "ORDER": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_118(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "XXascXX", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_119(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "ASC", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_120(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "XXlimitXX": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_121(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "LIMIT": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_122(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 6},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_123(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") and []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_124(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get(None) or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_125(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("XXresultsXX") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_126(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("RESULTS") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_127(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = None
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_128(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get(None, "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_129(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", None)
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_130(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_131(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", )
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_132(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("XXexecution_dateXX", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_133(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("EXECUTION_DATE", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_134(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "XXXX")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_135(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date or ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_136(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date > today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_137(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = None
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_138(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["XXcorp_actionsXX"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_139(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["CORP_ACTIONS"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_140(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["XXsplit_soonXX"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_141(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["SPLIT_SOON"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_142(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = False
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_143(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    return

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_144(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = None
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_145(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                None,
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_146(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                None,
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_147(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_148(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_149(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "XXhttps://api.polygon.io/v2/reference/ftdXX",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_150(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "HTTPS://API.POLYGON.IO/V2/REFERENCE/FTD",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_151(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"XXsymbolXX": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_152(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"SYMBOL": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_153(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "XXlimitXX": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_154(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "LIMIT": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_155(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 11},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_156(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = None
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_157(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") and []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_158(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get(None) or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_159(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("XXresultsXX") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_160(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("RESULTS") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_161(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = None
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_162(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[1]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_163(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = None
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_164(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(None)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_165(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) and 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_166(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get(None, 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_167(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", None) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_168(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get(0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_169(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", ) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_170(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("XXquantityXX", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_171(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("QUANTITY", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_172(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 1) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_173(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 1)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_174(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = None
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_175(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(None) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_176(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) and 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_177(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get(None, 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_178(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", None) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_179(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get(0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_180(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", ) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_181(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[2].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_182(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("XXquantityXX", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_183(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("QUANTITY", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_184(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 1) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_185(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 1) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_186(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) >= 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_187(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 2 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_188(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 1.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_189(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = None
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_190(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q / 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_191(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) * prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_192(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty + prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_193(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 101) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_194(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q >= 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_195(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 1 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_196(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 1.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_197(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = None
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_198(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(None)
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_199(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get(None, False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_200(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", None))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_201(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get(False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_202(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", ))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_203(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("XXthreshold_securities_listXX", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_204(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("THRESHOLD_SECURITIES_LIST", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_205(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", True))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_206(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = None

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_207(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["XXftdXX"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_208(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["FTD"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_209(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"XXis_reg_shoXX": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_210(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"IS_REG_SHO": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_211(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "XXspike_pctXX": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_212(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "SPIKE_PCT": round(spike, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_213(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(None, 1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_214(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, None)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_215(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(1)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_216(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, )}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_217(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 2)}

    except Exception as e:
        log.warning(f"[dark_pool] advanced signals error for {ticker}: {e}")

    return results


async def x_get_massive_advanced_signals__mutmut_218(ticker: str) -> dict:
    """
    Fetch Polygon-backed signals for corporate actions and FTDs.

    Endpoints used (all real, verified against Polygon docs):
      - /v3/reference/dividends  — detect upcoming ex-dividend dates
      - /v3/reference/splits     — detect upcoming stock splits
      - /v2/reference/ftd        — Fails-to-Deliver + Reg SHO (Business plan; graceful 403)

    Returns:
      ftd          — {"is_reg_sho": bool, "spike_pct": float}
      corp_actions — {"ex_div_soon": bool, "ex_div_date": str|None, "split_soon": bool}
      dark_pool_flow — float (millions, from live WebSocket accumulator)
    """
    api_key = os.getenv("POLYGON_API_KEY") or os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    from datetime import date, timedelta

    ssl_ctx = get_ssl_context()

    results = {
        "ftd": {"is_reg_sho": False, "spike_pct": 0.0},
        "corp_actions": {"ex_div_soon": False, "ex_div_date": None, "split_soon": False},
        "dark_pool_flow": _flow_data.get(ticker, 0.0),
    }

    today = date.today()
    lookahead_14 = (today + timedelta(days=14)).isoformat()
    lookahead_7 = (today + timedelta(days=7)).isoformat()

    try:
        async with shared_session() as session:

            async def _get(url: str, params: dict) -> dict:
                try:
                    params["apiKey"] = api_key
                    async with session.get(url, params=params, timeout=6, ssl=ssl_ctx) as r:
                        if r.status == 200:
                            return await r.json()
                except Exception:
                    pass
                return {}

            # ── Dividends: ex-div within 14 days blocks MR entry ────────────
            div_data = await _get(
                "https://api.polygon.io/v3/reference/dividends",
                {"ticker": ticker, "ex_dividend_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for div in div_data.get("results") or []:
                ex_date = div.get("ex_dividend_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["ex_div_soon"] = True
                    results["corp_actions"]["ex_div_date"] = ex_date
                    break

            # ── Splits: split within 14 days ─────────────────────────────────
            split_data = await _get(
                "https://api.polygon.io/v3/reference/splits",
                {"ticker": ticker, "execution_date.lte": lookahead_14, "order": "asc", "limit": 5},
            )
            for sp in split_data.get("results") or []:
                ex_date = sp.get("execution_date", "")
                if ex_date and ex_date >= today.isoformat():
                    results["corp_actions"]["split_soon"] = True
                    break

            # ── FTDs (Business plan — graceful 403) ──────────────────────────
            ftd_data = await _get(
                "https://api.polygon.io/v2/reference/ftd",
                {"symbol": ticker, "limit": 10},
            )
            ftd_results = ftd_data.get("results") or []
            if ftd_results:
                latest = ftd_results[0]
                qty = float(latest.get("quantity", 0) or 0)
                prev_q = float(ftd_results[1].get("quantity", 0) or 0) if len(ftd_results) > 1 else 0.0
                spike = ((qty - prev_q) / prev_q * 100) if prev_q > 0 else 0.0
                is_sho = bool(latest.get("threshold_securities_list", False))
                results["ftd"] = {"is_reg_sho": is_sho, "spike_pct": round(spike, 1)}

    except Exception as e:
        log.warning(None)

    return results

mutants_x_get_massive_advanced_signals__mutmut['_mutmut_orig'] = x_get_massive_advanced_signals__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_1'] = x_get_massive_advanced_signals__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_2'] = x_get_massive_advanced_signals__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_3'] = x_get_massive_advanced_signals__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_4'] = x_get_massive_advanced_signals__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_5'] = x_get_massive_advanced_signals__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_6'] = x_get_massive_advanced_signals__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_7'] = x_get_massive_advanced_signals__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_8'] = x_get_massive_advanced_signals__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_9'] = x_get_massive_advanced_signals__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_10'] = x_get_massive_advanced_signals__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_11'] = x_get_massive_advanced_signals__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_12'] = x_get_massive_advanced_signals__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_13'] = x_get_massive_advanced_signals__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_14'] = x_get_massive_advanced_signals__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_15'] = x_get_massive_advanced_signals__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_16'] = x_get_massive_advanced_signals__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_17'] = x_get_massive_advanced_signals__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_18'] = x_get_massive_advanced_signals__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_19'] = x_get_massive_advanced_signals__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_20'] = x_get_massive_advanced_signals__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_21'] = x_get_massive_advanced_signals__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_22'] = x_get_massive_advanced_signals__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_23'] = x_get_massive_advanced_signals__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_24'] = x_get_massive_advanced_signals__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_25'] = x_get_massive_advanced_signals__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_26'] = x_get_massive_advanced_signals__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_27'] = x_get_massive_advanced_signals__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_28'] = x_get_massive_advanced_signals__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_29'] = x_get_massive_advanced_signals__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_30'] = x_get_massive_advanced_signals__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_31'] = x_get_massive_advanced_signals__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_32'] = x_get_massive_advanced_signals__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_33'] = x_get_massive_advanced_signals__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_34'] = x_get_massive_advanced_signals__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_35'] = x_get_massive_advanced_signals__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_36'] = x_get_massive_advanced_signals__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_37'] = x_get_massive_advanced_signals__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_38'] = x_get_massive_advanced_signals__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_39'] = x_get_massive_advanced_signals__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_40'] = x_get_massive_advanced_signals__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_41'] = x_get_massive_advanced_signals__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_42'] = x_get_massive_advanced_signals__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_43'] = x_get_massive_advanced_signals__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_44'] = x_get_massive_advanced_signals__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_45'] = x_get_massive_advanced_signals__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_46'] = x_get_massive_advanced_signals__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_47'] = x_get_massive_advanced_signals__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_48'] = x_get_massive_advanced_signals__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_49'] = x_get_massive_advanced_signals__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut_50'] = x_get_massive_advanced_signals__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_massive_advanced_signals__mutmut['x_get_massive_advanced_signals__mutmut