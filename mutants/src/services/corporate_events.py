"""
Corporate Events — Polygon.io reference endpoints.

Sources:
  v3/reference/dividends  — ex-dividend dates (bullish pre-event bid)
  v3/reference/splits     — stock splits (accessibility + retail attention)
  v2/reference/news       — detect M&A, earnings, guidance mentions in recent headlines

Signal value:
  Ex-dividend ≤ 2 days away     → +2 pts (support bid, dividend capture flow)
  Stock split ≤ 7 days away     → +2 pts (retail attention + accessibility)
  M&A news detected in headline → +6 pts / −4 pts (merger/takeover vs divestiture)
  Earnings guidance headline    → ±4 pts (raise/cut language)

4-hour cache per batch.
"""

import asyncio
import logging
import os
import time
from datetime import date, timedelta

import aiohttp
from services.http_client import get_ssl_context, shared_session

log = logging.getLogger("signal.trade.corporate_events")

_cache: dict = {"events": None, "by_ticker": None, "ts": 0.0}
_TTL = 3600  # 1 hour — ex-div/split dates via Polygon (unlimited calls)
_BASE = "https://api.polygon.io"

_MA_KEYWORDS = {"acqui", "merger", "takeover", "buyout", "acquisition", "acquire"}
_DIV_KEYWORDS = {"divestiture", "spinoff", "spin-off", "divest"}
_RAISE_KEYWORDS = {
    "raises guidance",
    "raised guidance",
    "raised outlook",
    "above expectations",
    "beat estimates",
    "beats estimates",
    "record revenue",
    "record earnings",
}
_CUT_KEYWORDS = {
    "cuts guidance",
    "cut guidance",
    "lowered outlook",
    "below expectations",
    "missed estimates",
    "misses estimates",
    "warning",
    "profit warning",
}


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__fetch_dividends__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_dividends__mutmut)
async def _fetch_dividends(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_orig(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_1(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = None
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_2(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = None
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_3(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today - timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_4(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=None)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_5(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=8)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_6(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = None
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_7(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = None
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_8(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = None
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_9(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "XXtickerXX": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_10(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "TICKER": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_11(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "XXex_dividend_date.gteXX": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_12(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "EX_DIVIDEND_DATE.GTE": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_13(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "XXex_dividend_date.lteXX": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_14(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "EX_DIVIDEND_DATE.LTE": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_15(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "XXlimitXX": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_16(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "LIMIT": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_17(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 4,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_18(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "XXapiKeyXX": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_19(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apikey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_20(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "APIKEY": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_21(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(None, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_22(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=None, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_23(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_24(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=None) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_25(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_26(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_27(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_28(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, ) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_29(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_30(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=7)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_31(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status == 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_32(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 201:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_33(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = None
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_34(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") and []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_35(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get(None) or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_36(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("XXresultsXX") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_37(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("RESULTS") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_38(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = None
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_39(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get(None, "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_40(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", None)
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_41(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_42(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", )
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_43(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("XXex_dividend_dateXX", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_44(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("EX_DIVIDEND_DATE", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_45(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "XXXX")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_46(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_47(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            break
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_48(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = None
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_49(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) + today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_50(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(None) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_51(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            break
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_52(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            None
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_53(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "XXtickerXX": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_54(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "TICKER": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_55(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "XXtypeXX": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_56(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "TYPE": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_57(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "XXExDividendDateXX",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_58(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "exdividenddate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_59(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "EXDIVIDENDDATE",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_60(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "XXlabelXX": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_61(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "LABEL": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_62(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get(None, 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_63(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', None):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_64(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get(0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_65(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', ):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_66(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('XXcash_amountXX', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_67(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('CASH_AMOUNT', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_68(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 1):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_69(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "XXdateXX": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_70(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "DATE": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_71(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "XXdays_awayXX": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_72(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "DAYS_AWAY": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_73(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "XXsignal_ptsXX": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_74(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "SIGNAL_PTS": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_75(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 3 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_76(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away < 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_77(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 3 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_78(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 1,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_79(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "XXwithin_windowXX": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_80(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "WITHIN_WINDOW": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_81(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away < 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_82(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 3,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_dividends__mutmut_83(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=7)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/dividends"
            params = {
                "ticker": ticker,
                "ex_dividend_date.gte": today.isoformat(),
                "ex_dividend_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("ex_dividend_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "ExDividendDate",
                                "label": f"Ex-Dividend ${r.get('cash_amount', 0):.3f}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 2 else 0,
                                "within_window": days_away <= 2,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(None) for t in tickers])
    return events

mutants_x__fetch_dividends__mutmut['_mutmut_orig'] = x__fetch_dividends__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_1'] = x__fetch_dividends__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_2'] = x__fetch_dividends__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_3'] = x__fetch_dividends__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_4'] = x__fetch_dividends__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_5'] = x__fetch_dividends__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_6'] = x__fetch_dividends__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_7'] = x__fetch_dividends__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_8'] = x__fetch_dividends__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_9'] = x__fetch_dividends__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_10'] = x__fetch_dividends__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_11'] = x__fetch_dividends__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_12'] = x__fetch_dividends__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_13'] = x__fetch_dividends__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_14'] = x__fetch_dividends__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_15'] = x__fetch_dividends__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_16'] = x__fetch_dividends__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_17'] = x__fetch_dividends__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_18'] = x__fetch_dividends__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_19'] = x__fetch_dividends__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_20'] = x__fetch_dividends__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_21'] = x__fetch_dividends__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_22'] = x__fetch_dividends__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_23'] = x__fetch_dividends__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_24'] = x__fetch_dividends__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_25'] = x__fetch_dividends__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_26'] = x__fetch_dividends__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_27'] = x__fetch_dividends__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_28'] = x__fetch_dividends__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_29'] = x__fetch_dividends__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_30'] = x__fetch_dividends__mutmut_30 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_31'] = x__fetch_dividends__mutmut_31 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_32'] = x__fetch_dividends__mutmut_32 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_33'] = x__fetch_dividends__mutmut_33 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_34'] = x__fetch_dividends__mutmut_34 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_35'] = x__fetch_dividends__mutmut_35 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_36'] = x__fetch_dividends__mutmut_36 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_37'] = x__fetch_dividends__mutmut_37 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_38'] = x__fetch_dividends__mutmut_38 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_39'] = x__fetch_dividends__mutmut_39 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_40'] = x__fetch_dividends__mutmut_40 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_41'] = x__fetch_dividends__mutmut_41 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_42'] = x__fetch_dividends__mutmut_42 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_43'] = x__fetch_dividends__mutmut_43 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_44'] = x__fetch_dividends__mutmut_44 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_45'] = x__fetch_dividends__mutmut_45 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_46'] = x__fetch_dividends__mutmut_46 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_47'] = x__fetch_dividends__mutmut_47 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_48'] = x__fetch_dividends__mutmut_48 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_49'] = x__fetch_dividends__mutmut_49 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_50'] = x__fetch_dividends__mutmut_50 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_51'] = x__fetch_dividends__mutmut_51 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_52'] = x__fetch_dividends__mutmut_52 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_53'] = x__fetch_dividends__mutmut_53 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_54'] = x__fetch_dividends__mutmut_54 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_55'] = x__fetch_dividends__mutmut_55 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_56'] = x__fetch_dividends__mutmut_56 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_57'] = x__fetch_dividends__mutmut_57 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_58'] = x__fetch_dividends__mutmut_58 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_59'] = x__fetch_dividends__mutmut_59 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_60'] = x__fetch_dividends__mutmut_60 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_61'] = x__fetch_dividends__mutmut_61 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_62'] = x__fetch_dividends__mutmut_62 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_63'] = x__fetch_dividends__mutmut_63 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_64'] = x__fetch_dividends__mutmut_64 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_65'] = x__fetch_dividends__mutmut_65 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_66'] = x__fetch_dividends__mutmut_66 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_67'] = x__fetch_dividends__mutmut_67 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_68'] = x__fetch_dividends__mutmut_68 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_69'] = x__fetch_dividends__mutmut_69 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_70'] = x__fetch_dividends__mutmut_70 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_71'] = x__fetch_dividends__mutmut_71 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_72'] = x__fetch_dividends__mutmut_72 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_73'] = x__fetch_dividends__mutmut_73 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_74'] = x__fetch_dividends__mutmut_74 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_75'] = x__fetch_dividends__mutmut_75 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_76'] = x__fetch_dividends__mutmut_76 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_77'] = x__fetch_dividends__mutmut_77 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_78'] = x__fetch_dividends__mutmut_78 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_79'] = x__fetch_dividends__mutmut_79 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_80'] = x__fetch_dividends__mutmut_80 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_81'] = x__fetch_dividends__mutmut_81 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_82'] = x__fetch_dividends__mutmut_82 # type: ignore # mutmut generated
mutants_x__fetch_dividends__mutmut['x__fetch_dividends__mutmut_83'] = x__fetch_dividends__mutmut_83 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_splits__mutmut)
async def _fetch_splits(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_orig(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_1(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = None
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_2(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = None
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_3(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today - timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_4(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=None)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_5(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=15)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_6(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = None
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_7(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = None
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_8(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = None
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_9(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "XXtickerXX": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_10(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "TICKER": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_11(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "XXexecution_date.gteXX": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_12(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "EXECUTION_DATE.GTE": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_13(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "XXexecution_date.lteXX": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_14(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "EXECUTION_DATE.LTE": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_15(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "XXlimitXX": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_16(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "LIMIT": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_17(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 3,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_18(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "XXapiKeyXX": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_19(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apikey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_20(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "APIKEY": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_21(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(None, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_22(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=None, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_23(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_24(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=None) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_25(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_26(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_27(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_28(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, ) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_29(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_30(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=7)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_31(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status == 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_32(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 201:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_33(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = None
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_34(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") and []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_35(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get(None) or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_36(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("XXresultsXX") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_37(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("RESULTS") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_38(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = None
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_39(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get(None, "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_40(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", None)
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_41(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_42(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", )
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_43(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("XXexecution_dateXX", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_44(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("EXECUTION_DATE", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_45(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "XXXX")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_46(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_47(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            break
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_48(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = None
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_49(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) + today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_50(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(None) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_51(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            break
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_52(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = None
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_53(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get(None, 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_54(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', None)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_55(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get(1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_56(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', )}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_57(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('XXsplit_fromXX', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_58(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('SPLIT_FROM', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_59(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 2)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_60(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get(None, 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_61(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', None)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_62(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get(1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_63(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', )}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_64(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('XXsplit_toXX', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_65(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('SPLIT_TO', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_66(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 2)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_67(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            None
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_68(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "XXtickerXX": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_69(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "TICKER": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_70(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "XXtypeXX": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_71(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "TYPE": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_72(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "XXStockSplitXX",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_73(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "stocksplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_74(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "STOCKSPLIT",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_75(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "XXlabelXX": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_76(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "LABEL": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_77(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "XXdateXX": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_78(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "DATE": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_79(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "XXdays_awayXX": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_80(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "DAYS_AWAY": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_81(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "XXsignal_ptsXX": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_82(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "SIGNAL_PTS": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_83(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 3 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_84(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away < 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_85(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 8 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_86(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 1,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_87(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "XXwithin_windowXX": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_88(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "WITHIN_WINDOW": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_89(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away < 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_90(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 8,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_splits__mutmut_91(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    today = date.today()
    end_date = (today + timedelta(days=14)).isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v3/reference/splits"
            params = {
                "ticker": ticker,
                "execution_date.gte": today.isoformat(),
                "execution_date.lte": end_date,
                "limit": 2,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for r in data.get("results") or []:
                        ex_date = r.get("execution_date", "")
                        if not ex_date:
                            continue
                        try:
                            days_away = (date.fromisoformat(ex_date) - today).days
                        except ValueError:
                            continue
                        ratio = f"{r.get('split_from', 1)}:{r.get('split_to', 1)}"
                        events.append(
                            {
                                "ticker": ticker,
                                "type": "StockSplit",
                                "label": f"Stock Split {ratio}",
                                "date": ex_date,
                                "days_away": days_away,
                                "signal_pts": 2 if days_away <= 7 else 0,
                                "within_window": days_away <= 7,
                            }
                        )
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(None) for t in tickers])
    return events

mutants_x__fetch_splits__mutmut['_mutmut_orig'] = x__fetch_splits__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_1'] = x__fetch_splits__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_2'] = x__fetch_splits__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_3'] = x__fetch_splits__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_4'] = x__fetch_splits__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_5'] = x__fetch_splits__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_6'] = x__fetch_splits__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_7'] = x__fetch_splits__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_8'] = x__fetch_splits__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_9'] = x__fetch_splits__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_10'] = x__fetch_splits__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_11'] = x__fetch_splits__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_12'] = x__fetch_splits__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_13'] = x__fetch_splits__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_14'] = x__fetch_splits__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_15'] = x__fetch_splits__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_16'] = x__fetch_splits__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_17'] = x__fetch_splits__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_18'] = x__fetch_splits__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_19'] = x__fetch_splits__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_20'] = x__fetch_splits__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_21'] = x__fetch_splits__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_22'] = x__fetch_splits__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_23'] = x__fetch_splits__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_24'] = x__fetch_splits__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_25'] = x__fetch_splits__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_26'] = x__fetch_splits__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_27'] = x__fetch_splits__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_28'] = x__fetch_splits__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_29'] = x__fetch_splits__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_30'] = x__fetch_splits__mutmut_30 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_31'] = x__fetch_splits__mutmut_31 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_32'] = x__fetch_splits__mutmut_32 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_33'] = x__fetch_splits__mutmut_33 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_34'] = x__fetch_splits__mutmut_34 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_35'] = x__fetch_splits__mutmut_35 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_36'] = x__fetch_splits__mutmut_36 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_37'] = x__fetch_splits__mutmut_37 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_38'] = x__fetch_splits__mutmut_38 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_39'] = x__fetch_splits__mutmut_39 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_40'] = x__fetch_splits__mutmut_40 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_41'] = x__fetch_splits__mutmut_41 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_42'] = x__fetch_splits__mutmut_42 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_43'] = x__fetch_splits__mutmut_43 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_44'] = x__fetch_splits__mutmut_44 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_45'] = x__fetch_splits__mutmut_45 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_46'] = x__fetch_splits__mutmut_46 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_47'] = x__fetch_splits__mutmut_47 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_48'] = x__fetch_splits__mutmut_48 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_49'] = x__fetch_splits__mutmut_49 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_50'] = x__fetch_splits__mutmut_50 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_51'] = x__fetch_splits__mutmut_51 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_52'] = x__fetch_splits__mutmut_52 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_53'] = x__fetch_splits__mutmut_53 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_54'] = x__fetch_splits__mutmut_54 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_55'] = x__fetch_splits__mutmut_55 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_56'] = x__fetch_splits__mutmut_56 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_57'] = x__fetch_splits__mutmut_57 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_58'] = x__fetch_splits__mutmut_58 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_59'] = x__fetch_splits__mutmut_59 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_60'] = x__fetch_splits__mutmut_60 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_61'] = x__fetch_splits__mutmut_61 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_62'] = x__fetch_splits__mutmut_62 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_63'] = x__fetch_splits__mutmut_63 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_64'] = x__fetch_splits__mutmut_64 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_65'] = x__fetch_splits__mutmut_65 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_66'] = x__fetch_splits__mutmut_66 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_67'] = x__fetch_splits__mutmut_67 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_68'] = x__fetch_splits__mutmut_68 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_69'] = x__fetch_splits__mutmut_69 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_70'] = x__fetch_splits__mutmut_70 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_71'] = x__fetch_splits__mutmut_71 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_72'] = x__fetch_splits__mutmut_72 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_73'] = x__fetch_splits__mutmut_73 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_74'] = x__fetch_splits__mutmut_74 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_75'] = x__fetch_splits__mutmut_75 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_76'] = x__fetch_splits__mutmut_76 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_77'] = x__fetch_splits__mutmut_77 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_78'] = x__fetch_splits__mutmut_78 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_79'] = x__fetch_splits__mutmut_79 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_80'] = x__fetch_splits__mutmut_80 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_81'] = x__fetch_splits__mutmut_81 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_82'] = x__fetch_splits__mutmut_82 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_83'] = x__fetch_splits__mutmut_83 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_84'] = x__fetch_splits__mutmut_84 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_85'] = x__fetch_splits__mutmut_85 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_86'] = x__fetch_splits__mutmut_86 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_87'] = x__fetch_splits__mutmut_87 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_88'] = x__fetch_splits__mutmut_88 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_89'] = x__fetch_splits__mutmut_89 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_90'] = x__fetch_splits__mutmut_90 # type: ignore # mutmut generated
mutants_x__fetch_splits__mutmut['x__fetch_splits__mutmut_91'] = x__fetch_splits__mutmut_91 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_news_events__mutmut)
async def _fetch_news_events(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_orig(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_1(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = None
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_2(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = None
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_3(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = None
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_4(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = None
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_5(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "XXtickerXX": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_6(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "TICKER": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_7(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "XXlimitXX": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_8(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "LIMIT": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_9(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 6,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_10(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "XXorderXX": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_11(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "ORDER": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_12(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "XXdescXX",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_13(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "DESC",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_14(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "XXsortXX": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_15(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "SORT": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_16(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "XXpublished_utcXX",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_17(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "PUBLISHED_UTC",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_18(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "XXpublished_utc.gteXX": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_19(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "PUBLISHED_UTC.GTE": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_20(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "XXapiKeyXX": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_21(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apikey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_22(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "APIKEY": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_23(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(None, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_24(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=None, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_25(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_26(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=None) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_27(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_28(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_29(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_30(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, ) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_31(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_32(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=7)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_33(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status == 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_34(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 201:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_35(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = None
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_36(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") and []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_37(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get(None) or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_38(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("XXresultsXX") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_39(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("RESULTS") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_40(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = None
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_41(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").upper()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_42(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") and "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_43(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get(None) or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_44(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("XXtitleXX") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_45(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("TITLE") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_46(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "XXXX").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_47(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(None):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_48(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k not in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_49(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                None
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_50(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "XXtickerXX": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_51(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "TICKER": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_52(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "XXtypeXX": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_53(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "TYPE": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_54(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "XXMaterialAgreementXX",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_55(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "materialagreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_56(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MATERIALAGREEMENT",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_57(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "XXlabelXX": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_58(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "LABEL": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_59(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "XXM&A / Acquisition newsXX",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_60(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "m&a / acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_61(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / ACQUISITION NEWS",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_62(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "XXdateXX": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_63(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "DATE": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_64(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "XXdays_awayXX": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_65(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "DAYS_AWAY": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_66(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 1,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_67(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "XXsignal_ptsXX": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_68(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "SIGNAL_PTS": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_69(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 7,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_70(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "XXwithin_windowXX": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_71(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "WITHIN_WINDOW": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_72(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": False,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_73(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            return
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_74(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(None):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_75(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k not in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_76(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                None
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_77(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "XXtickerXX": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_78(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "TICKER": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_79(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "XXtypeXX": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_80(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "TYPE": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_81(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "XXGuidanceRaiseXX",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_82(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "guidanceraise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_83(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GUIDANCERAISE",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_84(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "XXlabelXX": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_85(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "LABEL": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_86(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "XXGuidance / earnings beat signalXX",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_87(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_88(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "GUIDANCE / EARNINGS BEAT SIGNAL",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_89(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "XXdateXX": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_90(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "DATE": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_91(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "XXdays_awayXX": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_92(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "DAYS_AWAY": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_93(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 1,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_94(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "XXsignal_ptsXX": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_95(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "SIGNAL_PTS": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_96(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 5,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_97(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "XXwithin_windowXX": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_98(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "WITHIN_WINDOW": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_99(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": False,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_100(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            return
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_101(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(None):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_102(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k not in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_103(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                None
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_104(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "XXtickerXX": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_105(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "TICKER": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_106(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "XXtypeXX": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_107(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "TYPE": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_108(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "XXGuidanceCutXX",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_109(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "guidancecut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_110(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GUIDANCECUT",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_111(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "XXlabelXX": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_112(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "LABEL": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_113(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "XXGuidance cut / earnings miss signalXX",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_114(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_115(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "GUIDANCE CUT / EARNINGS MISS SIGNAL",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_116(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "XXdateXX": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_117(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "DATE": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_118(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "XXdays_awayXX": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_119(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "DAYS_AWAY": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_120(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 1,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_121(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "XXsignal_ptsXX": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_122(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "SIGNAL_PTS": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_123(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": +4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_124(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -5,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_125(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "XXwithin_windowXX": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_126(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "WITHIN_WINDOW": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_127(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": False,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_128(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            return
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(t) for t in tickers])
    return events


async def x__fetch_news_events__mutmut_129(tickers: list[str], ssl_ctx, api_key: str) -> list[dict]:
    """Detect M&A, guidance raise/cut from recent news headlines."""
    today = date.today().isoformat()
    events = []
    async with shared_session() as session:

        async def fetch_one(ticker):
            url = f"{_BASE}/v2/reference/news"
            params = {
                "ticker": ticker,
                "limit": 5,
                "order": "desc",
                "sort": "published_utc",
                "published_utc.gte": today,
                "apiKey": api_key,
            }
            try:
                async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=6)) as resp:
                    if resp.status != 200:
                        return
                    data = await resp.json()
                    for art in data.get("results") or []:
                        title = (art.get("title") or "").lower()
                        if any(k in title for k in _MA_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "MaterialAgreement",
                                    "label": "M&A / Acquisition news",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 6,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _RAISE_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceRaise",
                                    "label": "Guidance / earnings beat signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": 4,
                                    "within_window": True,
                                }
                            )
                            break
                        if any(k in title for k in _CUT_KEYWORDS):
                            events.append(
                                {
                                    "ticker": ticker,
                                    "type": "GuidanceCut",
                                    "label": "Guidance cut / earnings miss signal",
                                    "date": today,
                                    "days_away": 0,
                                    "signal_pts": -4,
                                    "within_window": True,
                                }
                            )
                            break
            except Exception:
                pass

        await asyncio.gather(*[fetch_one(None) for t in tickers])
    return events

mutants_x__fetch_news_events__mutmut['_mutmut_orig'] = x__fetch_news_events__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_1'] = x__fetch_news_events__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_2'] = x__fetch_news_events__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_3'] = x__fetch_news_events__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_4'] = x__fetch_news_events__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_5'] = x__fetch_news_events__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_6'] = x__fetch_news_events__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_7'] = x__fetch_news_events__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_8'] = x__fetch_news_events__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_9'] = x__fetch_news_events__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_10'] = x__fetch_news_events__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_11'] = x__fetch_news_events__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_12'] = x__fetch_news_events__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_13'] = x__fetch_news_events__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_14'] = x__fetch_news_events__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_15'] = x__fetch_news_events__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_16'] = x__fetch_news_events__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_17'] = x__fetch_news_events__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_18'] = x__fetch_news_events__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_19'] = x__fetch_news_events__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_20'] = x__fetch_news_events__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_21'] = x__fetch_news_events__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_22'] = x__fetch_news_events__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_23'] = x__fetch_news_events__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_24'] = x__fetch_news_events__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_25'] = x__fetch_news_events__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_26'] = x__fetch_news_events__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_27'] = x__fetch_news_events__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_28'] = x__fetch_news_events__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_29'] = x__fetch_news_events__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_30'] = x__fetch_news_events__mutmut_30 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_31'] = x__fetch_news_events__mutmut_31 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_32'] = x__fetch_news_events__mutmut_32 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_33'] = x__fetch_news_events__mutmut_33 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_34'] = x__fetch_news_events__mutmut_34 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_35'] = x__fetch_news_events__mutmut_35 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_36'] = x__fetch_news_events__mutmut_36 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_37'] = x__fetch_news_events__mutmut_37 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_38'] = x__fetch_news_events__mutmut_38 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_39'] = x__fetch_news_events__mutmut_39 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_40'] = x__fetch_news_events__mutmut_40 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_41'] = x__fetch_news_events__mutmut_41 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_42'] = x__fetch_news_events__mutmut_42 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_43'] = x__fetch_news_events__mutmut_43 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_44'] = x__fetch_news_events__mutmut_44 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_45'] = x__fetch_news_events__mutmut_45 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_46'] = x__fetch_news_events__mutmut_46 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_47'] = x__fetch_news_events__mutmut_47 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_48'] = x__fetch_news_events__mutmut_48 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_49'] = x__fetch_news_events__mutmut_49 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_50'] = x__fetch_news_events__mutmut_50 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_51'] = x__fetch_news_events__mutmut_51 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_52'] = x__fetch_news_events__mutmut_52 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_53'] = x__fetch_news_events__mutmut_53 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_54'] = x__fetch_news_events__mutmut_54 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_55'] = x__fetch_news_events__mutmut_55 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_56'] = x__fetch_news_events__mutmut_56 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_57'] = x__fetch_news_events__mutmut_57 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_58'] = x__fetch_news_events__mutmut_58 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_59'] = x__fetch_news_events__mutmut_59 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_60'] = x__fetch_news_events__mutmut_60 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_61'] = x__fetch_news_events__mutmut_61 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_62'] = x__fetch_news_events__mutmut_62 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_63'] = x__fetch_news_events__mutmut_63 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_64'] = x__fetch_news_events__mutmut_64 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_65'] = x__fetch_news_events__mutmut_65 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_66'] = x__fetch_news_events__mutmut_66 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_67'] = x__fetch_news_events__mutmut_67 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_68'] = x__fetch_news_events__mutmut_68 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_69'] = x__fetch_news_events__mutmut_69 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_70'] = x__fetch_news_events__mutmut_70 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_71'] = x__fetch_news_events__mutmut_71 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_72'] = x__fetch_news_events__mutmut_72 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_73'] = x__fetch_news_events__mutmut_73 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_74'] = x__fetch_news_events__mutmut_74 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_75'] = x__fetch_news_events__mutmut_75 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_76'] = x__fetch_news_events__mutmut_76 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_77'] = x__fetch_news_events__mutmut_77 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_78'] = x__fetch_news_events__mutmut_78 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_79'] = x__fetch_news_events__mutmut_79 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_80'] = x__fetch_news_events__mutmut_80 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_81'] = x__fetch_news_events__mutmut_81 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_82'] = x__fetch_news_events__mutmut_82 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_83'] = x__fetch_news_events__mutmut_83 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_84'] = x__fetch_news_events__mutmut_84 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_85'] = x__fetch_news_events__mutmut_85 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_86'] = x__fetch_news_events__mutmut_86 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_87'] = x__fetch_news_events__mutmut_87 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_88'] = x__fetch_news_events__mutmut_88 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_89'] = x__fetch_news_events__mutmut_89 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_90'] = x__fetch_news_events__mutmut_90 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_91'] = x__fetch_news_events__mutmut_91 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_92'] = x__fetch_news_events__mutmut_92 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_93'] = x__fetch_news_events__mutmut_93 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_94'] = x__fetch_news_events__mutmut_94 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_95'] = x__fetch_news_events__mutmut_95 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_96'] = x__fetch_news_events__mutmut_96 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_97'] = x__fetch_news_events__mutmut_97 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_98'] = x__fetch_news_events__mutmut_98 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_99'] = x__fetch_news_events__mutmut_99 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_100'] = x__fetch_news_events__mutmut_100 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_101'] = x__fetch_news_events__mutmut_101 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_102'] = x__fetch_news_events__mutmut_102 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_103'] = x__fetch_news_events__mutmut_103 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_104'] = x__fetch_news_events__mutmut_104 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_105'] = x__fetch_news_events__mutmut_105 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_106'] = x__fetch_news_events__mutmut_106 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_107'] = x__fetch_news_events__mutmut_107 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_108'] = x__fetch_news_events__mutmut_108 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_109'] = x__fetch_news_events__mutmut_109 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_110'] = x__fetch_news_events__mutmut_110 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_111'] = x__fetch_news_events__mutmut_111 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_112'] = x__fetch_news_events__mutmut_112 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_113'] = x__fetch_news_events__mutmut_113 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_114'] = x__fetch_news_events__mutmut_114 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_115'] = x__fetch_news_events__mutmut_115 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_116'] = x__fetch_news_events__mutmut_116 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_117'] = x__fetch_news_events__mutmut_117 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_118'] = x__fetch_news_events__mutmut_118 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_119'] = x__fetch_news_events__mutmut_119 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_120'] = x__fetch_news_events__mutmut_120 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_121'] = x__fetch_news_events__mutmut_121 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_122'] = x__fetch_news_events__mutmut_122 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_123'] = x__fetch_news_events__mutmut_123 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_124'] = x__fetch_news_events__mutmut_124 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_125'] = x__fetch_news_events__mutmut_125 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_126'] = x__fetch_news_events__mutmut_126 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_127'] = x__fetch_news_events__mutmut_127 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_128'] = x__fetch_news_events__mutmut_128 # type: ignore # mutmut generated
mutants_x__fetch_news_events__mutmut['x__fetch_news_events__mutmut_129'] = x__fetch_news_events__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_corporate_events__mutmut)
async def get_corporate_events(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_orig(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_1(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = None
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_2(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None or now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_3(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["XXeventsXX"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_4(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["EVENTS"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_5(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_6(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now + _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_7(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["XXtsXX"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_8(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["TS"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_9(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] <= _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_10(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"XXeventsXX": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_11(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"EVENTS": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_12(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["XXeventsXX"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_13(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["EVENTS"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_14(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "XXby_tickerXX": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_15(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "BY_TICKER": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_16(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["XXby_tickerXX"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_17(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["BY_TICKER"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_18(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = None
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_19(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv(None)
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_20(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("XXMASSIVE_API_KEYXX")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_21(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("massive_api_key")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_22(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_23(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"XXeventsXX": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_24(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"EVENTS": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_25(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "XXby_tickerXX": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_26(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "BY_TICKER": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_27(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_28(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = None  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_29(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:51]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_30(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = None

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_31(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = None

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_32(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        None,
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_33(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        None,
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_34(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        None,
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_35(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=None,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_36(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_37(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_38(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_39(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_40(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(None, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_41(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, None, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_42(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, None),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_43(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_44(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_45(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, ),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_46(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(None, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_47(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, None, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_48(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, None),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_49(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_50(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_51(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, ),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_52(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(None, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_53(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, None, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_54(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, None),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_55(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_56(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_57(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, ),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_58(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=False,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_59(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = None
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_60(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(None)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_61(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=None)

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_62(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: None)

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_63(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["XXdateXX"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_64(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["DATE"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_65(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = None
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_66(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(None)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_67(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(None, []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_68(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], None).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_69(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault([]).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_70(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], ).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_71(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["XXtickerXX"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_72(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["TICKER"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_73(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = None
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_74(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["XXeventsXX"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_75(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["EVENTS"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_76(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = None
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_77(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["XXby_tickerXX"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_78(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["BY_TICKER"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_79(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = None
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_80(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["XXtsXX"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_81(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["TS"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_82(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(None)
    return {"events": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_83(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"XXeventsXX": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_84(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"EVENTS": all_events, "by_ticker": by_ticker}


async def x_get_corporate_events__mutmut_85(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "XXby_tickerXX": by_ticker}


async def x_get_corporate_events__mutmut_86(tickers: list[str] | None = None) -> dict:
    """
    Public entrypoint. Fetches dividends + splits + news events for all tickers.
    Returns {"events": list, "by_ticker": dict}. 4-hour cache.
    """
    global _cache
    now = time.time()
    if _cache["events"] is not None and now - _cache["ts"] < _TTL:
        return {"events": _cache["events"], "by_ticker": _cache["by_ticker"]}

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {"events": [], "by_ticker": {}}

    if not tickers:
        # Use a representative subset — fetching 154 tickers in parallel is fine
        from config import get_settings

        tickers = get_settings().tickers[:50]  # top 50 to keep startup fast

    ssl_ctx = get_ssl_context()

    divs, splits, news_ev = await asyncio.gather(
        _fetch_dividends(tickers, ssl_ctx, api_key),
        _fetch_splits(tickers, ssl_ctx, api_key),
        _fetch_news_events(tickers, ssl_ctx, api_key),
        return_exceptions=True,
    )

    all_events: list[dict] = []
    for batch in (divs, splits, news_ev):
        if isinstance(batch, list):
            all_events.extend(batch)

    all_events.sort(key=lambda e: e["date"])

    by_ticker: dict[str, list[dict]] = {}
    for ev in all_events:
        by_ticker.setdefault(ev["ticker"], []).append(ev)

    _cache["events"] = all_events
    _cache["by_ticker"] = by_ticker
    _cache["ts"] = now
    log.info(f"[corp_events] {len(all_events)} events for {len(by_ticker)} tickers")
    return {"events": all_events, "BY_TICKER": by_ticker}

mutants_x_get_corporate_events__mutmut['_mutmut_orig'] = x_get_corporate_events__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_1'] = x_get_corporate_events__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_2'] = x_get_corporate_events__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_3'] = x_get_corporate_events__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_4'] = x_get_corporate_events__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_5'] = x_get_corporate_events__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_6'] = x_get_corporate_events__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_7'] = x_get_corporate_events__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_8'] = x_get_corporate_events__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_9'] = x_get_corporate_events__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_10'] = x_get_corporate_events__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_11'] = x_get_corporate_events__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_12'] = x_get_corporate_events__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_13'] = x_get_corporate_events__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_14'] = x_get_corporate_events__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_15'] = x_get_corporate_events__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_16'] = x_get_corporate_events__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_17'] = x_get_corporate_events__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_18'] = x_get_corporate_events__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_19'] = x_get_corporate_events__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_20'] = x_get_corporate_events__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_21'] = x_get_corporate_events__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_22'] = x_get_corporate_events__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_23'] = x_get_corporate_events__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_24'] = x_get_corporate_events__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_25'] = x_get_corporate_events__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_26'] = x_get_corporate_events__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_27'] = x_get_corporate_events__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_28'] = x_get_corporate_events__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_29'] = x_get_corporate_events__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_30'] = x_get_corporate_events__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_31'] = x_get_corporate_events__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_32'] = x_get_corporate_events__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_33'] = x_get_corporate_events__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_34'] = x_get_corporate_events__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_35'] = x_get_corporate_events__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_36'] = x_get_corporate_events__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_37'] = x_get_corporate_events__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_38'] = x_get_corporate_events__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_39'] = x_get_corporate_events__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_40'] = x_get_corporate_events__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_41'] = x_get_corporate_events__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_42'] = x_get_corporate_events__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_43'] = x_get_corporate_events__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_44'] = x_get_corporate_events__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_45'] = x_get_corporate_events__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_46'] = x_get_corporate_events__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_47'] = x_get_corporate_events__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_48'] = x_get_corporate_events__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_49'] = x_get_corporate_events__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_50'] = x_get_corporate_events__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_51'] = x_get_corporate_events__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_52'] = x_get_corporate_events__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_53'] = x_get_corporate_events__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_54'] = x_get_corporate_events__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_55'] = x_get_corporate_events__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_56'] = x_get_corporate_events__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_57'] = x_get_corporate_events__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_58'] = x_get_corporate_events__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_59'] = x_get_corporate_events__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_60'] = x_get_corporate_events__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_61'] = x_get_corporate_events__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_62'] = x_get_corporate_events__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_63'] = x_get_corporate_events__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_64'] = x_get_corporate_events__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_65'] = x_get_corporate_events__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_66'] = x_get_corporate_events__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_67'] = x_get_corporate_events__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_68'] = x_get_corporate_events__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_69'] = x_get_corporate_events__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_70'] = x_get_corporate_events__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_71'] = x_get_corporate_events__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_72'] = x_get_corporate_events__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_73'] = x_get_corporate_events__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_74'] = x_get_corporate_events__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_75'] = x_get_corporate_events__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_76'] = x_get_corporate_events__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_77'] = x_get_corporate_events__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_78'] = x_get_corporate_events__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_79'] = x_get_corporate_events__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_80'] = x_get_corporate_events__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_81'] = x_get_corporate_events__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_82'] = x_get_corporate_events__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_83'] = x_get_corporate_events__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_84'] = x_get_corporate_events__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_85'] = x_get_corporate_events__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_corporate_events__mutmut['x_get_corporate_events__mutmut_86'] = x_get_corporate_events__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_exdiv_blackout__mutmut)
def get_exdiv_blackout(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_orig(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_1(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_2(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return True, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_3(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, "XXXX"
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_4(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(None, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_5(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, None):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_6(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get([]):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_7(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, ):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_8(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") and {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_9(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get(None) or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_10(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("XXby_tickerXX") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_11(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("BY_TICKER") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_12(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" or ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_13(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get(None) == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_14(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("XXtypeXX") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_15(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("TYPE") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_16(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") != "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_17(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "XXExDividendDateXX" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_18(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "exdividenddate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_19(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "EXDIVIDENDDATE" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_20(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get(None) == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_21(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("XXdays_awayXX") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_22(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("DAYS_AWAY") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_23(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") != 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_24(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 1:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_25(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return False, ev.get("label", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_26(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get(None, "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_27(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", None)
    return False, ""


def x_get_exdiv_blackout__mutmut_28(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_29(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", )
    return False, ""


def x_get_exdiv_blackout__mutmut_30(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("XXlabelXX", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_31(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("LABEL", "Ex-Dividend Date")
    return False, ""


def x_get_exdiv_blackout__mutmut_32(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "XXEx-Dividend DateXX")
    return False, ""


def x_get_exdiv_blackout__mutmut_33(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "ex-dividend date")
    return False, ""


def x_get_exdiv_blackout__mutmut_34(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "EX-DIVIDEND DATE")
    return False, ""


def x_get_exdiv_blackout__mutmut_35(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return True, ""


def x_get_exdiv_blackout__mutmut_36(ticker: str, events_ctx: dict | None) -> tuple[bool, str]:
    """
    Returns (True, label) when today IS the ex-dividend date for the ticker.
    On ex-date the stock price mechanically drops by the dividend amount —
    technical breakout/momentum signals are invalidated by this guaranteed drop.
    A hard HOLD avoids Supertrend / moving-average false-positives on ex-date.
    """
    if not events_ctx:
        return False, ""
    for ev in (events_ctx.get("by_ticker") or {}).get(ticker, []):
        if ev.get("type") == "ExDividendDate" and ev.get("days_away") == 0:
            return True, ev.get("label", "Ex-Dividend Date")
    return False, "XXXX"

mutants_x_get_exdiv_blackout__mutmut['_mutmut_orig'] = x_get_exdiv_blackout__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_1'] = x_get_exdiv_blackout__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_2'] = x_get_exdiv_blackout__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_3'] = x_get_exdiv_blackout__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_4'] = x_get_exdiv_blackout__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_5'] = x_get_exdiv_blackout__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_6'] = x_get_exdiv_blackout__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_7'] = x_get_exdiv_blackout__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_8'] = x_get_exdiv_blackout__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_9'] = x_get_exdiv_blackout__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_10'] = x_get_exdiv_blackout__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_11'] = x_get_exdiv_blackout__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_12'] = x_get_exdiv_blackout__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_13'] = x_get_exdiv_blackout__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_14'] = x_get_exdiv_blackout__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_15'] = x_get_exdiv_blackout__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_16'] = x_get_exdiv_blackout__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_17'] = x_get_exdiv_blackout__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_18'] = x_get_exdiv_blackout__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_19'] = x_get_exdiv_blackout__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_20'] = x_get_exdiv_blackout__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_21'] = x_get_exdiv_blackout__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_22'] = x_get_exdiv_blackout__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_23'] = x_get_exdiv_blackout__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_24'] = x_get_exdiv_blackout__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_25'] = x_get_exdiv_blackout__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_26'] = x_get_exdiv_blackout__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_27'] = x_get_exdiv_blackout__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_28'] = x_get_exdiv_blackout__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_29'] = x_get_exdiv_blackout__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_30'] = x_get_exdiv_blackout__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_31'] = x_get_exdiv_blackout__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_32'] = x_get_exdiv_blackout__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_33'] = x_get_exdiv_blackout__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_34'] = x_get_exdiv_blackout__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_35'] = x_get_exdiv_blackout__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_exdiv_blackout__mutmut['x_get_exdiv_blackout__mutmut_36'] = x_get_exdiv_blackout__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_event_score__mutmut)
def get_event_score(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_orig(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_1(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_2(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 1.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_3(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = None
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_4(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get(None, {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_5(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", None)
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_6(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get({})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_7(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", )
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_8(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("XXby_tickerXX", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_9(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("BY_TICKER", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_10(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = None
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_11(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 1.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_12(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(None, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_13(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, None):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_14(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get([]):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_15(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, ):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_16(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = None
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_17(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get(None, 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_18(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", None)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_19(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get(0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_20(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", )
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_21(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("XXsignal_ptsXX", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_22(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("SIGNAL_PTS", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_23(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 1)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_24(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts or ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_25(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get(None):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_26(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("XXwithin_windowXX"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_27(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("WITHIN_WINDOW"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_28(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total = pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_29(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total -= pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_30(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(None)
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_31(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['XXlabelXX']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_32(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['LABEL']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_33(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['XXdateXX']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_34(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['DATE']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_35(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['XXdays_awayXX']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_36(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['DAYS_AWAY']}d)")
    return (min(max(total, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_37(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(None, 8.0), reasons)


def x_get_event_score__mutmut_38(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), None), reasons)


def x_get_event_score__mutmut_39(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(8.0), reasons)


def x_get_event_score__mutmut_40(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), ), reasons)


def x_get_event_score__mutmut_41(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(None, -8.0), 8.0), reasons)


def x_get_event_score__mutmut_42(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, None), 8.0), reasons)


def x_get_event_score__mutmut_43(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(-8.0), 8.0), reasons)


def x_get_event_score__mutmut_44(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, ), 8.0), reasons)


def x_get_event_score__mutmut_45(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, +8.0), 8.0), reasons)


def x_get_event_score__mutmut_46(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -9.0), 8.0), reasons)


def x_get_event_score__mutmut_47(ticker: str, events_ctx: dict | None) -> tuple[float, list[str]]:
    """Return (score_pts, reasons) for signal_engine. Cap ±8."""
    if not events_ctx:
        return 0.0, []
    by_ticker = events_ctx.get("by_ticker", {})
    total, reasons = 0.0, []
    for ev in by_ticker.get(ticker, []):
        pts = ev.get("signal_pts", 0)
        if pts and ev.get("within_window"):
            total += pts
            reasons.append(f"{ev['label']} ({ev['date']}, +{ev['days_away']}d)")
    return (min(max(total, -8.0), 9.0), reasons)

mutants_x_get_event_score__mutmut['_mutmut_orig'] = x_get_event_score__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_1'] = x_get_event_score__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_2'] = x_get_event_score__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_3'] = x_get_event_score__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_4'] = x_get_event_score__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_5'] = x_get_event_score__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_6'] = x_get_event_score__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_7'] = x_get_event_score__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_8'] = x_get_event_score__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_9'] = x_get_event_score__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_10'] = x_get_event_score__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_11'] = x_get_event_score__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_12'] = x_get_event_score__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_13'] = x_get_event_score__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_14'] = x_get_event_score__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_15'] = x_get_event_score__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_16'] = x_get_event_score__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_17'] = x_get_event_score__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_18'] = x_get_event_score__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_19'] = x_get_event_score__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_20'] = x_get_event_score__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_21'] = x_get_event_score__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_22'] = x_get_event_score__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_23'] = x_get_event_score__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_24'] = x_get_event_score__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_25'] = x_get_event_score__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_26'] = x_get_event_score__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_27'] = x_get_event_score__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_28'] = x_get_event_score__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_29'] = x_get_event_score__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_30'] = x_get_event_score__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_31'] = x_get_event_score__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_32'] = x_get_event_score__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_33'] = x_get_event_score__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_34'] = x_get_event_score__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_35'] = x_get_event_score__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_36'] = x_get_event_score__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_37'] = x_get_event_score__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_38'] = x_get_event_score__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_39'] = x_get_event_score__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_40'] = x_get_event_score__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_41'] = x_get_event_score__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_42'] = x_get_event_score__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_43'] = x_get_event_score__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_44'] = x_get_event_score__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_45'] = x_get_event_score__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_46'] = x_get_event_score__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_event_score__mutmut['x_get_event_score__mutmut_47'] = x_get_event_score__mutmut_47 # type: ignore # mutmut generated
