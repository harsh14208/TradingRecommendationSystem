"""
Earnings calendar and EPS surprise history via yfinance (free, no API key).
"""

import asyncio
import logging
import time as _time
from concurrent.futures import ThreadPoolExecutor

log = logging.getLogger("signal.trade.earnings")

import yfinance as yf

from services.market_data import _retry, _session

_executor = ThreadPoolExecutor(max_workers=2)
_cal_cache: dict[str, tuple[dict, float]] = {}
_surp_cache: dict[str, tuple[dict, float]] = {}
CACHE_TTL = 3600 * 4  # 4 hours — earnings dates don't change intraday


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x__fetch_earnings_calendar__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_earnings_calendar__mutmut)
def _fetch_earnings_calendar(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_orig(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_1(ticker: str) -> dict:
    cached = None
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_2(ticker: str) -> dict:
    cached = _cal_cache.get(None)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_3(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached or _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_4(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() + cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_5(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[2] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_6(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] <= CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_7(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[1]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_8(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = None
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_9(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(None, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_10(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=None)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_11(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_12(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, )
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_13(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = None
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_14(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(None)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_15(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: None)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_16(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is not None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_17(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = None
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_18(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = None
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_19(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") and cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_20(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get(None) or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_21(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("XXEarnings DateXX") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_22(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("earnings date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_23(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("EARNINGS DATE") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_24(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get(None)
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_25(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("XXearningsDateXX")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_26(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsdate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_27(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("EARNINGSDATE")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_28(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None or isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_29(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is not None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_30(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "XXearningXX" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_31(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "EARNING" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_32(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" not in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_33(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.upper():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_34(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = None
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_35(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    return

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_36(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_37(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) or len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_38(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) >= 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_39(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 1:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_40(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = None
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_41(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[1]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_42(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(None, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_43(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, None):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_44(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr("date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_45(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, ):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_46(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "XXdateXX"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_47(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "DATE"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_48(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = None
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_49(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = None
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_50(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = None
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_51(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date + today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_52(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = None
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_53(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["XXnext_earnings_dateXX"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_54(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["NEXT_EARNINGS_DATE"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_55(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(None)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_56(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = None
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_57(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["XXdays_to_earningsXX"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_58(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["DAYS_TO_EARNINGS"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_59(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = None
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_60(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(None, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_61(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=None)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_62(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_63(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, )
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_64(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = None
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_65(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(None)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_66(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: None)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_67(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None or not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_68(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_69(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_70(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") or eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_71(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(None, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_72(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, None) and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_73(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr("empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_74(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, ) and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_75(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "XXemptyXX") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_76(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "EMPTY") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_77(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = None
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_78(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "XXdateXX" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_79(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "DATE" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_80(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" not in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_81(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.upper():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_82(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = None
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_83(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(None)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_84(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        return
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_85(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates or hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_86(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_87(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(None, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_88(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, None):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_89(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr("index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_90(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, ):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_91(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "XXindexXX"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_92(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "INDEX"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_93(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = None
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_94(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(None)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_95(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = None
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_96(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") or d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_97(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(None, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_98(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, None) and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_99(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr("date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_100(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, ) and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_101(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "XXdateXX") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_102(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "DATE") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_103(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() <= datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_104(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = None
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_105(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(None)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_106(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = None
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_107(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() + last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_108(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = None
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_109(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["XXlast_earnings_dateXX"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_110(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["LAST_EARNINGS_DATE"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_111(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(None)
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_112(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = None
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_113(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["XXdays_since_earningsXX"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_114(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["DAYS_SINCE_EARNINGS"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_115(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = None
        return result
    except Exception as e:
        log.warning(f"[earnings] calendar {ticker}: {e}")
        return {}


def x__fetch_earnings_calendar__mutmut_116(ticker: str) -> dict:
    cached = _cal_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        from datetime import datetime

        t = yf.Ticker(ticker, session=_session)
        cal = _retry(lambda: t.calendar)
        if cal is None:
            return {}

        result: dict = {}
        # yfinance returns a dict; key may be "Earnings Date" or list
        raw_date = cal.get("Earnings Date") or cal.get("earningsDate")
        if raw_date is None and isinstance(cal, dict):
            # Some versions return a flat dict with a single key
            for k, v in cal.items():
                if "earning" in k.lower():
                    raw_date = v
                    break

        if raw_date is not None:
            if isinstance(raw_date, (list, tuple)) and len(raw_date) > 0:
                raw_date = raw_date[0]
            if hasattr(raw_date, "date"):
                raw_date = raw_date.date()
            today = datetime.now().date()
            try:
                days_to = (raw_date - today).days
                result["next_earnings_date"] = str(raw_date)
                result["days_to_earnings"] = days_to
            except Exception:
                pass

        # ── Days SINCE last earnings (post-earnings cooldown) ────────────
        try:
            t2 = yf.Ticker(ticker, session=_session)
            eh = _retry(lambda: t2.earnings_history)
            if eh is not None and not (hasattr(eh, "empty") and eh.empty):
                import pandas as _pd

                dates = []
                for col in eh.columns:
                    if "date" in col.lower():
                        dates = [d for d in eh[col] if _pd.notna(d)]
                        break
                if not dates and hasattr(eh, "index"):
                    dates = list(eh.index)
                if dates:
                    past = [d for d in dates if hasattr(d, "date") and d.date() < datetime.now().date()]
                    if past:
                        last_dt = max(past)
                        days_since = (datetime.now().date() - last_dt.date()).days
                        result["last_earnings_date"] = str(last_dt.date())
                        result["days_since_earnings"] = days_since
        except Exception:
            pass

        _cal_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(None)
        return {}

mutants_x__fetch_earnings_calendar__mutmut['_mutmut_orig'] = x__fetch_earnings_calendar__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_1'] = x__fetch_earnings_calendar__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_2'] = x__fetch_earnings_calendar__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_3'] = x__fetch_earnings_calendar__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_4'] = x__fetch_earnings_calendar__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_5'] = x__fetch_earnings_calendar__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_6'] = x__fetch_earnings_calendar__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_7'] = x__fetch_earnings_calendar__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_8'] = x__fetch_earnings_calendar__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_9'] = x__fetch_earnings_calendar__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_10'] = x__fetch_earnings_calendar__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_11'] = x__fetch_earnings_calendar__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_12'] = x__fetch_earnings_calendar__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_13'] = x__fetch_earnings_calendar__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_14'] = x__fetch_earnings_calendar__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_15'] = x__fetch_earnings_calendar__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_16'] = x__fetch_earnings_calendar__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_17'] = x__fetch_earnings_calendar__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_18'] = x__fetch_earnings_calendar__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_19'] = x__fetch_earnings_calendar__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_20'] = x__fetch_earnings_calendar__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_21'] = x__fetch_earnings_calendar__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_22'] = x__fetch_earnings_calendar__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_23'] = x__fetch_earnings_calendar__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_24'] = x__fetch_earnings_calendar__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_25'] = x__fetch_earnings_calendar__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_26'] = x__fetch_earnings_calendar__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_27'] = x__fetch_earnings_calendar__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_28'] = x__fetch_earnings_calendar__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_29'] = x__fetch_earnings_calendar__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_30'] = x__fetch_earnings_calendar__mutmut_30 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_31'] = x__fetch_earnings_calendar__mutmut_31 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_32'] = x__fetch_earnings_calendar__mutmut_32 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_33'] = x__fetch_earnings_calendar__mutmut_33 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_34'] = x__fetch_earnings_calendar__mutmut_34 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_35'] = x__fetch_earnings_calendar__mutmut_35 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_36'] = x__fetch_earnings_calendar__mutmut_36 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_37'] = x__fetch_earnings_calendar__mutmut_37 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_38'] = x__fetch_earnings_calendar__mutmut_38 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_39'] = x__fetch_earnings_calendar__mutmut_39 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_40'] = x__fetch_earnings_calendar__mutmut_40 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_41'] = x__fetch_earnings_calendar__mutmut_41 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_42'] = x__fetch_earnings_calendar__mutmut_42 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_43'] = x__fetch_earnings_calendar__mutmut_43 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_44'] = x__fetch_earnings_calendar__mutmut_44 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_45'] = x__fetch_earnings_calendar__mutmut_45 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_46'] = x__fetch_earnings_calendar__mutmut_46 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_47'] = x__fetch_earnings_calendar__mutmut_47 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_48'] = x__fetch_earnings_calendar__mutmut_48 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_49'] = x__fetch_earnings_calendar__mutmut_49 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_50'] = x__fetch_earnings_calendar__mutmut_50 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_51'] = x__fetch_earnings_calendar__mutmut_51 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_52'] = x__fetch_earnings_calendar__mutmut_52 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_53'] = x__fetch_earnings_calendar__mutmut_53 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_54'] = x__fetch_earnings_calendar__mutmut_54 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_55'] = x__fetch_earnings_calendar__mutmut_55 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_56'] = x__fetch_earnings_calendar__mutmut_56 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_57'] = x__fetch_earnings_calendar__mutmut_57 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_58'] = x__fetch_earnings_calendar__mutmut_58 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_59'] = x__fetch_earnings_calendar__mutmut_59 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_60'] = x__fetch_earnings_calendar__mutmut_60 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_61'] = x__fetch_earnings_calendar__mutmut_61 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_62'] = x__fetch_earnings_calendar__mutmut_62 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_63'] = x__fetch_earnings_calendar__mutmut_63 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_64'] = x__fetch_earnings_calendar__mutmut_64 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_65'] = x__fetch_earnings_calendar__mutmut_65 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_66'] = x__fetch_earnings_calendar__mutmut_66 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_67'] = x__fetch_earnings_calendar__mutmut_67 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_68'] = x__fetch_earnings_calendar__mutmut_68 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_69'] = x__fetch_earnings_calendar__mutmut_69 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_70'] = x__fetch_earnings_calendar__mutmut_70 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_71'] = x__fetch_earnings_calendar__mutmut_71 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_72'] = x__fetch_earnings_calendar__mutmut_72 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_73'] = x__fetch_earnings_calendar__mutmut_73 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_74'] = x__fetch_earnings_calendar__mutmut_74 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_75'] = x__fetch_earnings_calendar__mutmut_75 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_76'] = x__fetch_earnings_calendar__mutmut_76 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_77'] = x__fetch_earnings_calendar__mutmut_77 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_78'] = x__fetch_earnings_calendar__mutmut_78 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_79'] = x__fetch_earnings_calendar__mutmut_79 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_80'] = x__fetch_earnings_calendar__mutmut_80 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_81'] = x__fetch_earnings_calendar__mutmut_81 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_82'] = x__fetch_earnings_calendar__mutmut_82 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_83'] = x__fetch_earnings_calendar__mutmut_83 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_84'] = x__fetch_earnings_calendar__mutmut_84 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_85'] = x__fetch_earnings_calendar__mutmut_85 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_86'] = x__fetch_earnings_calendar__mutmut_86 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_87'] = x__fetch_earnings_calendar__mutmut_87 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_88'] = x__fetch_earnings_calendar__mutmut_88 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_89'] = x__fetch_earnings_calendar__mutmut_89 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_90'] = x__fetch_earnings_calendar__mutmut_90 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_91'] = x__fetch_earnings_calendar__mutmut_91 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_92'] = x__fetch_earnings_calendar__mutmut_92 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_93'] = x__fetch_earnings_calendar__mutmut_93 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_94'] = x__fetch_earnings_calendar__mutmut_94 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_95'] = x__fetch_earnings_calendar__mutmut_95 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_96'] = x__fetch_earnings_calendar__mutmut_96 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_97'] = x__fetch_earnings_calendar__mutmut_97 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_98'] = x__fetch_earnings_calendar__mutmut_98 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_99'] = x__fetch_earnings_calendar__mutmut_99 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_100'] = x__fetch_earnings_calendar__mutmut_100 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_101'] = x__fetch_earnings_calendar__mutmut_101 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_102'] = x__fetch_earnings_calendar__mutmut_102 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_103'] = x__fetch_earnings_calendar__mutmut_103 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_104'] = x__fetch_earnings_calendar__mutmut_104 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_105'] = x__fetch_earnings_calendar__mutmut_105 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_106'] = x__fetch_earnings_calendar__mutmut_106 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_107'] = x__fetch_earnings_calendar__mutmut_107 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_108'] = x__fetch_earnings_calendar__mutmut_108 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_109'] = x__fetch_earnings_calendar__mutmut_109 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_110'] = x__fetch_earnings_calendar__mutmut_110 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_111'] = x__fetch_earnings_calendar__mutmut_111 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_112'] = x__fetch_earnings_calendar__mutmut_112 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_113'] = x__fetch_earnings_calendar__mutmut_113 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_114'] = x__fetch_earnings_calendar__mutmut_114 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_115'] = x__fetch_earnings_calendar__mutmut_115 # type: ignore # mutmut generated
mutants_x__fetch_earnings_calendar__mutmut['x__fetch_earnings_calendar__mutmut_116'] = x__fetch_earnings_calendar__mutmut_116 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__fetch_earnings_surprise__mutmut)
def _fetch_earnings_surprise(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_orig(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_1(ticker: str) -> dict:
    cached = None
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_2(ticker: str) -> dict:
    cached = _surp_cache.get(None)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_3(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached or _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_4(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() + cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_5(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[2] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_6(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] <= CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_7(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[1]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_8(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = None
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_9(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(None, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_10(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=None)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_11(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_12(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, )
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_13(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = None
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_14(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(None)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_15(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: None)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_16(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None and (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_17(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is not None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_18(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") or hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_19(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(None, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_20(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, None) and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_21(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr("empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_22(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, ) and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_23(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "XXemptyXX") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_24(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "EMPTY") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_25(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = None
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_26(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = None
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_27(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(None, "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_28(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", None)
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_29(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace("_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_30(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", )
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_31(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.upper().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_32(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace("XX XX", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_33(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "XX_XX")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_34(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "XXactualXX" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_35(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "ACTUAL" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_36(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" not in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_37(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = None
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_38(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["XXactualXX"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_39(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["ACTUAL"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_40(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "XXestimateXX" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_41(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "ESTIMATE" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_42(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" not in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_43(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = None

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_44(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["XXestimateXX"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_45(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["ESTIMATE"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_46(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map and "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_47(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "XXactualXX" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_48(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "ACTUAL" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_49(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_50(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "XXestimateXX" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_51(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "ESTIMATE" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_52(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_53(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = None
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_54(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(None)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_55(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(5)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_56(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = None
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_57(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 1
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_58(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = None
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_59(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = None
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_60(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(None)
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_61(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["XXactualXX"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_62(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["ACTUAL"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_63(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = None
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_64(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(None)
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_65(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["XXestimateXX"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_66(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["ESTIMATE"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_67(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None and est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_68(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is not None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_69(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is not None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_70(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_71(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = None
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_72(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(None), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_73(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(None)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_74(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a >= e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_75(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats = 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_76(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats -= 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_77(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 2
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_78(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a <= e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_79(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses = 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_80(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses -= 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_81(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 2
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_82(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(None) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_83(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) >= 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_84(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 1.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_85(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append(None)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_86(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) / 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_87(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) * abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_88(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a + e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_89(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(None) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_90(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 101)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_91(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = None
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_92(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 1
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_93(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::+1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_94(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-2].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_95(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = None
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_96(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(None)
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_97(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["XXactualXX"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_98(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["ACTUAL"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_99(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = None
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_100(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(None)
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_101(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["XXestimateXX"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_102(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["ESTIMATE"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_103(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None and est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_104(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is not None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_105(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is not None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_106(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                return
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_107(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(None) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_108(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) >= float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_109(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(None):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_110(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec = 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_111(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec -= 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_112(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 2
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_113(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    return
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_114(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                return

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_115(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_116(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(None, 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_117(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), None) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_118(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_119(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), ) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_120(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) * len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_121(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(None) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_122(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 2) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_123(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_124(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(None, 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_125(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], None) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_126(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_127(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], ) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_128(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[+1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_129(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-2], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_130(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 2) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_131(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = None

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_132(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "XXbeats_last_4qXX": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_133(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "BEATS_LAST_4Q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_134(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "XXmisses_last_4qXX": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_135(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "MISSES_LAST_4Q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_136(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "XXconsec_beatsXX": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_137(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "CONSEC_BEATS": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_138(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "XXavg_surprise_pctXX": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_139(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "AVG_SURPRISE_PCT": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_140(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "XXlast_surprise_pctXX": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_141(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "LAST_SURPRISE_PCT": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_142(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = None
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_143(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = None
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_144(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=None)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_145(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = None
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_146(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) and []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_147(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(None, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_148(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=None) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_149(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_150(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, ) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_151(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=5) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_152(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) > 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_153(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 3:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_154(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = None
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_155(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get(None, 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_156(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", None) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_157(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get(0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_158(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", ) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_159(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("XXsurprisePercentXX", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_160(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisepercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_161(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("SURPRISEPERCENT", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_162(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 1) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_163(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get(None) is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_164(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("XXsurprisePercentXX") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_165(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisepercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_166(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("SURPRISEPERCENT") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_167(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_168(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) > 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_169(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 3:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_170(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = None
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_171(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(None, 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_172(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], None)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_173(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_174(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], )
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_175(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] + _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_176(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[1] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_177(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[+1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_178(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-2], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_179(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 3)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_180(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = None
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_181(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["XXsurprise_accelerationXX"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_182(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["SURPRISE_ACCELERATION"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_183(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = None
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_184(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["XXquarterly_surprisesXX"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_185(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["QUARTERLY_SURPRISES"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_186(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(None, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_187(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, None) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_188(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_189(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, ) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_190(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 3) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_191(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = None
        return result
    except Exception as e:
        log.warning(f"[earnings] surprise {ticker}: {e}")
        return {}


def x__fetch_earnings_surprise__mutmut_192(ticker: str) -> dict:
    cached = _surp_cache.get(ticker)
    if cached and _time.time() - cached[1] < CACHE_TTL:
        return cached[0]
    try:
        t = yf.Ticker(ticker, session=_session)
        hist = _retry(lambda: t.earnings_history)
        if hist is None or (hasattr(hist, "empty") and hist.empty):
            return {}

        # Normalise column names — yfinance varies across versions
        col_map = {}
        for c in hist.columns:
            cl = c.lower().replace(" ", "_")
            if "actual" in cl:
                col_map["actual"] = c
            elif "estimate" in cl:
                col_map["estimate"] = c

        if "actual" not in col_map or "estimate" not in col_map:
            return {}

        recent = hist.tail(4)
        beats = misses = 0
        surprise_pcts = []
        for _, row in recent.iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                continue
            try:
                a, e = float(act), float(est)
                if a > e:
                    beats += 1
                elif a < e:
                    misses += 1
                if abs(e) > 0.001:
                    surprise_pcts.append((a - e) / abs(e) * 100)
            except (TypeError, ValueError):
                pass

        # Consecutive beats from most recent backwards
        consec = 0
        for _, row in hist.iloc[::-1].iterrows():
            act = row.get(col_map["actual"])
            est = row.get(col_map["estimate"])
            if act is None or est is None:
                break
            try:
                if float(act) > float(est):
                    consec += 1
                else:
                    break
            except (TypeError, ValueError):
                break

        avg_surprise = round(sum(surprise_pcts) / len(surprise_pcts), 1) if surprise_pcts else None
        last_surprise = round(surprise_pcts[-1], 1) if surprise_pcts else None

        result = {
            "beats_last_4q": beats,
            "misses_last_4q": misses,
            "consec_beats": consec,
            "avg_surprise_pct": avg_surprise,
            "last_surprise_pct": last_surprise,
        }

        # ── Finnhub EPS surprise acceleration ────────────────────────────────
        # Compares Q1 (most recent) vs Q4 (oldest) surprise magnitude.
        # Accelerating beats = management execution improving = alpha signal.
        # Uses the same Finnhub key already configured; zero extra auth cost.
        try:
            import finnhub
            from config import get_settings

            _key = get_settings().finnhub_api_key
            if _key:
                _client = finnhub.Client(api_key=_key)
                _fh_eps = _client.company_earnings(ticker, limit=4) or []
                if len(_fh_eps) >= 2:
                    _surp_list = [q.get("surprisePercent", 0) for q in _fh_eps if q.get("surprisePercent") is not None]
                    if len(_surp_list) >= 2:
                        # Index 0 = most recent, last index = oldest
                        acceleration = round(_surp_list[0] - _surp_list[-1], 2)
                        result["surprise_acceleration"] = acceleration
                        result["quarterly_surprises"] = [round(s, 2) for s in _surp_list]
        except Exception:
            pass  # Finnhub unavailable — yfinance data still valid

        _surp_cache[ticker] = (result, _time.time())
        return result
    except Exception as e:
        log.warning(None)
        return {}

mutants_x__fetch_earnings_surprise__mutmut['_mutmut_orig'] = x__fetch_earnings_surprise__mutmut_orig # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_1'] = x__fetch_earnings_surprise__mutmut_1 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_2'] = x__fetch_earnings_surprise__mutmut_2 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_3'] = x__fetch_earnings_surprise__mutmut_3 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_4'] = x__fetch_earnings_surprise__mutmut_4 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_5'] = x__fetch_earnings_surprise__mutmut_5 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_6'] = x__fetch_earnings_surprise__mutmut_6 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_7'] = x__fetch_earnings_surprise__mutmut_7 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_8'] = x__fetch_earnings_surprise__mutmut_8 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_9'] = x__fetch_earnings_surprise__mutmut_9 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_10'] = x__fetch_earnings_surprise__mutmut_10 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_11'] = x__fetch_earnings_surprise__mutmut_11 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_12'] = x__fetch_earnings_surprise__mutmut_12 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_13'] = x__fetch_earnings_surprise__mutmut_13 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_14'] = x__fetch_earnings_surprise__mutmut_14 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_15'] = x__fetch_earnings_surprise__mutmut_15 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_16'] = x__fetch_earnings_surprise__mutmut_16 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_17'] = x__fetch_earnings_surprise__mutmut_17 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_18'] = x__fetch_earnings_surprise__mutmut_18 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_19'] = x__fetch_earnings_surprise__mutmut_19 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_20'] = x__fetch_earnings_surprise__mutmut_20 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_21'] = x__fetch_earnings_surprise__mutmut_21 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_22'] = x__fetch_earnings_surprise__mutmut_22 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_23'] = x__fetch_earnings_surprise__mutmut_23 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_24'] = x__fetch_earnings_surprise__mutmut_24 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_25'] = x__fetch_earnings_surprise__mutmut_25 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_26'] = x__fetch_earnings_surprise__mutmut_26 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_27'] = x__fetch_earnings_surprise__mutmut_27 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_28'] = x__fetch_earnings_surprise__mutmut_28 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_29'] = x__fetch_earnings_surprise__mutmut_29 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_30'] = x__fetch_earnings_surprise__mutmut_30 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_31'] = x__fetch_earnings_surprise__mutmut_31 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_32'] = x__fetch_earnings_surprise__mutmut_32 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_33'] = x__fetch_earnings_surprise__mutmut_33 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_34'] = x__fetch_earnings_surprise__mutmut_34 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_35'] = x__fetch_earnings_surprise__mutmut_35 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_36'] = x__fetch_earnings_surprise__mutmut_36 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_37'] = x__fetch_earnings_surprise__mutmut_37 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_38'] = x__fetch_earnings_surprise__mutmut_38 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_39'] = x__fetch_earnings_surprise__mutmut_39 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_40'] = x__fetch_earnings_surprise__mutmut_40 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_41'] = x__fetch_earnings_surprise__mutmut_41 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_42'] = x__fetch_earnings_surprise__mutmut_42 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_43'] = x__fetch_earnings_surprise__mutmut_43 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_44'] = x__fetch_earnings_surprise__mutmut_44 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_45'] = x__fetch_earnings_surprise__mutmut_45 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_46'] = x__fetch_earnings_surprise__mutmut_46 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_47'] = x__fetch_earnings_surprise__mutmut_47 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_48'] = x__fetch_earnings_surprise__mutmut_48 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_49'] = x__fetch_earnings_surprise__mutmut_49 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_50'] = x__fetch_earnings_surprise__mutmut_50 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_51'] = x__fetch_earnings_surprise__mutmut_51 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_52'] = x__fetch_earnings_surprise__mutmut_52 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_53'] = x__fetch_earnings_surprise__mutmut_53 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_54'] = x__fetch_earnings_surprise__mutmut_54 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_55'] = x__fetch_earnings_surprise__mutmut_55 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_56'] = x__fetch_earnings_surprise__mutmut_56 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_57'] = x__fetch_earnings_surprise__mutmut_57 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_58'] = x__fetch_earnings_surprise__mutmut_58 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_59'] = x__fetch_earnings_surprise__mutmut_59 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_60'] = x__fetch_earnings_surprise__mutmut_60 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_61'] = x__fetch_earnings_surprise__mutmut_61 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_62'] = x__fetch_earnings_surprise__mutmut_62 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_63'] = x__fetch_earnings_surprise__mutmut_63 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_64'] = x__fetch_earnings_surprise__mutmut_64 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_65'] = x__fetch_earnings_surprise__mutmut_65 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_66'] = x__fetch_earnings_surprise__mutmut_66 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_67'] = x__fetch_earnings_surprise__mutmut_67 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_68'] = x__fetch_earnings_surprise__mutmut_68 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_69'] = x__fetch_earnings_surprise__mutmut_69 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_70'] = x__fetch_earnings_surprise__mutmut_70 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_71'] = x__fetch_earnings_surprise__mutmut_71 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_72'] = x__fetch_earnings_surprise__mutmut_72 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_73'] = x__fetch_earnings_surprise__mutmut_73 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_74'] = x__fetch_earnings_surprise__mutmut_74 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_75'] = x__fetch_earnings_surprise__mutmut_75 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_76'] = x__fetch_earnings_surprise__mutmut_76 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_77'] = x__fetch_earnings_surprise__mutmut_77 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_78'] = x__fetch_earnings_surprise__mutmut_78 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_79'] = x__fetch_earnings_surprise__mutmut_79 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_80'] = x__fetch_earnings_surprise__mutmut_80 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_81'] = x__fetch_earnings_surprise__mutmut_81 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_82'] = x__fetch_earnings_surprise__mutmut_82 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_83'] = x__fetch_earnings_surprise__mutmut_83 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_84'] = x__fetch_earnings_surprise__mutmut_84 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_85'] = x__fetch_earnings_surprise__mutmut_85 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_86'] = x__fetch_earnings_surprise__mutmut_86 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_87'] = x__fetch_earnings_surprise__mutmut_87 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_88'] = x__fetch_earnings_surprise__mutmut_88 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_89'] = x__fetch_earnings_surprise__mutmut_89 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_90'] = x__fetch_earnings_surprise__mutmut_90 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_91'] = x__fetch_earnings_surprise__mutmut_91 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_92'] = x__fetch_earnings_surprise__mutmut_92 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_93'] = x__fetch_earnings_surprise__mutmut_93 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_94'] = x__fetch_earnings_surprise__mutmut_94 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_95'] = x__fetch_earnings_surprise__mutmut_95 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_96'] = x__fetch_earnings_surprise__mutmut_96 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_97'] = x__fetch_earnings_surprise__mutmut_97 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_98'] = x__fetch_earnings_surprise__mutmut_98 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_99'] = x__fetch_earnings_surprise__mutmut_99 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_100'] = x__fetch_earnings_surprise__mutmut_100 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_101'] = x__fetch_earnings_surprise__mutmut_101 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_102'] = x__fetch_earnings_surprise__mutmut_102 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_103'] = x__fetch_earnings_surprise__mutmut_103 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_104'] = x__fetch_earnings_surprise__mutmut_104 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_105'] = x__fetch_earnings_surprise__mutmut_105 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_106'] = x__fetch_earnings_surprise__mutmut_106 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_107'] = x__fetch_earnings_surprise__mutmut_107 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_108'] = x__fetch_earnings_surprise__mutmut_108 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_109'] = x__fetch_earnings_surprise__mutmut_109 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_110'] = x__fetch_earnings_surprise__mutmut_110 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_111'] = x__fetch_earnings_surprise__mutmut_111 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_112'] = x__fetch_earnings_surprise__mutmut_112 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_113'] = x__fetch_earnings_surprise__mutmut_113 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_114'] = x__fetch_earnings_surprise__mutmut_114 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_115'] = x__fetch_earnings_surprise__mutmut_115 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_116'] = x__fetch_earnings_surprise__mutmut_116 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_117'] = x__fetch_earnings_surprise__mutmut_117 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_118'] = x__fetch_earnings_surprise__mutmut_118 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_119'] = x__fetch_earnings_surprise__mutmut_119 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_120'] = x__fetch_earnings_surprise__mutmut_120 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_121'] = x__fetch_earnings_surprise__mutmut_121 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_122'] = x__fetch_earnings_surprise__mutmut_122 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_123'] = x__fetch_earnings_surprise__mutmut_123 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_124'] = x__fetch_earnings_surprise__mutmut_124 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_125'] = x__fetch_earnings_surprise__mutmut_125 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_126'] = x__fetch_earnings_surprise__mutmut_126 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_127'] = x__fetch_earnings_surprise__mutmut_127 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_128'] = x__fetch_earnings_surprise__mutmut_128 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_129'] = x__fetch_earnings_surprise__mutmut_129 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_130'] = x__fetch_earnings_surprise__mutmut_130 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_131'] = x__fetch_earnings_surprise__mutmut_131 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_132'] = x__fetch_earnings_surprise__mutmut_132 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_133'] = x__fetch_earnings_surprise__mutmut_133 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_134'] = x__fetch_earnings_surprise__mutmut_134 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_135'] = x__fetch_earnings_surprise__mutmut_135 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_136'] = x__fetch_earnings_surprise__mutmut_136 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_137'] = x__fetch_earnings_surprise__mutmut_137 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_138'] = x__fetch_earnings_surprise__mutmut_138 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_139'] = x__fetch_earnings_surprise__mutmut_139 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_140'] = x__fetch_earnings_surprise__mutmut_140 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_141'] = x__fetch_earnings_surprise__mutmut_141 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_142'] = x__fetch_earnings_surprise__mutmut_142 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_143'] = x__fetch_earnings_surprise__mutmut_143 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_144'] = x__fetch_earnings_surprise__mutmut_144 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_145'] = x__fetch_earnings_surprise__mutmut_145 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_146'] = x__fetch_earnings_surprise__mutmut_146 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_147'] = x__fetch_earnings_surprise__mutmut_147 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_148'] = x__fetch_earnings_surprise__mutmut_148 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_149'] = x__fetch_earnings_surprise__mutmut_149 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_150'] = x__fetch_earnings_surprise__mutmut_150 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_151'] = x__fetch_earnings_surprise__mutmut_151 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_152'] = x__fetch_earnings_surprise__mutmut_152 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_153'] = x__fetch_earnings_surprise__mutmut_153 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_154'] = x__fetch_earnings_surprise__mutmut_154 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_155'] = x__fetch_earnings_surprise__mutmut_155 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_156'] = x__fetch_earnings_surprise__mutmut_156 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_157'] = x__fetch_earnings_surprise__mutmut_157 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_158'] = x__fetch_earnings_surprise__mutmut_158 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_159'] = x__fetch_earnings_surprise__mutmut_159 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_160'] = x__fetch_earnings_surprise__mutmut_160 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_161'] = x__fetch_earnings_surprise__mutmut_161 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_162'] = x__fetch_earnings_surprise__mutmut_162 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_163'] = x__fetch_earnings_surprise__mutmut_163 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_164'] = x__fetch_earnings_surprise__mutmut_164 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_165'] = x__fetch_earnings_surprise__mutmut_165 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_166'] = x__fetch_earnings_surprise__mutmut_166 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_167'] = x__fetch_earnings_surprise__mutmut_167 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_168'] = x__fetch_earnings_surprise__mutmut_168 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_169'] = x__fetch_earnings_surprise__mutmut_169 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_170'] = x__fetch_earnings_surprise__mutmut_170 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_171'] = x__fetch_earnings_surprise__mutmut_171 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_172'] = x__fetch_earnings_surprise__mutmut_172 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_173'] = x__fetch_earnings_surprise__mutmut_173 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_174'] = x__fetch_earnings_surprise__mutmut_174 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_175'] = x__fetch_earnings_surprise__mutmut_175 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_176'] = x__fetch_earnings_surprise__mutmut_176 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_177'] = x__fetch_earnings_surprise__mutmut_177 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_178'] = x__fetch_earnings_surprise__mutmut_178 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_179'] = x__fetch_earnings_surprise__mutmut_179 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_180'] = x__fetch_earnings_surprise__mutmut_180 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_181'] = x__fetch_earnings_surprise__mutmut_181 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_182'] = x__fetch_earnings_surprise__mutmut_182 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_183'] = x__fetch_earnings_surprise__mutmut_183 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_184'] = x__fetch_earnings_surprise__mutmut_184 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_185'] = x__fetch_earnings_surprise__mutmut_185 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_186'] = x__fetch_earnings_surprise__mutmut_186 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_187'] = x__fetch_earnings_surprise__mutmut_187 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_188'] = x__fetch_earnings_surprise__mutmut_188 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_189'] = x__fetch_earnings_surprise__mutmut_189 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_190'] = x__fetch_earnings_surprise__mutmut_190 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_191'] = x__fetch_earnings_surprise__mutmut_191 # type: ignore # mutmut generated
mutants_x__fetch_earnings_surprise__mutmut['x__fetch_earnings_surprise__mutmut_192'] = x__fetch_earnings_surprise__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_earnings_calendar__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_earnings_calendar__mutmut)
async def get_earnings_calendar(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_calendar, ticker)


async def x_get_earnings_calendar__mutmut_orig(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_calendar, ticker)


async def x_get_earnings_calendar__mutmut_1(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(None, _fetch_earnings_calendar, ticker)


async def x_get_earnings_calendar__mutmut_2(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, None, ticker)


async def x_get_earnings_calendar__mutmut_3(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_calendar, None)


async def x_get_earnings_calendar__mutmut_4(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_fetch_earnings_calendar, ticker)


async def x_get_earnings_calendar__mutmut_5(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, ticker)


async def x_get_earnings_calendar__mutmut_6(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_calendar, )

mutants_x_get_earnings_calendar__mutmut['_mutmut_orig'] = x_get_earnings_calendar__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_earnings_calendar__mutmut['x_get_earnings_calendar__mutmut_1'] = x_get_earnings_calendar__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_earnings_calendar__mutmut['x_get_earnings_calendar__mutmut_2'] = x_get_earnings_calendar__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_earnings_calendar__mutmut['x_get_earnings_calendar__mutmut_3'] = x_get_earnings_calendar__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_earnings_calendar__mutmut['x_get_earnings_calendar__mutmut_4'] = x_get_earnings_calendar__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_earnings_calendar__mutmut['x_get_earnings_calendar__mutmut_5'] = x_get_earnings_calendar__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_earnings_calendar__mutmut['x_get_earnings_calendar__mutmut_6'] = x_get_earnings_calendar__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_earnings_surprise__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_earnings_surprise__mutmut)
async def get_earnings_surprise(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_surprise, ticker)


async def x_get_earnings_surprise__mutmut_orig(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_surprise, ticker)


async def x_get_earnings_surprise__mutmut_1(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(None, _fetch_earnings_surprise, ticker)


async def x_get_earnings_surprise__mutmut_2(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, None, ticker)


async def x_get_earnings_surprise__mutmut_3(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_surprise, None)


async def x_get_earnings_surprise__mutmut_4(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_fetch_earnings_surprise, ticker)


async def x_get_earnings_surprise__mutmut_5(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, ticker)


async def x_get_earnings_surprise__mutmut_6(ticker: str) -> dict:
    return await asyncio.get_running_loop().run_in_executor(_executor, _fetch_earnings_surprise, )

mutants_x_get_earnings_surprise__mutmut['_mutmut_orig'] = x_get_earnings_surprise__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_earnings_surprise__mutmut['x_get_earnings_surprise__mutmut_1'] = x_get_earnings_surprise__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_earnings_surprise__mutmut['x_get_earnings_surprise__mutmut_2'] = x_get_earnings_surprise__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_earnings_surprise__mutmut['x_get_earnings_surprise__mutmut_3'] = x_get_earnings_surprise__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_earnings_surprise__mutmut['x_get_earnings_surprise__mutmut_4'] = x_get_earnings_surprise__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_earnings_surprise__mutmut['x_get_earnings_surprise__mutmut_5'] = x_get_earnings_surprise__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_earnings_surprise__mutmut['x_get_earnings_surprise__mutmut_6'] = x_get_earnings_surprise__mutmut_6 # type: ignore # mutmut generated
