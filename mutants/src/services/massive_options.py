"""
Massive Options — Full Option Chain Snapshot

Replaces the single-endpoint GEX hack with a full option chain analysis:
  • Net GEX across all strikes and expiries (dealer positioning)
  • 25-delta skew (put-call IV asymmetry — fear gauge)
  • Term structure: VIX term premium / contango vs backwardation
  • Max pain strike (price where options expire most worthless)
  • Unusual strike concentration (OI clustering = magnet levels)

Cache: 15 minutes (options data moves fast).
"""

import logging
import os
import time

import aiohttp
from services.http_client import get_ssl_context, shared_session

log = logging.getLogger("signal.trade.massive_options")

_cache: dict[str, dict] = {}
_TTL = 300  # 5 minutes — unlimited Polygon calls

_BASE = "https://api.polygon.io"


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict
mutants_x_get_option_chain_signals__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_option_chain_signals__mutmut)
async def get_option_chain_signals(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_orig(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_1(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = None
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_2(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache or now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_3(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker not in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_4(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now + _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_5(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["XXtsXX"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_6(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["TS"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_7(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] <= _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_8(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["XXdataXX"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_9(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["DATA"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_10(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = None
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_11(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv(None)
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_12(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("XXMASSIVE_API_KEYXX")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_13(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("massive_api_key")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_14(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_15(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = None
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_16(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = None

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_17(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"XXapiKeyXX": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_18(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apikey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_19(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"APIKEY": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_20(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "XXlimitXX": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_21(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "LIMIT": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_22(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 251}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_23(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = None
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_24(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(None, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_25(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=None, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_26(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=None, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_27(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=None) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_28(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_29(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_30(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_31(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, ) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_32(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=None)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_33(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=11)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_34(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status == 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_35(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 201:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_36(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = None
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_37(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = None
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_38(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") and []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_39(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get(None) or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_40(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("XXresultsXX") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_41(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("RESULTS") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_42(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(None)
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_43(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_44(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = None
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_45(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 1.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_46(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = None
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_47(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = None

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_48(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = ""
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_49(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = None

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_50(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float(None)

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_51(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("XXinfXX")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_52(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("INF")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_53(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = None
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_54(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(None)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_55(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") and 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_56(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") and contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_57(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get(None) or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_58(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("XXstrike_priceXX") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_59(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("STRIKE_PRICE") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_60(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get(None) or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_61(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("XXstrikeXX") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_62(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("STRIKE") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_63(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 1)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_64(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = None
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_65(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").upper()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_66(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") and "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_67(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") and contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_68(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get(None) or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_69(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("XXcontract_typeXX") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_70(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("CONTRACT_TYPE") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_71(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get(None) or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_72(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("XXoption_typeXX") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_73(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("OPTION_TYPE") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_74(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "XXXX").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_75(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = None
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_76(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(None)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_77(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") and 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_78(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get(None) or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_79(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("XXopen_interestXX") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_80(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("OPEN_INTEREST") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_81(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 1)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_82(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = None
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_83(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(None)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_84(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") and 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_85(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") and contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_86(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get(None) or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_87(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get(None, {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_88(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", None).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_89(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get({}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_90(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", ).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_91(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("XXgreeksXX", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_92(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("GREEKS", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_93(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("XXgammaXX") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_94(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("GAMMA") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_95(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get(None) or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_96(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("XXgammaXX") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_97(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("GAMMA") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_98(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 1)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_99(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = None
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_100(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(None)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_101(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") and 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_102(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") and contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_103(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get(None) or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_104(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("XXimplied_volatilityXX") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_105(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("IMPLIED_VOLATILITY") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_106(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get(None) or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_107(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("XXivXX") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_108(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("IV") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_109(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 1)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_110(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = None

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_111(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(None)

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_112(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(None))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_113(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") and 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_114(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") and contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_115(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get(None) or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_116(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get(None, {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_117(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", None).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_118(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get({}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_119(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", ).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_120(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("XXgreeksXX", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_121(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("GREEKS", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_122(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("XXdeltaXX") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_123(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("DELTA") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_124(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get(None) or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_125(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("XXdeltaXX") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_126(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("DELTA") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_127(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 1))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_128(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 and oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_129(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike < 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_130(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 1 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_131(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi < 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_132(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 1:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_133(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            break

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_134(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = None
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_135(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price * 2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_136(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**3
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_137(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = None
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_138(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 / spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_139(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi / 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_140(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma / oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_141(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 101 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_142(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith(None):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_143(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("XXcXX"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_144(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("C"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_145(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex = gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_146(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex -= gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_147(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = None
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_148(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) - oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_149(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(None, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_150(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, None) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_151(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_152(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, ) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_153(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 1) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_154(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith(None):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_155(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("XXpXX"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_156(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("P"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_157(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex = gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_158(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex += gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_159(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = None

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_160(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) - oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_161(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(None, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_162(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, None) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_163(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_164(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, ) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_165(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 1) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_166(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = None
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_167(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(None)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_168(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike + current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_169(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv >= 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_170(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 1:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_171(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist <= atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_172(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = None
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_173(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith(None):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_174(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("XXcXX"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_175(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("C"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_176(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = None
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_177(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith(None):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_178(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("XXpXX"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_179(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("P"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_180(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = None
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_181(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 1.2 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_182(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 < delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_183(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs < 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_184(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 1.3:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_185(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith(None):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_186(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("XXcXX"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_187(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("C"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_188(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = None
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_189(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith(None):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_190(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("XXpXX"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_191(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("P"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_192(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = None

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_193(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = ""
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_194(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike and put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_195(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = None
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_196(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(None)
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_197(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(None))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_198(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) - list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_199(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(None) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_200(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(None)))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_201(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = None
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_202(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float(None)
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_203(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("XXinfXX")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_204(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("INF")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_205(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = None
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_206(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(None)
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_207(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) / oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_208(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(None, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_209(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, None) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_210(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_211(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, ) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_212(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(1, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_213(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s + s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_214(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = None
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_215(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(None)
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_216(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) / oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_217(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(None, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_218(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, None) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_219(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_220(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, ) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_221(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(1, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_222(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s + test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_223(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = None
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_224(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain - put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_225(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain <= min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_226(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = None

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_227(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = ""
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_228(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None or d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_229(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_230(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_231(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = None  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_232(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round(None, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_233(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, None)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_234(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round(2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_235(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, )  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_236(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) / 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_237(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv + d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_238(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 101, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_239(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 3)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_240(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = None

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_241(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "XXnet_gexXX": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_242(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "NET_GEX": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_243(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(None, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_244(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, None),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_245(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_246(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, ),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_247(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex * 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_248(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1000001, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_249(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 3),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_250(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "XXmax_painXX": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_251(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "MAX_PAIN": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_252(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "XXskew_25dXX": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_253(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "SKEW_25D": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_254(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "XXatm_call_ivXX": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_255(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "ATM_CALL_IV": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_256(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(None, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_257(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, None) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_258(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_259(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, ) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_260(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv / 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_261(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 101, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_262(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 2) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_263(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "XXatm_put_ivXX": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_264(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "ATM_PUT_IV": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_265(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(None, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_266(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, None) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_267(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_268(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, ) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_269(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv / 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_270(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 101, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_271(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 2) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_272(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "XXcontracts_parsedXX": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_273(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "CONTRACTS_PARSED": len(chain),
    }

    _cache[ticker] = {"data": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_274(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = None
    return result


async def x_get_option_chain_signals__mutmut_275(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"XXdataXX": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_276(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"DATA": result, "ts": now}
    return result


async def x_get_option_chain_signals__mutmut_277(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "XXtsXX": now}
    return result


async def x_get_option_chain_signals__mutmut_278(ticker: str, current_price: float) -> dict:
    """
    Fetch full option chain snapshot and derive GEX, skew, max pain, term structure.
    Returns enriched signal dict compatible with signal_engine existing options block.
    """
    now = time.time()
    if ticker in _cache and now - _cache[ticker]["ts"] < _TTL:
        return _cache[ticker]["data"]

    api_key = os.getenv("MASSIVE_API_KEY")
    if not api_key:
        return {}

    # Polygon.io options chain endpoint (requires Starter plan or higher)
    url = f"{_BASE}/v3/snapshot/options/{ticker}"
    params = {"apiKey": api_key, "limit": 250}

    ssl_ctx = get_ssl_context()
    try:
        async with shared_session() as session:
            async with session.get(url, params=params, ssl=ssl_ctx, timeout=aiohttp.ClientTimeout(total=10)) as resp:
                if resp.status != 200:
                    return {}
                data = await resp.json()
                chain = data.get("results") or []
    except Exception as e:
        log.debug(f"[massive_options] {ticker} fetch failed: {e}")
        return {}

    if not chain:
        return {}

    # ── Net GEX across full chain ─────────────────────────────────────────────
    net_gex = 0.0
    call_oi_by_strike: dict[float, float] = {}
    put_oi_by_strike: dict[float, float] = {}

    atm_call_iv = atm_put_iv = d25_call_iv = d25_put_iv = None
    atm_dist = float("inf")

    for contract in chain:
        strike = float(contract.get("strike_price") or contract.get("strike") or 0)
        cp = (contract.get("contract_type") or contract.get("option_type") or "").lower()
        oi = float(contract.get("open_interest") or 0)
        gamma = float(contract.get("greeks", {}).get("gamma") or contract.get("gamma") or 0)
        iv = float(contract.get("implied_volatility") or contract.get("iv") or 0)
        delta_abs = abs(float(contract.get("greeks", {}).get("delta") or contract.get("delta") or 0))

        if strike <= 0 or oi <= 0:
            continue

        # GEX = gamma × OI × 100 (shares per contract) × spot²
        spot_sq = current_price**2
        gex_contrib = gamma * oi * 100 * spot_sq
        if cp.startswith("c"):
            net_gex += gex_contrib
            call_oi_by_strike[strike] = call_oi_by_strike.get(strike, 0) + oi
        elif cp.startswith("p"):
            net_gex -= gex_contrib
            put_oi_by_strike[strike] = put_oi_by_strike.get(strike, 0) + oi

        # Track ATM and 25-delta IVs for skew
        dist = abs(strike - current_price)
        if iv > 0:
            if dist < atm_dist:
                atm_dist = dist
                if cp.startswith("c"):
                    atm_call_iv = iv
                elif cp.startswith("p"):
                    atm_put_iv = iv
            # 25-delta: delta ≈ 0.25
            if 0.20 <= delta_abs <= 0.30:
                if cp.startswith("c"):
                    d25_call_iv = iv
                elif cp.startswith("p"):
                    d25_put_iv = iv

    # ── Max pain (strike with minimum combined option value at expiry) ─────────
    max_pain_strike = None
    if call_oi_by_strike or put_oi_by_strike:
        all_strikes = sorted(set(list(call_oi_by_strike.keys()) + list(put_oi_by_strike.keys())))
        min_pain = float("inf")
        for test_s in all_strikes:
            # Value to call holders if expired at test_s
            call_pain = sum(max(0, test_s - s) * oi for s, oi in call_oi_by_strike.items())
            put_pain = sum(max(0, s - test_s) * oi for s, oi in put_oi_by_strike.items())
            total_pain = call_pain + put_pain
            if total_pain < min_pain:
                min_pain, max_pain_strike = total_pain, test_s

    # ── 25-delta skew ─────────────────────────────────────────────────────────
    skew_25d = None
    if d25_put_iv is not None and d25_call_iv is not None:
        skew_25d = round((d25_put_iv - d25_call_iv) * 100, 2)  # in vols points

    result = {
        "net_gex": round(net_gex / 1_000_000, 2),  # $M
        "max_pain": max_pain_strike,
        "skew_25d": skew_25d,  # positive = put IV > call IV (fear)
        "atm_call_iv": round(atm_call_iv * 100, 1) if atm_call_iv else None,
        "atm_put_iv": round(atm_put_iv * 100, 1) if atm_put_iv else None,
        "contracts_parsed": len(chain),
    }

    _cache[ticker] = {"data": result, "TS": now}
    return result

mutants_x_get_option_chain_signals__mutmut['_mutmut_orig'] = x_get_option_chain_signals__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_1'] = x_get_option_chain_signals__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_2'] = x_get_option_chain_signals__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_3'] = x_get_option_chain_signals__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_4'] = x_get_option_chain_signals__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_5'] = x_get_option_chain_signals__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_6'] = x_get_option_chain_signals__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_7'] = x_get_option_chain_signals__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_8'] = x_get_option_chain_signals__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_9'] = x_get_option_chain_signals__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_10'] = x_get_option_chain_signals__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_11'] = x_get_option_chain_signals__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_12'] = x_get_option_chain_signals__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_13'] = x_get_option_chain_signals__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_14'] = x_get_option_chain_signals__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_15'] = x_get_option_chain_signals__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_16'] = x_get_option_chain_signals__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_17'] = x_get_option_chain_signals__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_18'] = x_get_option_chain_signals__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_19'] = x_get_option_chain_signals__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_20'] = x_get_option_chain_signals__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_21'] = x_get_option_chain_signals__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_22'] = x_get_option_chain_signals__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_23'] = x_get_option_chain_signals__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_24'] = x_get_option_chain_signals__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_25'] = x_get_option_chain_signals__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_26'] = x_get_option_chain_signals__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_27'] = x_get_option_chain_signals__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_28'] = x_get_option_chain_signals__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_29'] = x_get_option_chain_signals__mutmut_29 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_30'] = x_get_option_chain_signals__mutmut_30 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_31'] = x_get_option_chain_signals__mutmut_31 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_32'] = x_get_option_chain_signals__mutmut_32 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_33'] = x_get_option_chain_signals__mutmut_33 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_34'] = x_get_option_chain_signals__mutmut_34 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_35'] = x_get_option_chain_signals__mutmut_35 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_36'] = x_get_option_chain_signals__mutmut_36 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_37'] = x_get_option_chain_signals__mutmut_37 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_38'] = x_get_option_chain_signals__mutmut_38 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_39'] = x_get_option_chain_signals__mutmut_39 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_40'] = x_get_option_chain_signals__mutmut_40 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_41'] = x_get_option_chain_signals__mutmut_41 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_42'] = x_get_option_chain_signals__mutmut_42 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_43'] = x_get_option_chain_signals__mutmut_43 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_44'] = x_get_option_chain_signals__mutmut_44 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_45'] = x_get_option_chain_signals__mutmut_45 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_46'] = x_get_option_chain_signals__mutmut_46 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_47'] = x_get_option_chain_signals__mutmut_47 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_48'] = x_get_option_chain_signals__mutmut_48 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_49'] = x_get_option_chain_signals__mutmut_49 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_50'] = x_get_option_chain_signals__mutmut_50 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_51'] = x_get_option_chain_signals__mutmut_51 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_52'] = x_get_option_chain_signals__mutmut_52 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_53'] = x_get_option_chain_signals__mutmut_53 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_54'] = x_get_option_chain_signals__mutmut_54 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_55'] = x_get_option_chain_signals__mutmut_55 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_56'] = x_get_option_chain_signals__mutmut_56 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_57'] = x_get_option_chain_signals__mutmut_57 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_58'] = x_get_option_chain_signals__mutmut_58 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_59'] = x_get_option_chain_signals__mutmut_59 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_60'] = x_get_option_chain_signals__mutmut_60 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_61'] = x_get_option_chain_signals__mutmut_61 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_62'] = x_get_option_chain_signals__mutmut_62 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_63'] = x_get_option_chain_signals__mutmut_63 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_64'] = x_get_option_chain_signals__mutmut_64 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_65'] = x_get_option_chain_signals__mutmut_65 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_66'] = x_get_option_chain_signals__mutmut_66 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_67'] = x_get_option_chain_signals__mutmut_67 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_68'] = x_get_option_chain_signals__mutmut_68 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_69'] = x_get_option_chain_signals__mutmut_69 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_70'] = x_get_option_chain_signals__mutmut_70 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_71'] = x_get_option_chain_signals__mutmut_71 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_72'] = x_get_option_chain_signals__mutmut_72 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_73'] = x_get_option_chain_signals__mutmut_73 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_74'] = x_get_option_chain_signals__mutmut_74 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_75'] = x_get_option_chain_signals__mutmut_75 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_76'] = x_get_option_chain_signals__mutmut_76 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_77'] = x_get_option_chain_signals__mutmut_77 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_78'] = x_get_option_chain_signals__mutmut_78 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_79'] = x_get_option_chain_signals__mutmut_79 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_80'] = x_get_option_chain_signals__mutmut_80 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_81'] = x_get_option_chain_signals__mutmut_81 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_82'] = x_get_option_chain_signals__mutmut_82 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_83'] = x_get_option_chain_signals__mutmut_83 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_84'] = x_get_option_chain_signals__mutmut_84 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_85'] = x_get_option_chain_signals__mutmut_85 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_86'] = x_get_option_chain_signals__mutmut_86 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_87'] = x_get_option_chain_signals__mutmut_87 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_88'] = x_get_option_chain_signals__mutmut_88 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_89'] = x_get_option_chain_signals__mutmut_89 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_90'] = x_get_option_chain_signals__mutmut_90 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_91'] = x_get_option_chain_signals__mutmut_91 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_92'] = x_get_option_chain_signals__mutmut_92 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_93'] = x_get_option_chain_signals__mutmut_93 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_94'] = x_get_option_chain_signals__mutmut_94 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_95'] = x_get_option_chain_signals__mutmut_95 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_96'] = x_get_option_chain_signals__mutmut_96 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_97'] = x_get_option_chain_signals__mutmut_97 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_98'] = x_get_option_chain_signals__mutmut_98 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_99'] = x_get_option_chain_signals__mutmut_99 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_100'] = x_get_option_chain_signals__mutmut_100 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_101'] = x_get_option_chain_signals__mutmut_101 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_102'] = x_get_option_chain_signals__mutmut_102 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_103'] = x_get_option_chain_signals__mutmut_103 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_104'] = x_get_option_chain_signals__mutmut_104 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_105'] = x_get_option_chain_signals__mutmut_105 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_106'] = x_get_option_chain_signals__mutmut_106 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_107'] = x_get_option_chain_signals__mutmut_107 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_108'] = x_get_option_chain_signals__mutmut_108 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_109'] = x_get_option_chain_signals__mutmut_109 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_110'] = x_get_option_chain_signals__mutmut_110 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_111'] = x_get_option_chain_signals__mutmut_111 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_112'] = x_get_option_chain_signals__mutmut_112 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_113'] = x_get_option_chain_signals__mutmut_113 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_114'] = x_get_option_chain_signals__mutmut_114 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_115'] = x_get_option_chain_signals__mutmut_115 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_116'] = x_get_option_chain_signals__mutmut_116 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_117'] = x_get_option_chain_signals__mutmut_117 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_118'] = x_get_option_chain_signals__mutmut_118 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_119'] = x_get_option_chain_signals__mutmut_119 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_120'] = x_get_option_chain_signals__mutmut_120 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_121'] = x_get_option_chain_signals__mutmut_121 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_122'] = x_get_option_chain_signals__mutmut_122 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_123'] = x_get_option_chain_signals__mutmut_123 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_124'] = x_get_option_chain_signals__mutmut_124 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_125'] = x_get_option_chain_signals__mutmut_125 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_126'] = x_get_option_chain_signals__mutmut_126 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_127'] = x_get_option_chain_signals__mutmut_127 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_128'] = x_get_option_chain_signals__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_129'] = x_get_option_chain_signals__mutmut_129 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_130'] = x_get_option_chain_signals__mutmut_130 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_131'] = x_get_option_chain_signals__mutmut_131 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_132'] = x_get_option_chain_signals__mutmut_132 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_133'] = x_get_option_chain_signals__mutmut_133 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_134'] = x_get_option_chain_signals__mutmut_134 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_135'] = x_get_option_chain_signals__mutmut_135 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_136'] = x_get_option_chain_signals__mutmut_136 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_137'] = x_get_option_chain_signals__mutmut_137 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_138'] = x_get_option_chain_signals__mutmut_138 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_139'] = x_get_option_chain_signals__mutmut_139 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_140'] = x_get_option_chain_signals__mutmut_140 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_141'] = x_get_option_chain_signals__mutmut_141 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_142'] = x_get_option_chain_signals__mutmut_142 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_143'] = x_get_option_chain_signals__mutmut_143 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_144'] = x_get_option_chain_signals__mutmut_144 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_145'] = x_get_option_chain_signals__mutmut_145 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_146'] = x_get_option_chain_signals__mutmut_146 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_147'] = x_get_option_chain_signals__mutmut_147 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_148'] = x_get_option_chain_signals__mutmut_148 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_149'] = x_get_option_chain_signals__mutmut_149 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_150'] = x_get_option_chain_signals__mutmut_150 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_151'] = x_get_option_chain_signals__mutmut_151 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_152'] = x_get_option_chain_signals__mutmut_152 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_153'] = x_get_option_chain_signals__mutmut_153 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_154'] = x_get_option_chain_signals__mutmut_154 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_155'] = x_get_option_chain_signals__mutmut_155 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_156'] = x_get_option_chain_signals__mutmut_156 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_157'] = x_get_option_chain_signals__mutmut_157 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_158'] = x_get_option_chain_signals__mutmut_158 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_159'] = x_get_option_chain_signals__mutmut_159 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_160'] = x_get_option_chain_signals__mutmut_160 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_161'] = x_get_option_chain_signals__mutmut_161 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_162'] = x_get_option_chain_signals__mutmut_162 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_163'] = x_get_option_chain_signals__mutmut_163 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_164'] = x_get_option_chain_signals__mutmut_164 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_165'] = x_get_option_chain_signals__mutmut_165 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_166'] = x_get_option_chain_signals__mutmut_166 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_167'] = x_get_option_chain_signals__mutmut_167 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_168'] = x_get_option_chain_signals__mutmut_168 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_169'] = x_get_option_chain_signals__mutmut_169 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_170'] = x_get_option_chain_signals__mutmut_170 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_171'] = x_get_option_chain_signals__mutmut_171 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_172'] = x_get_option_chain_signals__mutmut_172 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_173'] = x_get_option_chain_signals__mutmut_173 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_174'] = x_get_option_chain_signals__mutmut_174 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_175'] = x_get_option_chain_signals__mutmut_175 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_176'] = x_get_option_chain_signals__mutmut_176 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_177'] = x_get_option_chain_signals__mutmut_177 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_178'] = x_get_option_chain_signals__mutmut_178 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_179'] = x_get_option_chain_signals__mutmut_179 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_180'] = x_get_option_chain_signals__mutmut_180 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_181'] = x_get_option_chain_signals__mutmut_181 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_182'] = x_get_option_chain_signals__mutmut_182 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_183'] = x_get_option_chain_signals__mutmut_183 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_184'] = x_get_option_chain_signals__mutmut_184 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_185'] = x_get_option_chain_signals__mutmut_185 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_186'] = x_get_option_chain_signals__mutmut_186 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_187'] = x_get_option_chain_signals__mutmut_187 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_188'] = x_get_option_chain_signals__mutmut_188 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_189'] = x_get_option_chain_signals__mutmut_189 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_190'] = x_get_option_chain_signals__mutmut_190 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_191'] = x_get_option_chain_signals__mutmut_191 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_192'] = x_get_option_chain_signals__mutmut_192 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_193'] = x_get_option_chain_signals__mutmut_193 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_194'] = x_get_option_chain_signals__mutmut_194 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_195'] = x_get_option_chain_signals__mutmut_195 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_196'] = x_get_option_chain_signals__mutmut_196 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_197'] = x_get_option_chain_signals__mutmut_197 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_198'] = x_get_option_chain_signals__mutmut_198 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_199'] = x_get_option_chain_signals__mutmut_199 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_200'] = x_get_option_chain_signals__mutmut_200 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_201'] = x_get_option_chain_signals__mutmut_201 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_202'] = x_get_option_chain_signals__mutmut_202 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_203'] = x_get_option_chain_signals__mutmut_203 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_204'] = x_get_option_chain_signals__mutmut_204 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_205'] = x_get_option_chain_signals__mutmut_205 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_206'] = x_get_option_chain_signals__mutmut_206 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_207'] = x_get_option_chain_signals__mutmut_207 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_208'] = x_get_option_chain_signals__mutmut_208 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_209'] = x_get_option_chain_signals__mutmut_209 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_210'] = x_get_option_chain_signals__mutmut_210 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_211'] = x_get_option_chain_signals__mutmut_211 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_212'] = x_get_option_chain_signals__mutmut_212 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_213'] = x_get_option_chain_signals__mutmut_213 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_214'] = x_get_option_chain_signals__mutmut_214 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_215'] = x_get_option_chain_signals__mutmut_215 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_216'] = x_get_option_chain_signals__mutmut_216 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_217'] = x_get_option_chain_signals__mutmut_217 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_218'] = x_get_option_chain_signals__mutmut_218 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_219'] = x_get_option_chain_signals__mutmut_219 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_220'] = x_get_option_chain_signals__mutmut_220 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_221'] = x_get_option_chain_signals__mutmut_221 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_222'] = x_get_option_chain_signals__mutmut_222 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_223'] = x_get_option_chain_signals__mutmut_223 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_224'] = x_get_option_chain_signals__mutmut_224 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_225'] = x_get_option_chain_signals__mutmut_225 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_226'] = x_get_option_chain_signals__mutmut_226 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_227'] = x_get_option_chain_signals__mutmut_227 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_228'] = x_get_option_chain_signals__mutmut_228 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_229'] = x_get_option_chain_signals__mutmut_229 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_230'] = x_get_option_chain_signals__mutmut_230 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_231'] = x_get_option_chain_signals__mutmut_231 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_232'] = x_get_option_chain_signals__mutmut_232 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_233'] = x_get_option_chain_signals__mutmut_233 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_234'] = x_get_option_chain_signals__mutmut_234 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_235'] = x_get_option_chain_signals__mutmut_235 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_236'] = x_get_option_chain_signals__mutmut_236 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_237'] = x_get_option_chain_signals__mutmut_237 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_238'] = x_get_option_chain_signals__mutmut_238 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_239'] = x_get_option_chain_signals__mutmut_239 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_240'] = x_get_option_chain_signals__mutmut_240 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_241'] = x_get_option_chain_signals__mutmut_241 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_242'] = x_get_option_chain_signals__mutmut_242 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_243'] = x_get_option_chain_signals__mutmut_243 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_244'] = x_get_option_chain_signals__mutmut_244 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_245'] = x_get_option_chain_signals__mutmut_245 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_246'] = x_get_option_chain_signals__mutmut_246 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_247'] = x_get_option_chain_signals__mutmut_247 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_248'] = x_get_option_chain_signals__mutmut_248 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_249'] = x_get_option_chain_signals__mutmut_249 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_250'] = x_get_option_chain_signals__mutmut_250 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_251'] = x_get_option_chain_signals__mutmut_251 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_252'] = x_get_option_chain_signals__mutmut_252 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_253'] = x_get_option_chain_signals__mutmut_253 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_254'] = x_get_option_chain_signals__mutmut_254 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_255'] = x_get_option_chain_signals__mutmut_255 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_256'] = x_get_option_chain_signals__mutmut_256 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_257'] = x_get_option_chain_signals__mutmut_257 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_258'] = x_get_option_chain_signals__mutmut_258 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_259'] = x_get_option_chain_signals__mutmut_259 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_260'] = x_get_option_chain_signals__mutmut_260 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_261'] = x_get_option_chain_signals__mutmut_261 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_262'] = x_get_option_chain_signals__mutmut_262 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_263'] = x_get_option_chain_signals__mutmut_263 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_264'] = x_get_option_chain_signals__mutmut_264 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_265'] = x_get_option_chain_signals__mutmut_265 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_266'] = x_get_option_chain_signals__mutmut_266 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_267'] = x_get_option_chain_signals__mutmut_267 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_268'] = x_get_option_chain_signals__mutmut_268 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_269'] = x_get_option_chain_signals__mutmut_269 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_270'] = x_get_option_chain_signals__mutmut_270 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_271'] = x_get_option_chain_signals__mutmut_271 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_272'] = x_get_option_chain_signals__mutmut_272 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_273'] = x_get_option_chain_signals__mutmut_273 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_274'] = x_get_option_chain_signals__mutmut_274 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_275'] = x_get_option_chain_signals__mutmut_275 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_276'] = x_get_option_chain_signals__mutmut_276 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_277'] = x_get_option_chain_signals__mutmut_277 # type: ignore # mutmut generated
mutants_x_get_option_chain_signals__mutmut['x_get_option_chain_signals__mutmut_278'] = x_get_option_chain_signals__mutmut_278 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_score_option_chain__mutmut)
def score_option_chain(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_orig(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_1(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_2(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 1.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_3(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = None
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_4(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 1.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_5(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = None
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_6(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = None  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_7(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get(None, 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_8(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", None)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_9(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get(0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_10(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", )  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_11(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("XXnet_gexXX", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_12(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("NET_GEX", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_13(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 1.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_14(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = None
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_15(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get(None)
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_16(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("XXskew_25dXX")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_17(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("SKEW_25D")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_18(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = None

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_19(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get(None)

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_20(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("XXmax_painXX")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_21(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("MAX_PAIN")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_22(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex >= 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_23(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 3.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_24(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score = 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_25(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_26(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 4 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_27(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint != "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_28(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "XXBUYXX" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_29(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "buy" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_30(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 1
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_31(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            None
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_32(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "XXsrcXX": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_33(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "SRC": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_34(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "XXOptionsXX",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_35(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_36(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "OPTIONS",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_37(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "XXheadXX": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_38(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "HEAD": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_39(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "XXbodyXX": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_40(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "BODY": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_41(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "XXDealers are net long gamma. They will SELL into rallies and BUY dips, XX"
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_42(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "dealers are net long gamma. they will sell into rallies and buy dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_43(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "DEALERS ARE NET LONG GAMMA. THEY WILL SELL INTO RALLIES AND BUY DIPS, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_44(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "XXcompressing volatility and pinning price near current levels. XX"
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_45(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "COMPRESSING VOLATILITY AND PINNING PRICE NEAR CURRENT LEVELS. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_46(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "XXBreakouts are more likely to fail in this regime.XX"
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_47(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_48(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "BREAKOUTS ARE MORE LIKELY TO FAIL IN THIS REGIME."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_49(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "XXsentimentXX": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_50(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "SENTIMENT": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_51(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "XXnegXX",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_52(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "NEG",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_53(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "XXmetaXX": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_54(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "META": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_55(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex <= -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_56(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < +2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_57(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -3.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_58(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score = 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_59(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_60(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 4 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_61(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint != "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_62(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "XXBUYXX" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_63(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "buy" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_64(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 1
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_65(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            None
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_66(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "XXsrcXX": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_67(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "SRC": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_68(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "XXOptionsXX",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_69(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_70(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "OPTIONS",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_71(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "XXheadXX": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_72(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "HEAD": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_73(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(None):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_74(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "XXbodyXX": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_75(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "BODY": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_76(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "XXDealers are net short gamma. They must BUY into rallies and SELL into dips XX"
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_77(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "dealers are net short gamma. they must buy into rallies and sell into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_78(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "DEALERS ARE NET SHORT GAMMA. THEY MUST BUY INTO RALLIES AND SELL INTO DIPS "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_79(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "XXto stay hedged, amplifying directional moves. Breakouts are more likely to sustain.XX"
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_80(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_81(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "TO STAY HEDGED, AMPLIFYING DIRECTIONAL MOVES. BREAKOUTS ARE MORE LIKELY TO SUSTAIN."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_82(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "XXsentimentXX": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_83(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "SENTIMENT": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_84(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "XXposXX",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_85(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "POS",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_86(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "XXmetaXX": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_87(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "META": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_88(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_89(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew >= 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_90(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 9.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_91(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score = 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_92(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score -= 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_93(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 5
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_94(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                None
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_95(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_96(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "SRC": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_97(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_98(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_99(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_100(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_101(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_102(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "XXbodyXX": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_103(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "BODY": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_104(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "XX25-delta put IV significantly exceeds call IV. Extreme put skew signals XX"
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_105(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put iv significantly exceeds call iv. extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_106(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-DELTA PUT IV SIGNIFICANTLY EXCEEDS CALL IV. EXTREME PUT SKEW SIGNALS "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_107(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "XXcrowded hedging/fear — historically a contrarian bullish setup at this magnitude.XX"
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_108(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "CROWDED HEDGING/FEAR — HISTORICALLY A CONTRARIAN BULLISH SETUP AT THIS MAGNITUDE."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_109(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "XXsentimentXX": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_110(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "SENTIMENT": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_111(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "XXposXX",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_112(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "POS",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_113(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "XXmetaXX": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_114(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "META": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_115(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew <= -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_116(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < +4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_117(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -5.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_118(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score = 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_119(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score += 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_120(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_121(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                None
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_122(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_123(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "SRC": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_124(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_125(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_126(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_127(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_128(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_129(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "XXbodyXX": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_130(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "BODY": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_131(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "XX25-delta call IV exceeds put IV — unusual. Retail speculation in calls XX"
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_132(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call iv exceeds put iv — unusual. retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_133(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-DELTA CALL IV EXCEEDS PUT IV — UNUSUAL. RETAIL SPECULATION IN CALLS "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_134(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "XXdrives this inversion, which has historically preceded short-term pullbacks.XX"
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_135(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "DRIVES THIS INVERSION, WHICH HAS HISTORICALLY PRECEDED SHORT-TERM PULLBACKS."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_136(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "XXsentimentXX": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_137(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "SENTIMENT": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_138(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "XXnegXX",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_139(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "NEG",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_140(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "XXmetaXX": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_141(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "META": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_142(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain or current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_143(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = None
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_144(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price / 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_145(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) * current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_146(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain + current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_147(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 101
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_148(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(None) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_149(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) >= 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_150(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 4:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_151(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = None
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_152(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "XXupwardXX" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_153(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "UPWARD" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_154(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct >= 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_155(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 1 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_156(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "XXdownwardXX"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_157(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "DOWNWARD"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_158(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                None
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_159(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "XXsrcXX": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_160(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "SRC": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_161(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "XXOptionsXX",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_162(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_163(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "OPTIONS",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_164(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "XXheadXX": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_165(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "HEAD": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_166(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "XXbodyXX": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_167(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "BODY": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_168(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "XXsentimentXX": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_169(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "SENTIMENT": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_170(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "XXposXX" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_171(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "POS" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_172(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct >= 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_173(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 1 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_174(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "XXnegXX",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_175(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "NEG",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_176(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "XXmetaXX": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_177(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "META": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 1), rationale


def x_score_option_chain__mutmut_178(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(None, 1), rationale


def x_score_option_chain__mutmut_179(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, None), rationale


def x_score_option_chain__mutmut_180(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(1), rationale


def x_score_option_chain__mutmut_181(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, ), rationale


def x_score_option_chain__mutmut_182(signals: dict, current_price: float, action_hint: str) -> tuple[float, list[dict]]:
    """
    Convert full chain signals into score pts + rationale items.
    Used by signal_engine after existing options block.
    """
    if not signals:
        return 0.0, []

    score = 0.0
    rationale = []
    net_gex = signals.get("net_gex", 0.0)  # already in $M
    skew = signals.get("skew_25d")
    max_pain = signals.get("max_pain")

    # GEX: positive = dealers long gamma = they absorb moves = compression/reversal
    #       negative = dealers short gamma = they amplify moves = volatility/trend
    if net_gex > 2.0:
        score -= 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX +${net_gex:.0f}M — Dealer Long Gamma (Pinning Risk)",
                "body": (
                    "Dealers are net long gamma. They will SELL into rallies and BUY dips, "
                    "compressing volatility and pinning price near current levels. "
                    "Breakouts are more likely to fail in this regime."
                ),
                "sentiment": "neg",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )
    elif net_gex < -2.0:
        score += 3 if action_hint == "BUY" else 0
        rationale.append(
            {
                "src": "Options",
                "head": f"GEX −${abs(net_gex):.0f}M — Dealer Short Gamma (Momentum Fuel)",
                "body": (
                    "Dealers are net short gamma. They must BUY into rallies and SELL into dips "
                    "to stay hedged, amplifying directional moves. Breakouts are more likely to sustain."
                ),
                "sentiment": "pos",
                "meta": f"net_gex={net_gex:+.1f}M",
            }
        )

    # Skew: elevated put skew (fear) → contrarian BUY signal at extremes
    if skew is not None:
        if skew > 8.0:
            score += 4
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Elevated Put Skew ({skew:.1f}vp) — Contrarian Bullish",
                    "body": (
                        "25-delta put IV significantly exceeds call IV. Extreme put skew signals "
                        "crowded hedging/fear — historically a contrarian bullish setup at this magnitude."
                    ),
                    "sentiment": "pos",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )
        elif skew < -4.0:
            score -= 3
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Inverted Skew ({skew:.1f}vp) — Call-side Speculation",
                    "body": (
                        "25-delta call IV exceeds put IV — unusual. Retail speculation in calls "
                        "drives this inversion, which has historically preceded short-term pullbacks."
                    ),
                    "sentiment": "neg",
                    "meta": f"25d_skew={skew:.1f}vp",
                }
            )

    # Max pain gravitational pull (near-term expiry)
    if max_pain and current_price:
        gap_pct = (max_pain - current_price) / current_price * 100
        if abs(gap_pct) > 3:
            pull_dir = "upward" if gap_pct > 0 else "downward"
            rationale.append(
                {
                    "src": "Options",
                    "head": f"Max Pain ${max_pain:.0f} ({gap_pct:+.1f}% from spot) — {pull_dir.capitalize()} Gravitational Pull",
                    "body": (
                        f"Options market max pain at ${max_pain:.0f}. As expiry approaches, price tends "
                        f"to gravitate {pull_dir} toward this level to maximise option decay."
                    ),
                    "sentiment": "pos" if gap_pct > 0 else "neg",
                    "meta": f"max_pain={max_pain} spot={current_price:.2f}",
                }
            )

    return round(score, 2), rationale

mutants_x_score_option_chain__mutmut['_mutmut_orig'] = x_score_option_chain__mutmut_orig # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_1'] = x_score_option_chain__mutmut_1 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_2'] = x_score_option_chain__mutmut_2 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_3'] = x_score_option_chain__mutmut_3 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_4'] = x_score_option_chain__mutmut_4 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_5'] = x_score_option_chain__mutmut_5 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_6'] = x_score_option_chain__mutmut_6 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_7'] = x_score_option_chain__mutmut_7 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_8'] = x_score_option_chain__mutmut_8 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_9'] = x_score_option_chain__mutmut_9 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_10'] = x_score_option_chain__mutmut_10 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_11'] = x_score_option_chain__mutmut_11 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_12'] = x_score_option_chain__mutmut_12 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_13'] = x_score_option_chain__mutmut_13 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_14'] = x_score_option_chain__mutmut_14 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_15'] = x_score_option_chain__mutmut_15 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_16'] = x_score_option_chain__mutmut_16 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_17'] = x_score_option_chain__mutmut_17 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_18'] = x_score_option_chain__mutmut_18 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_19'] = x_score_option_chain__mutmut_19 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_20'] = x_score_option_chain__mutmut_20 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_21'] = x_score_option_chain__mutmut_21 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_22'] = x_score_option_chain__mutmut_22 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_23'] = x_score_option_chain__mutmut_23 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_24'] = x_score_option_chain__mutmut_24 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_25'] = x_score_option_chain__mutmut_25 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_26'] = x_score_option_chain__mutmut_26 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_27'] = x_score_option_chain__mutmut_27 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_28'] = x_score_option_chain__mutmut_28 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_29'] = x_score_option_chain__mutmut_29 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_30'] = x_score_option_chain__mutmut_30 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_31'] = x_score_option_chain__mutmut_31 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_32'] = x_score_option_chain__mutmut_32 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_33'] = x_score_option_chain__mutmut_33 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_34'] = x_score_option_chain__mutmut_34 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_35'] = x_score_option_chain__mutmut_35 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_36'] = x_score_option_chain__mutmut_36 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_37'] = x_score_option_chain__mutmut_37 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_38'] = x_score_option_chain__mutmut_38 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_39'] = x_score_option_chain__mutmut_39 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_40'] = x_score_option_chain__mutmut_40 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_41'] = x_score_option_chain__mutmut_41 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_42'] = x_score_option_chain__mutmut_42 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_43'] = x_score_option_chain__mutmut_43 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_44'] = x_score_option_chain__mutmut_44 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_45'] = x_score_option_chain__mutmut_45 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_46'] = x_score_option_chain__mutmut_46 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_47'] = x_score_option_chain__mutmut_47 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_48'] = x_score_option_chain__mutmut_48 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_49'] = x_score_option_chain__mutmut_49 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_50'] = x_score_option_chain__mutmut_50 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_51'] = x_score_option_chain__mutmut_51 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_52'] = x_score_option_chain__mutmut_52 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_53'] = x_score_option_chain__mutmut_53 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_54'] = x_score_option_chain__mutmut_54 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_55'] = x_score_option_chain__mutmut_55 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_56'] = x_score_option_chain__mutmut_56 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_57'] = x_score_option_chain__mutmut_57 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_58'] = x_score_option_chain__mutmut_58 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_59'] = x_score_option_chain__mutmut_59 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_60'] = x_score_option_chain__mutmut_60 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_61'] = x_score_option_chain__mutmut_61 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_62'] = x_score_option_chain__mutmut_62 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_63'] = x_score_option_chain__mutmut_63 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_64'] = x_score_option_chain__mutmut_64 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_65'] = x_score_option_chain__mutmut_65 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_66'] = x_score_option_chain__mutmut_66 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_67'] = x_score_option_chain__mutmut_67 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_68'] = x_score_option_chain__mutmut_68 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_69'] = x_score_option_chain__mutmut_69 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_70'] = x_score_option_chain__mutmut_70 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_71'] = x_score_option_chain__mutmut_71 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_72'] = x_score_option_chain__mutmut_72 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_73'] = x_score_option_chain__mutmut_73 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_74'] = x_score_option_chain__mutmut_74 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_75'] = x_score_option_chain__mutmut_75 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_76'] = x_score_option_chain__mutmut_76 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_77'] = x_score_option_chain__mutmut_77 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_78'] = x_score_option_chain__mutmut_78 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_79'] = x_score_option_chain__mutmut_79 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_80'] = x_score_option_chain__mutmut_80 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_81'] = x_score_option_chain__mutmut_81 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_82'] = x_score_option_chain__mutmut_82 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_83'] = x_score_option_chain__mutmut_83 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_84'] = x_score_option_chain__mutmut_84 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_85'] = x_score_option_chain__mutmut_85 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_86'] = x_score_option_chain__mutmut_86 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_87'] = x_score_option_chain__mutmut_87 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_88'] = x_score_option_chain__mutmut_88 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_89'] = x_score_option_chain__mutmut_89 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_90'] = x_score_option_chain__mutmut_90 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_91'] = x_score_option_chain__mutmut_91 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_92'] = x_score_option_chain__mutmut_92 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_93'] = x_score_option_chain__mutmut_93 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_94'] = x_score_option_chain__mutmut_94 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_95'] = x_score_option_chain__mutmut_95 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_96'] = x_score_option_chain__mutmut_96 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_97'] = x_score_option_chain__mutmut_97 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_98'] = x_score_option_chain__mutmut_98 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_99'] = x_score_option_chain__mutmut_99 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_100'] = x_score_option_chain__mutmut_100 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_101'] = x_score_option_chain__mutmut_101 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_102'] = x_score_option_chain__mutmut_102 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_103'] = x_score_option_chain__mutmut_103 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_104'] = x_score_option_chain__mutmut_104 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_105'] = x_score_option_chain__mutmut_105 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_106'] = x_score_option_chain__mutmut_106 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_107'] = x_score_option_chain__mutmut_107 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_108'] = x_score_option_chain__mutmut_108 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_109'] = x_score_option_chain__mutmut_109 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_110'] = x_score_option_chain__mutmut_110 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_111'] = x_score_option_chain__mutmut_111 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_112'] = x_score_option_chain__mutmut_112 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_113'] = x_score_option_chain__mutmut_113 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_114'] = x_score_option_chain__mutmut_114 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_115'] = x_score_option_chain__mutmut_115 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_116'] = x_score_option_chain__mutmut_116 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_117'] = x_score_option_chain__mutmut_117 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_118'] = x_score_option_chain__mutmut_118 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_119'] = x_score_option_chain__mutmut_119 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_120'] = x_score_option_chain__mutmut_120 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_121'] = x_score_option_chain__mutmut_121 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_122'] = x_score_option_chain__mutmut_122 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_123'] = x_score_option_chain__mutmut_123 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_124'] = x_score_option_chain__mutmut_124 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_125'] = x_score_option_chain__mutmut_125 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_126'] = x_score_option_chain__mutmut_126 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_127'] = x_score_option_chain__mutmut_127 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_128'] = x_score_option_chain__mutmut_128 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_129'] = x_score_option_chain__mutmut_129 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_130'] = x_score_option_chain__mutmut_130 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_131'] = x_score_option_chain__mutmut_131 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_132'] = x_score_option_chain__mutmut_132 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_133'] = x_score_option_chain__mutmut_133 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_134'] = x_score_option_chain__mutmut_134 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_135'] = x_score_option_chain__mutmut_135 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_136'] = x_score_option_chain__mutmut_136 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_137'] = x_score_option_chain__mutmut_137 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_138'] = x_score_option_chain__mutmut_138 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_139'] = x_score_option_chain__mutmut_139 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_140'] = x_score_option_chain__mutmut_140 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_141'] = x_score_option_chain__mutmut_141 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_142'] = x_score_option_chain__mutmut_142 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_143'] = x_score_option_chain__mutmut_143 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_144'] = x_score_option_chain__mutmut_144 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_145'] = x_score_option_chain__mutmut_145 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_146'] = x_score_option_chain__mutmut_146 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_147'] = x_score_option_chain__mutmut_147 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_148'] = x_score_option_chain__mutmut_148 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_149'] = x_score_option_chain__mutmut_149 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_150'] = x_score_option_chain__mutmut_150 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_151'] = x_score_option_chain__mutmut_151 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_152'] = x_score_option_chain__mutmut_152 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_153'] = x_score_option_chain__mutmut_153 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_154'] = x_score_option_chain__mutmut_154 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_155'] = x_score_option_chain__mutmut_155 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_156'] = x_score_option_chain__mutmut_156 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_157'] = x_score_option_chain__mutmut_157 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_158'] = x_score_option_chain__mutmut_158 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_159'] = x_score_option_chain__mutmut_159 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_160'] = x_score_option_chain__mutmut_160 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_161'] = x_score_option_chain__mutmut_161 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_162'] = x_score_option_chain__mutmut_162 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_163'] = x_score_option_chain__mutmut_163 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_164'] = x_score_option_chain__mutmut_164 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_165'] = x_score_option_chain__mutmut_165 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_166'] = x_score_option_chain__mutmut_166 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_167'] = x_score_option_chain__mutmut_167 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_168'] = x_score_option_chain__mutmut_168 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_169'] = x_score_option_chain__mutmut_169 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_170'] = x_score_option_chain__mutmut_170 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_171'] = x_score_option_chain__mutmut_171 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_172'] = x_score_option_chain__mutmut_172 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_173'] = x_score_option_chain__mutmut_173 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_174'] = x_score_option_chain__mutmut_174 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_175'] = x_score_option_chain__mutmut_175 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_176'] = x_score_option_chain__mutmut_176 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_177'] = x_score_option_chain__mutmut_177 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_178'] = x_score_option_chain__mutmut_178 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_179'] = x_score_option_chain__mutmut_179 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_180'] = x_score_option_chain__mutmut_180 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_181'] = x_score_option_chain__mutmut_181 # type: ignore # mutmut generated
mutants_x_score_option_chain__mutmut['x_score_option_chain__mutmut_182'] = x_score_option_chain__mutmut_182 # type: ignore # mutmut generated
