"""
Local LLM Inference for Sentiment Analysis and Macro Risk-Gating.

This module provides a local LLM abstraction for offloading sentiment analysis
and macro risk-gating to a local model (e.g., Llama 3.1, Mistral, Phi-3).
This keeps trading logic private and avoids API costs for processing news feeds.

Supports Ollama, LM Studio, or any OpenAI-compatible local server.
"""

import logging
import re
import time
from dataclasses import dataclass
from typing import Optional
from services.http_client import get_ssl_context

logger = logging.getLogger(__name__)

# Configuration
LOCAL_LLM_ENABLED = True  # Set to False to disable local LLM
LOCAL_LLM_PROVIDER = "ollama"  # Options: "ollama", "lm-studio", "openai-compatible"
LOCAL_LLM_MODEL = "llama3.1:8b"  # Default model for Ollama
LOCAL_LLM_URL = "http://localhost:11434"  # Ollama default
LOCAL_LLM_API_KEY = None  # For OpenAI-compatible servers

# Fallback to rule-based analysis if LLM unavailable
_fallback_mode = False


from mutmut.mutation.trampoline import wrap_in_trampoline as _mutmut_mutated, MutantDict


@dataclass
class SentimentResult:
    """Result of sentiment analysis."""

    score: float  # -1.0 (very bearish) to +1.0 (very bullish)
    confidence: float  # 0.0 to 1.0
    label: str  # "bullish", "bearish", "neutral"
    reasoning: str  # Brief explanation
    source: str  # "llm" or "fallback"


@dataclass
class RiskGateResult:
    """Result of macro risk gating analysis."""

    risk_level: str  # "low", "medium", "high", "extreme"
    confidence: float  # 0.0 to 1.0
    factors: list[str]  # Key risk factors identified
    recommendation: str  # "proceed", "caution", "reduce_size", "avoid"
    source: str  # "llm" or "fallback"
mutants_x__parse_sentiment_score__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__parse_sentiment_score__mutmut)
def _parse_sentiment_score(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_orig(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_1(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = None
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_2(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(None, text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_3(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", None)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_4(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_5(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", )
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_6(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"XX[-+]?\d*\.\d+|[-+]?\d+XX", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_7(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = None
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_8(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(None)
        return max(-1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_9(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(None, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_10(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, None)
    return 0.0


def x__parse_sentiment_score__mutmut_11(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_12(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, )
    return 0.0


def x__parse_sentiment_score__mutmut_13(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(+1.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_14(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-2.0, min(1.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_15(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(None, score))
    return 0.0


def x__parse_sentiment_score__mutmut_16(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, None))
    return 0.0


def x__parse_sentiment_score__mutmut_17(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(score))
    return 0.0


def x__parse_sentiment_score__mutmut_18(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, ))
    return 0.0


def x__parse_sentiment_score__mutmut_19(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(2.0, score))
    return 0.0


def x__parse_sentiment_score__mutmut_20(text: str) -> float:
    """Parse a sentiment score from LLM response text."""
    # Look for a number between -1 and 1
    match = re.search(r"[-+]?\d*\.\d+|[-+]?\d+", text)
    if match:
        score = float(match.group())
        return max(-1.0, min(1.0, score))
    return 1.0

mutants_x__parse_sentiment_score__mutmut['_mutmut_orig'] = x__parse_sentiment_score__mutmut_orig # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_1'] = x__parse_sentiment_score__mutmut_1 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_2'] = x__parse_sentiment_score__mutmut_2 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_3'] = x__parse_sentiment_score__mutmut_3 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_4'] = x__parse_sentiment_score__mutmut_4 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_5'] = x__parse_sentiment_score__mutmut_5 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_6'] = x__parse_sentiment_score__mutmut_6 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_7'] = x__parse_sentiment_score__mutmut_7 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_8'] = x__parse_sentiment_score__mutmut_8 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_9'] = x__parse_sentiment_score__mutmut_9 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_10'] = x__parse_sentiment_score__mutmut_10 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_11'] = x__parse_sentiment_score__mutmut_11 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_12'] = x__parse_sentiment_score__mutmut_12 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_13'] = x__parse_sentiment_score__mutmut_13 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_14'] = x__parse_sentiment_score__mutmut_14 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_15'] = x__parse_sentiment_score__mutmut_15 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_16'] = x__parse_sentiment_score__mutmut_16 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_17'] = x__parse_sentiment_score__mutmut_17 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_18'] = x__parse_sentiment_score__mutmut_18 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_19'] = x__parse_sentiment_score__mutmut_19 # type: ignore # mutmut generated
mutants_x__parse_sentiment_score__mutmut['x__parse_sentiment_score__mutmut_20'] = x__parse_sentiment_score__mutmut_20 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__analyze_sentiment_fallback__mutmut)
def _analyze_sentiment_fallback(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_orig(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_1(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_2(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=None,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_3(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=None,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_4(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label=None,
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_5(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning=None,
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_6(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source=None,
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_7(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_8(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_9(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_10(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_11(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_12(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=1.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_13(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=1.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_14(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="XXneutralXX",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_15(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="NEUTRAL",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_16(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="XXNo news items to analyzeXX",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_17(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="no news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_18(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="NO NEWS ITEMS TO ANALYZE",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_19(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="XXfallbackXX",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_20(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="FALLBACK",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_21(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = None
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_22(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "XXbeatXX",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_23(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "BEAT",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_24(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "XXupgradeXX",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_25(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "UPGRADE",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_26(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "XXbuyXX",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_27(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "BUY",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_28(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "XXgrowthXX",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_29(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "GROWTH",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_30(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "XXprofitXX",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_31(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "PROFIT",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_32(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "XXrevenueXX",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_33(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "REVENUE",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_34(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "XXpositiveXX",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_35(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "POSITIVE",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_36(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "XXexceedXX",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_37(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "EXCEED",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_38(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "XXoutperformXX",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_39(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "OUTPERFORM",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_40(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "XXbullishXX",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_41(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "BULLISH",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_42(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "XXrallyXX",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_43(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "RALLY",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_44(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "XXsurgeXX",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_45(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "SURGE",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_46(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "XXgainXX",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_47(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "GAIN",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_48(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "XXriseXX",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_49(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "RISE",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_50(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "XXstrongXX",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_51(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "STRONG",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_52(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "XXrecordXX",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_53(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "RECORD",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_54(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "XXbreakthroughXX",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_55(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "BREAKTHROUGH",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_56(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "XXapprovalXX",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_57(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "APPROVAL",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_58(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "XXpartnershipXX",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_59(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "PARTNERSHIP",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_60(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = None

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_61(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "XXmissXX",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_62(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "MISS",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_63(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "XXdowngradeXX",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_64(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "DOWNGRADE",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_65(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "XXsellXX",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_66(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "SELL",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_67(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "XXlossXX",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_68(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "LOSS",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_69(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "XXdeclineXX",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_70(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "DECLINE",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_71(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "XXnegativeXX",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_72(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "NEGATIVE",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_73(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "XXunderperformXX",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_74(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "UNDERPERFORM",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_75(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "XXbearishXX",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_76(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "BEARISH",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_77(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "XXdropXX",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_78(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "DROP",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_79(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "XXplungeXX",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_80(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "PLUNGE",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_81(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "XXfallXX",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_82(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "FALL",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_83(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "XXweakXX",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_84(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "WEAK",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_85(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "XXlawsuitXX",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_86(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "LAWSUIT",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_87(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "XXinvestigationXX",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_88(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "INVESTIGATION",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_89(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "XXrecallXX",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_90(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "RECALL",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_91(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "XXwarningXX",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_92(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "WARNING",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_93(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "XXdelayXX",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_94(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "DELAY",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_95(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = None
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_96(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 1.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_97(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = None

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_98(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 1.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_99(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = None
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_100(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").upper()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_101(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") and "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_102(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get(None) or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_103(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("XXheadlineXX") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_104(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("HEADLINE") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_105(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "XXXX").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_106(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = None
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_107(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").upper()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_108(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") and "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_109(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get(None) or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_110(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("XXsummaryXX") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_111(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("SUMMARY") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_112(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "XXXX").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_113(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = None

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_114(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = None
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_115(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(None)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_116(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(2 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_117(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw not in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_118(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = None
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_119(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(None)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_120(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(2 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_121(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw not in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_122(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = None

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_123(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words >= 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_124(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 1:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_125(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = None
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_126(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) / 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_127(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) * max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_128(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count + bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_129(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(None, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_130(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, None) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_131(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_132(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, ) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_133(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(2, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_134(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 11
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_135(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = None
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_136(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(None, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_137(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, None)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_138(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min((bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_139(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, )
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_140(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(1.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_141(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) / 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_142(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) * max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_143(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count - bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_144(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(None, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_145(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, None) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_146(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_147(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, ) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_148(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(2, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_149(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 6)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_150(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score = item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_151(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score -= item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_152(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence = item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_153(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence -= item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_154(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = None
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_155(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score * len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_156(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 1
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_157(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = None

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_158(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence * len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_159(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 1

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_160(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = None
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_161(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(None, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_162(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, None)
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_163(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_164(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, )
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_165(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(+1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_166(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-2.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_167(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(None, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_168(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, None))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_169(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_170(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, ))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_171(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(2.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_172(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = None

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_173(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "XXbullishXX" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_174(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "BULLISH" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_175(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score >= 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_176(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 1.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_177(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "XXbearishXX" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_178(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "BEARISH" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_179(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score <= -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_180(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < +0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_181(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -1.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_182(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "XXneutralXX"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_183(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "NEUTRAL"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_184(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=None,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_185(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=None,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_186(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=None,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_187(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=None,
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_188(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source=None,
    )


def x__analyze_sentiment_fallback__mutmut_189(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_190(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_191(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_192(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        source="fallback",
    )


def x__analyze_sentiment_fallback__mutmut_193(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        )


def x__analyze_sentiment_fallback__mutmut_194(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="XXfallbackXX",
    )


def x__analyze_sentiment_fallback__mutmut_195(news_items: list[dict]) -> SentimentResult:
    """Fallback rule-based sentiment analysis when LLM is unavailable."""
    if not news_items:
        return SentimentResult(
            score=0.0,
            confidence=0.3,
            label="neutral",
            reasoning="No news items to analyze",
            source="fallback",
        )

    # Simple keyword-based sentiment
    bullish_keywords = [
        "beat",
        "upgrade",
        "buy",
        "growth",
        "profit",
        "revenue",
        "positive",
        "exceed",
        "outperform",
        "bullish",
        "rally",
        "surge",
        "gain",
        "rise",
        "strong",
        "record",
        "breakthrough",
        "approval",
        "partnership",
    ]
    bearish_keywords = [
        "miss",
        "downgrade",
        "sell",
        "loss",
        "decline",
        "negative",
        "underperform",
        "bearish",
        "drop",
        "plunge",
        "fall",
        "weak",
        "lawsuit",
        "investigation",
        "recall",
        "warning",
        "delay",
    ]

    total_score = 0.0
    total_confidence = 0.0

    for item in news_items:
        headline = (item.get("headline") or "").lower()
        summary = (item.get("summary") or "").lower()
        text = f"{headline} {summary}"

        bull_count = sum(1 for kw in bullish_keywords if kw in text)
        bear_count = sum(1 for kw in bearish_keywords if kw in text)
        total_words = len(text.split())

        if total_words > 0:
            item_score = (bull_count - bear_count) / max(1, total_words) * 10
            item_confidence = min(0.8, (bull_count + bear_count) / max(1, total_words) * 5)
            total_score += item_score
            total_confidence += item_confidence

    avg_score = total_score / len(news_items) if news_items else 0
    avg_confidence = total_confidence / len(news_items) if news_items else 0

    score = max(-1.0, min(1.0, avg_score))
    label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"

    return SentimentResult(
        score=score,
        confidence=avg_confidence,
        label=label,
        reasoning=f"Keyword analysis: {len(news_items)} news items processed",
        source="FALLBACK",
    )

mutants_x__analyze_sentiment_fallback__mutmut['_mutmut_orig'] = x__analyze_sentiment_fallback__mutmut_orig # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_1'] = x__analyze_sentiment_fallback__mutmut_1 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_2'] = x__analyze_sentiment_fallback__mutmut_2 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_3'] = x__analyze_sentiment_fallback__mutmut_3 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_4'] = x__analyze_sentiment_fallback__mutmut_4 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_5'] = x__analyze_sentiment_fallback__mutmut_5 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_6'] = x__analyze_sentiment_fallback__mutmut_6 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_7'] = x__analyze_sentiment_fallback__mutmut_7 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_8'] = x__analyze_sentiment_fallback__mutmut_8 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_9'] = x__analyze_sentiment_fallback__mutmut_9 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_10'] = x__analyze_sentiment_fallback__mutmut_10 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_11'] = x__analyze_sentiment_fallback__mutmut_11 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_12'] = x__analyze_sentiment_fallback__mutmut_12 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_13'] = x__analyze_sentiment_fallback__mutmut_13 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_14'] = x__analyze_sentiment_fallback__mutmut_14 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_15'] = x__analyze_sentiment_fallback__mutmut_15 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_16'] = x__analyze_sentiment_fallback__mutmut_16 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_17'] = x__analyze_sentiment_fallback__mutmut_17 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_18'] = x__analyze_sentiment_fallback__mutmut_18 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_19'] = x__analyze_sentiment_fallback__mutmut_19 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_20'] = x__analyze_sentiment_fallback__mutmut_20 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_21'] = x__analyze_sentiment_fallback__mutmut_21 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_22'] = x__analyze_sentiment_fallback__mutmut_22 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_23'] = x__analyze_sentiment_fallback__mutmut_23 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_24'] = x__analyze_sentiment_fallback__mutmut_24 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_25'] = x__analyze_sentiment_fallback__mutmut_25 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_26'] = x__analyze_sentiment_fallback__mutmut_26 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_27'] = x__analyze_sentiment_fallback__mutmut_27 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_28'] = x__analyze_sentiment_fallback__mutmut_28 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_29'] = x__analyze_sentiment_fallback__mutmut_29 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_30'] = x__analyze_sentiment_fallback__mutmut_30 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_31'] = x__analyze_sentiment_fallback__mutmut_31 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_32'] = x__analyze_sentiment_fallback__mutmut_32 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_33'] = x__analyze_sentiment_fallback__mutmut_33 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_34'] = x__analyze_sentiment_fallback__mutmut_34 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_35'] = x__analyze_sentiment_fallback__mutmut_35 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_36'] = x__analyze_sentiment_fallback__mutmut_36 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_37'] = x__analyze_sentiment_fallback__mutmut_37 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_38'] = x__analyze_sentiment_fallback__mutmut_38 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_39'] = x__analyze_sentiment_fallback__mutmut_39 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_40'] = x__analyze_sentiment_fallback__mutmut_40 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_41'] = x__analyze_sentiment_fallback__mutmut_41 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_42'] = x__analyze_sentiment_fallback__mutmut_42 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_43'] = x__analyze_sentiment_fallback__mutmut_43 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_44'] = x__analyze_sentiment_fallback__mutmut_44 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_45'] = x__analyze_sentiment_fallback__mutmut_45 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_46'] = x__analyze_sentiment_fallback__mutmut_46 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_47'] = x__analyze_sentiment_fallback__mutmut_47 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_48'] = x__analyze_sentiment_fallback__mutmut_48 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_49'] = x__analyze_sentiment_fallback__mutmut_49 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_50'] = x__analyze_sentiment_fallback__mutmut_50 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_51'] = x__analyze_sentiment_fallback__mutmut_51 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_52'] = x__analyze_sentiment_fallback__mutmut_52 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_53'] = x__analyze_sentiment_fallback__mutmut_53 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_54'] = x__analyze_sentiment_fallback__mutmut_54 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_55'] = x__analyze_sentiment_fallback__mutmut_55 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_56'] = x__analyze_sentiment_fallback__mutmut_56 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_57'] = x__analyze_sentiment_fallback__mutmut_57 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_58'] = x__analyze_sentiment_fallback__mutmut_58 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_59'] = x__analyze_sentiment_fallback__mutmut_59 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_60'] = x__analyze_sentiment_fallback__mutmut_60 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_61'] = x__analyze_sentiment_fallback__mutmut_61 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_62'] = x__analyze_sentiment_fallback__mutmut_62 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_63'] = x__analyze_sentiment_fallback__mutmut_63 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_64'] = x__analyze_sentiment_fallback__mutmut_64 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_65'] = x__analyze_sentiment_fallback__mutmut_65 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_66'] = x__analyze_sentiment_fallback__mutmut_66 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_67'] = x__analyze_sentiment_fallback__mutmut_67 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_68'] = x__analyze_sentiment_fallback__mutmut_68 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_69'] = x__analyze_sentiment_fallback__mutmut_69 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_70'] = x__analyze_sentiment_fallback__mutmut_70 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_71'] = x__analyze_sentiment_fallback__mutmut_71 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_72'] = x__analyze_sentiment_fallback__mutmut_72 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_73'] = x__analyze_sentiment_fallback__mutmut_73 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_74'] = x__analyze_sentiment_fallback__mutmut_74 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_75'] = x__analyze_sentiment_fallback__mutmut_75 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_76'] = x__analyze_sentiment_fallback__mutmut_76 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_77'] = x__analyze_sentiment_fallback__mutmut_77 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_78'] = x__analyze_sentiment_fallback__mutmut_78 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_79'] = x__analyze_sentiment_fallback__mutmut_79 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_80'] = x__analyze_sentiment_fallback__mutmut_80 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_81'] = x__analyze_sentiment_fallback__mutmut_81 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_82'] = x__analyze_sentiment_fallback__mutmut_82 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_83'] = x__analyze_sentiment_fallback__mutmut_83 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_84'] = x__analyze_sentiment_fallback__mutmut_84 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_85'] = x__analyze_sentiment_fallback__mutmut_85 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_86'] = x__analyze_sentiment_fallback__mutmut_86 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_87'] = x__analyze_sentiment_fallback__mutmut_87 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_88'] = x__analyze_sentiment_fallback__mutmut_88 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_89'] = x__analyze_sentiment_fallback__mutmut_89 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_90'] = x__analyze_sentiment_fallback__mutmut_90 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_91'] = x__analyze_sentiment_fallback__mutmut_91 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_92'] = x__analyze_sentiment_fallback__mutmut_92 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_93'] = x__analyze_sentiment_fallback__mutmut_93 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_94'] = x__analyze_sentiment_fallback__mutmut_94 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_95'] = x__analyze_sentiment_fallback__mutmut_95 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_96'] = x__analyze_sentiment_fallback__mutmut_96 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_97'] = x__analyze_sentiment_fallback__mutmut_97 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_98'] = x__analyze_sentiment_fallback__mutmut_98 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_99'] = x__analyze_sentiment_fallback__mutmut_99 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_100'] = x__analyze_sentiment_fallback__mutmut_100 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_101'] = x__analyze_sentiment_fallback__mutmut_101 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_102'] = x__analyze_sentiment_fallback__mutmut_102 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_103'] = x__analyze_sentiment_fallback__mutmut_103 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_104'] = x__analyze_sentiment_fallback__mutmut_104 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_105'] = x__analyze_sentiment_fallback__mutmut_105 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_106'] = x__analyze_sentiment_fallback__mutmut_106 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_107'] = x__analyze_sentiment_fallback__mutmut_107 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_108'] = x__analyze_sentiment_fallback__mutmut_108 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_109'] = x__analyze_sentiment_fallback__mutmut_109 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_110'] = x__analyze_sentiment_fallback__mutmut_110 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_111'] = x__analyze_sentiment_fallback__mutmut_111 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_112'] = x__analyze_sentiment_fallback__mutmut_112 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_113'] = x__analyze_sentiment_fallback__mutmut_113 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_114'] = x__analyze_sentiment_fallback__mutmut_114 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_115'] = x__analyze_sentiment_fallback__mutmut_115 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_116'] = x__analyze_sentiment_fallback__mutmut_116 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_117'] = x__analyze_sentiment_fallback__mutmut_117 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_118'] = x__analyze_sentiment_fallback__mutmut_118 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_119'] = x__analyze_sentiment_fallback__mutmut_119 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_120'] = x__analyze_sentiment_fallback__mutmut_120 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_121'] = x__analyze_sentiment_fallback__mutmut_121 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_122'] = x__analyze_sentiment_fallback__mutmut_122 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_123'] = x__analyze_sentiment_fallback__mutmut_123 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_124'] = x__analyze_sentiment_fallback__mutmut_124 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_125'] = x__analyze_sentiment_fallback__mutmut_125 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_126'] = x__analyze_sentiment_fallback__mutmut_126 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_127'] = x__analyze_sentiment_fallback__mutmut_127 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_128'] = x__analyze_sentiment_fallback__mutmut_128 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_129'] = x__analyze_sentiment_fallback__mutmut_129 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_130'] = x__analyze_sentiment_fallback__mutmut_130 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_131'] = x__analyze_sentiment_fallback__mutmut_131 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_132'] = x__analyze_sentiment_fallback__mutmut_132 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_133'] = x__analyze_sentiment_fallback__mutmut_133 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_134'] = x__analyze_sentiment_fallback__mutmut_134 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_135'] = x__analyze_sentiment_fallback__mutmut_135 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_136'] = x__analyze_sentiment_fallback__mutmut_136 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_137'] = x__analyze_sentiment_fallback__mutmut_137 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_138'] = x__analyze_sentiment_fallback__mutmut_138 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_139'] = x__analyze_sentiment_fallback__mutmut_139 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_140'] = x__analyze_sentiment_fallback__mutmut_140 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_141'] = x__analyze_sentiment_fallback__mutmut_141 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_142'] = x__analyze_sentiment_fallback__mutmut_142 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_143'] = x__analyze_sentiment_fallback__mutmut_143 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_144'] = x__analyze_sentiment_fallback__mutmut_144 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_145'] = x__analyze_sentiment_fallback__mutmut_145 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_146'] = x__analyze_sentiment_fallback__mutmut_146 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_147'] = x__analyze_sentiment_fallback__mutmut_147 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_148'] = x__analyze_sentiment_fallback__mutmut_148 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_149'] = x__analyze_sentiment_fallback__mutmut_149 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_150'] = x__analyze_sentiment_fallback__mutmut_150 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_151'] = x__analyze_sentiment_fallback__mutmut_151 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_152'] = x__analyze_sentiment_fallback__mutmut_152 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_153'] = x__analyze_sentiment_fallback__mutmut_153 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_154'] = x__analyze_sentiment_fallback__mutmut_154 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_155'] = x__analyze_sentiment_fallback__mutmut_155 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_156'] = x__analyze_sentiment_fallback__mutmut_156 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_157'] = x__analyze_sentiment_fallback__mutmut_157 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_158'] = x__analyze_sentiment_fallback__mutmut_158 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_159'] = x__analyze_sentiment_fallback__mutmut_159 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_160'] = x__analyze_sentiment_fallback__mutmut_160 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_161'] = x__analyze_sentiment_fallback__mutmut_161 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_162'] = x__analyze_sentiment_fallback__mutmut_162 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_163'] = x__analyze_sentiment_fallback__mutmut_163 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_164'] = x__analyze_sentiment_fallback__mutmut_164 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_165'] = x__analyze_sentiment_fallback__mutmut_165 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_166'] = x__analyze_sentiment_fallback__mutmut_166 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_167'] = x__analyze_sentiment_fallback__mutmut_167 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_168'] = x__analyze_sentiment_fallback__mutmut_168 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_169'] = x__analyze_sentiment_fallback__mutmut_169 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_170'] = x__analyze_sentiment_fallback__mutmut_170 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_171'] = x__analyze_sentiment_fallback__mutmut_171 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_172'] = x__analyze_sentiment_fallback__mutmut_172 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_173'] = x__analyze_sentiment_fallback__mutmut_173 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_174'] = x__analyze_sentiment_fallback__mutmut_174 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_175'] = x__analyze_sentiment_fallback__mutmut_175 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_176'] = x__analyze_sentiment_fallback__mutmut_176 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_177'] = x__analyze_sentiment_fallback__mutmut_177 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_178'] = x__analyze_sentiment_fallback__mutmut_178 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_179'] = x__analyze_sentiment_fallback__mutmut_179 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_180'] = x__analyze_sentiment_fallback__mutmut_180 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_181'] = x__analyze_sentiment_fallback__mutmut_181 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_182'] = x__analyze_sentiment_fallback__mutmut_182 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_183'] = x__analyze_sentiment_fallback__mutmut_183 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_184'] = x__analyze_sentiment_fallback__mutmut_184 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_185'] = x__analyze_sentiment_fallback__mutmut_185 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_186'] = x__analyze_sentiment_fallback__mutmut_186 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_187'] = x__analyze_sentiment_fallback__mutmut_187 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_188'] = x__analyze_sentiment_fallback__mutmut_188 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_189'] = x__analyze_sentiment_fallback__mutmut_189 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_190'] = x__analyze_sentiment_fallback__mutmut_190 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_191'] = x__analyze_sentiment_fallback__mutmut_191 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_192'] = x__analyze_sentiment_fallback__mutmut_192 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_193'] = x__analyze_sentiment_fallback__mutmut_193 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_194'] = x__analyze_sentiment_fallback__mutmut_194 # type: ignore # mutmut generated
mutants_x__analyze_sentiment_fallback__mutmut['x__analyze_sentiment_fallback__mutmut_195'] = x__analyze_sentiment_fallback__mutmut_195 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__risk_gate_fallback__mutmut)
def _risk_gate_fallback(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_orig(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_1(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = None
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_2(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get(None, 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_3(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", None)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_4(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get(20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_5(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", )
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_6(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("XXvixXX", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_7(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("VIX", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_8(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 21)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_9(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = None
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_10(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get(None, 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_11(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", None)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_12(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get(0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_13(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", )
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_14(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("XXyc_spreadXX", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_15(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("YC_SPREAD", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_16(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 1.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_17(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = None
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_18(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get(None, 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_19(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", None)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_20(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get(50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_21(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", )
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_22(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("XXbreadth_pctXX", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_23(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("BREADTH_PCT", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_24(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 51)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_25(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = None

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_26(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get(None, 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_27(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", None)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_28(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get(100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_29(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", )

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_30(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("XXcredit_spreadXX", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_31(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("CREDIT_SPREAD", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_32(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 101)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_33(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = None
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_34(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 1
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_35(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = None

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_36(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix >= 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_37(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 41:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_38(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score = 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_39(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score -= 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_40(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 4
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_41(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(None)
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_42(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix >= 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_43(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 26:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_44(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score = 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_45(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score -= 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_46(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 3
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_47(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(None)
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_48(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix <= 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_49(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 13:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_50(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score = 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_51(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score += 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_52(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 2
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_53(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append(None)

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_54(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("XXLow volatility environmentXX")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_55(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_56(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("LOW VOLATILITY ENVIRONMENT")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_57(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread <= -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_58(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < +0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_59(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -1.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_60(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score = 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_61(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score -= 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_62(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 3
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_63(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append(None)
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_64(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("XXInverted yield curve (recession signal)XX")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_65(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_66(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("INVERTED YIELD CURVE (RECESSION SIGNAL)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_67(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread <= 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_68(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 1:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_69(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score = 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_70(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score -= 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_71(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 2
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_72(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append(None)

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_73(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("XXFlattening yield curveXX")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_74(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_75(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("FLATTENING YIELD CURVE")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_76(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth <= 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_77(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 31:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_78(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score = 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_79(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score -= 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_80(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 3
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_81(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(None)
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_82(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth >= 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_83(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 81:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_84(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score = 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_85(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score += 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_86(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 2
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_87(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append(None)

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_88(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("XXStrong market participationXX")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_89(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_90(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("STRONG MARKET PARTICIPATION")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_91(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread >= 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_92(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 201:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_93(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score = 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_94(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score -= 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_95(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 3
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_96(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append(None)
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_97(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("XXWide credit spreads (stress signal)XX")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_98(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_99(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("WIDE CREDIT SPREADS (STRESS SIGNAL)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_100(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread <= 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_101(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 101:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_102(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score = 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_103(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score += 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_104(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 2
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_105(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append(None)

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_106(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("XXTight credit spreads (healthy)XX")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_107(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_108(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("TIGHT CREDIT SPREADS (HEALTHY)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_109(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score > 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_110(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 6:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_111(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = None
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_112(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "XXextremeXX"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_113(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "EXTREME"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_114(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = None
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_115(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "XXavoidXX"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_116(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "AVOID"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_117(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score > 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_118(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 4:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_119(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = None
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_120(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "XXhighXX"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_121(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "HIGH"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_122(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = None
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_123(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "XXreduce_sizeXX"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_124(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "REDUCE_SIZE"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_125(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score > 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_126(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 2:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_127(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = None
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_128(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "XXmediumXX"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_129(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "MEDIUM"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_130(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = None
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_131(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "XXcautionXX"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_132(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "CAUTION"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_133(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = None
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_134(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "XXlowXX"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_135(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "LOW"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_136(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = None

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_137(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "XXproceedXX"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_138(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "PROCEED"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_139(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = None

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_140(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(None, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_141(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, None)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_142(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_143(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, )

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_144(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(1.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_145(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 - abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_146(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 1.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_147(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) / 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_148(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(None) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_149(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 1.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_150(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=None,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_151(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=None,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_152(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=None,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_153(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=None,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_154(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source=None,
    )


def x__risk_gate_fallback__mutmut_155(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_156(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        factors=factors,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_157(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        recommendation=recommendation,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_158(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        source="fallback",
    )


def x__risk_gate_fallback__mutmut_159(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        )


def x__risk_gate_fallback__mutmut_160(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="XXfallbackXX",
    )


def x__risk_gate_fallback__mutmut_161(macro_data: dict) -> RiskGateResult:
    """Fallback rule-based risk gating when LLM is unavailable."""
    vix = macro_data.get("vix", 20)
    yc_spread = macro_data.get("yc_spread", 0.5)
    breadth = macro_data.get("breadth_pct", 50)
    credit_spread = macro_data.get("credit_spread", 100)

    risk_score = 0
    factors = []

    # VIX assessment
    if vix > 40:
        risk_score += 3
        factors.append(f"Extreme volatility (VIX={vix:.0f})")
    elif vix > 25:
        risk_score += 2
        factors.append(f"High volatility (VIX={vix:.0f})")
    elif vix < 12:
        risk_score -= 1
        factors.append("Low volatility environment")

    # Yield curve
    if yc_spread < -0.5:
        risk_score += 2
        factors.append("Inverted yield curve (recession signal)")
    elif yc_spread < 0:
        risk_score += 1
        factors.append("Flattening yield curve")

    # Market breadth
    if breadth < 30:
        risk_score += 2
        factors.append(f"Poor market breadth ({breadth}% above 200DMA)")
    elif breadth > 80:
        risk_score -= 1
        factors.append("Strong market participation")

    # Credit spreads
    if credit_spread > 200:
        risk_score += 2
        factors.append("Wide credit spreads (stress signal)")
    elif credit_spread < 100:
        risk_score -= 1
        factors.append("Tight credit spreads (healthy)")

    # Determine risk level
    if risk_score >= 5:
        risk_level = "extreme"
        recommendation = "avoid"
    elif risk_score >= 3:
        risk_level = "high"
        recommendation = "reduce_size"
    elif risk_score >= 1:
        risk_level = "medium"
        recommendation = "caution"
    else:
        risk_level = "low"
        recommendation = "proceed"

    confidence = min(0.9, 0.5 + abs(risk_score) * 0.05)

    return RiskGateResult(
        risk_level=risk_level,
        confidence=confidence,
        factors=factors,
        recommendation=recommendation,
        source="FALLBACK",
    )

mutants_x__risk_gate_fallback__mutmut['_mutmut_orig'] = x__risk_gate_fallback__mutmut_orig # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_1'] = x__risk_gate_fallback__mutmut_1 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_2'] = x__risk_gate_fallback__mutmut_2 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_3'] = x__risk_gate_fallback__mutmut_3 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_4'] = x__risk_gate_fallback__mutmut_4 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_5'] = x__risk_gate_fallback__mutmut_5 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_6'] = x__risk_gate_fallback__mutmut_6 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_7'] = x__risk_gate_fallback__mutmut_7 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_8'] = x__risk_gate_fallback__mutmut_8 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_9'] = x__risk_gate_fallback__mutmut_9 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_10'] = x__risk_gate_fallback__mutmut_10 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_11'] = x__risk_gate_fallback__mutmut_11 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_12'] = x__risk_gate_fallback__mutmut_12 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_13'] = x__risk_gate_fallback__mutmut_13 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_14'] = x__risk_gate_fallback__mutmut_14 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_15'] = x__risk_gate_fallback__mutmut_15 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_16'] = x__risk_gate_fallback__mutmut_16 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_17'] = x__risk_gate_fallback__mutmut_17 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_18'] = x__risk_gate_fallback__mutmut_18 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_19'] = x__risk_gate_fallback__mutmut_19 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_20'] = x__risk_gate_fallback__mutmut_20 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_21'] = x__risk_gate_fallback__mutmut_21 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_22'] = x__risk_gate_fallback__mutmut_22 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_23'] = x__risk_gate_fallback__mutmut_23 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_24'] = x__risk_gate_fallback__mutmut_24 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_25'] = x__risk_gate_fallback__mutmut_25 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_26'] = x__risk_gate_fallback__mutmut_26 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_27'] = x__risk_gate_fallback__mutmut_27 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_28'] = x__risk_gate_fallback__mutmut_28 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_29'] = x__risk_gate_fallback__mutmut_29 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_30'] = x__risk_gate_fallback__mutmut_30 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_31'] = x__risk_gate_fallback__mutmut_31 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_32'] = x__risk_gate_fallback__mutmut_32 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_33'] = x__risk_gate_fallback__mutmut_33 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_34'] = x__risk_gate_fallback__mutmut_34 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_35'] = x__risk_gate_fallback__mutmut_35 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_36'] = x__risk_gate_fallback__mutmut_36 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_37'] = x__risk_gate_fallback__mutmut_37 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_38'] = x__risk_gate_fallback__mutmut_38 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_39'] = x__risk_gate_fallback__mutmut_39 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_40'] = x__risk_gate_fallback__mutmut_40 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_41'] = x__risk_gate_fallback__mutmut_41 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_42'] = x__risk_gate_fallback__mutmut_42 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_43'] = x__risk_gate_fallback__mutmut_43 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_44'] = x__risk_gate_fallback__mutmut_44 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_45'] = x__risk_gate_fallback__mutmut_45 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_46'] = x__risk_gate_fallback__mutmut_46 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_47'] = x__risk_gate_fallback__mutmut_47 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_48'] = x__risk_gate_fallback__mutmut_48 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_49'] = x__risk_gate_fallback__mutmut_49 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_50'] = x__risk_gate_fallback__mutmut_50 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_51'] = x__risk_gate_fallback__mutmut_51 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_52'] = x__risk_gate_fallback__mutmut_52 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_53'] = x__risk_gate_fallback__mutmut_53 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_54'] = x__risk_gate_fallback__mutmut_54 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_55'] = x__risk_gate_fallback__mutmut_55 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_56'] = x__risk_gate_fallback__mutmut_56 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_57'] = x__risk_gate_fallback__mutmut_57 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_58'] = x__risk_gate_fallback__mutmut_58 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_59'] = x__risk_gate_fallback__mutmut_59 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_60'] = x__risk_gate_fallback__mutmut_60 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_61'] = x__risk_gate_fallback__mutmut_61 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_62'] = x__risk_gate_fallback__mutmut_62 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_63'] = x__risk_gate_fallback__mutmut_63 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_64'] = x__risk_gate_fallback__mutmut_64 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_65'] = x__risk_gate_fallback__mutmut_65 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_66'] = x__risk_gate_fallback__mutmut_66 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_67'] = x__risk_gate_fallback__mutmut_67 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_68'] = x__risk_gate_fallback__mutmut_68 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_69'] = x__risk_gate_fallback__mutmut_69 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_70'] = x__risk_gate_fallback__mutmut_70 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_71'] = x__risk_gate_fallback__mutmut_71 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_72'] = x__risk_gate_fallback__mutmut_72 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_73'] = x__risk_gate_fallback__mutmut_73 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_74'] = x__risk_gate_fallback__mutmut_74 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_75'] = x__risk_gate_fallback__mutmut_75 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_76'] = x__risk_gate_fallback__mutmut_76 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_77'] = x__risk_gate_fallback__mutmut_77 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_78'] = x__risk_gate_fallback__mutmut_78 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_79'] = x__risk_gate_fallback__mutmut_79 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_80'] = x__risk_gate_fallback__mutmut_80 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_81'] = x__risk_gate_fallback__mutmut_81 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_82'] = x__risk_gate_fallback__mutmut_82 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_83'] = x__risk_gate_fallback__mutmut_83 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_84'] = x__risk_gate_fallback__mutmut_84 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_85'] = x__risk_gate_fallback__mutmut_85 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_86'] = x__risk_gate_fallback__mutmut_86 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_87'] = x__risk_gate_fallback__mutmut_87 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_88'] = x__risk_gate_fallback__mutmut_88 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_89'] = x__risk_gate_fallback__mutmut_89 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_90'] = x__risk_gate_fallback__mutmut_90 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_91'] = x__risk_gate_fallback__mutmut_91 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_92'] = x__risk_gate_fallback__mutmut_92 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_93'] = x__risk_gate_fallback__mutmut_93 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_94'] = x__risk_gate_fallback__mutmut_94 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_95'] = x__risk_gate_fallback__mutmut_95 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_96'] = x__risk_gate_fallback__mutmut_96 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_97'] = x__risk_gate_fallback__mutmut_97 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_98'] = x__risk_gate_fallback__mutmut_98 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_99'] = x__risk_gate_fallback__mutmut_99 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_100'] = x__risk_gate_fallback__mutmut_100 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_101'] = x__risk_gate_fallback__mutmut_101 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_102'] = x__risk_gate_fallback__mutmut_102 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_103'] = x__risk_gate_fallback__mutmut_103 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_104'] = x__risk_gate_fallback__mutmut_104 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_105'] = x__risk_gate_fallback__mutmut_105 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_106'] = x__risk_gate_fallback__mutmut_106 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_107'] = x__risk_gate_fallback__mutmut_107 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_108'] = x__risk_gate_fallback__mutmut_108 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_109'] = x__risk_gate_fallback__mutmut_109 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_110'] = x__risk_gate_fallback__mutmut_110 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_111'] = x__risk_gate_fallback__mutmut_111 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_112'] = x__risk_gate_fallback__mutmut_112 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_113'] = x__risk_gate_fallback__mutmut_113 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_114'] = x__risk_gate_fallback__mutmut_114 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_115'] = x__risk_gate_fallback__mutmut_115 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_116'] = x__risk_gate_fallback__mutmut_116 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_117'] = x__risk_gate_fallback__mutmut_117 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_118'] = x__risk_gate_fallback__mutmut_118 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_119'] = x__risk_gate_fallback__mutmut_119 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_120'] = x__risk_gate_fallback__mutmut_120 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_121'] = x__risk_gate_fallback__mutmut_121 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_122'] = x__risk_gate_fallback__mutmut_122 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_123'] = x__risk_gate_fallback__mutmut_123 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_124'] = x__risk_gate_fallback__mutmut_124 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_125'] = x__risk_gate_fallback__mutmut_125 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_126'] = x__risk_gate_fallback__mutmut_126 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_127'] = x__risk_gate_fallback__mutmut_127 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_128'] = x__risk_gate_fallback__mutmut_128 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_129'] = x__risk_gate_fallback__mutmut_129 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_130'] = x__risk_gate_fallback__mutmut_130 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_131'] = x__risk_gate_fallback__mutmut_131 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_132'] = x__risk_gate_fallback__mutmut_132 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_133'] = x__risk_gate_fallback__mutmut_133 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_134'] = x__risk_gate_fallback__mutmut_134 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_135'] = x__risk_gate_fallback__mutmut_135 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_136'] = x__risk_gate_fallback__mutmut_136 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_137'] = x__risk_gate_fallback__mutmut_137 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_138'] = x__risk_gate_fallback__mutmut_138 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_139'] = x__risk_gate_fallback__mutmut_139 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_140'] = x__risk_gate_fallback__mutmut_140 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_141'] = x__risk_gate_fallback__mutmut_141 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_142'] = x__risk_gate_fallback__mutmut_142 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_143'] = x__risk_gate_fallback__mutmut_143 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_144'] = x__risk_gate_fallback__mutmut_144 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_145'] = x__risk_gate_fallback__mutmut_145 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_146'] = x__risk_gate_fallback__mutmut_146 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_147'] = x__risk_gate_fallback__mutmut_147 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_148'] = x__risk_gate_fallback__mutmut_148 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_149'] = x__risk_gate_fallback__mutmut_149 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_150'] = x__risk_gate_fallback__mutmut_150 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_151'] = x__risk_gate_fallback__mutmut_151 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_152'] = x__risk_gate_fallback__mutmut_152 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_153'] = x__risk_gate_fallback__mutmut_153 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_154'] = x__risk_gate_fallback__mutmut_154 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_155'] = x__risk_gate_fallback__mutmut_155 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_156'] = x__risk_gate_fallback__mutmut_156 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_157'] = x__risk_gate_fallback__mutmut_157 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_158'] = x__risk_gate_fallback__mutmut_158 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_159'] = x__risk_gate_fallback__mutmut_159 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_160'] = x__risk_gate_fallback__mutmut_160 # type: ignore # mutmut generated
mutants_x__risk_gate_fallback__mutmut['x__risk_gate_fallback__mutmut_161'] = x__risk_gate_fallback__mutmut_161 # type: ignore # mutmut generated
mutants_xǁLocalLLMClientǁgenerate__mutmut: MutantDict = {}  # type: ignore


class LocalLLMClient:
    """Abstract local LLM client interface."""

    @_mutmut_mutated(mutants_xǁLocalLLMClientǁgenerate__mutmut)
    def generate(self, prompt: str, system_prompt: str = "") -> str:
        """Generate text from a prompt."""
        raise NotImplementedError

    def xǁLocalLLMClientǁgenerate__mutmut_orig(self, prompt: str, system_prompt: str = "") -> str:
        """Generate text from a prompt."""
        raise NotImplementedError

    def xǁLocalLLMClientǁgenerate__mutmut_1(self, prompt: str, system_prompt: str = "XXXX") -> str:
        """Generate text from a prompt."""
        raise NotImplementedError

    def is_available(self) -> bool:
        """Check if the LLM is available."""
        raise NotImplementedError

mutants_xǁLocalLLMClientǁgenerate__mutmut['_mutmut_orig'] = LocalLLMClient.xǁLocalLLMClientǁgenerate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁLocalLLMClientǁgenerate__mutmut['xǁLocalLLMClientǁgenerate__mutmut_1'] = LocalLLMClient.xǁLocalLLMClientǁgenerate__mutmut_1 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__aio_post__mutmut)
async def _aio_post(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_orig(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_1(url: str, payload: dict, headers: dict = None, timeout: int = 31) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_2(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = None
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_3(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = None
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_4(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"XXContent-TypeXX": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_5(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"content-type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_6(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"CONTENT-TYPE": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_7(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "XXapplication/jsonXX", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_8(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "APPLICATION/JSON", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_9(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers and {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_10(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            None, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_11(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=None, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_12(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=None, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_13(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=None, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_14(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=None
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_15(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_16(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_17(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_18(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, timeout=aiohttp.ClientTimeout(total=timeout)
        ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_19(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, ) as r:
            return await r.json(content_type=None)


async def x__aio_post__mutmut_20(url: str, payload: dict, headers: dict = None, timeout: int = 30) -> dict:
    """Non-blocking POST via aiohttp. Replaces urllib.request.urlopen."""

    import aiohttp

    _ssl = get_ssl_context()
    hdrs = {"Content-Type": "application/json", **(headers or {})}
    async with aiohttp.ClientSession() as sess:
        async with sess.post(
            url, json=payload, headers=hdrs, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=None)
        ) as r:
            return await r.json(content_type=None)

mutants_x__aio_post__mutmut['_mutmut_orig'] = x__aio_post__mutmut_orig # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_1'] = x__aio_post__mutmut_1 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_2'] = x__aio_post__mutmut_2 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_3'] = x__aio_post__mutmut_3 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_4'] = x__aio_post__mutmut_4 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_5'] = x__aio_post__mutmut_5 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_6'] = x__aio_post__mutmut_6 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_7'] = x__aio_post__mutmut_7 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_8'] = x__aio_post__mutmut_8 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_9'] = x__aio_post__mutmut_9 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_10'] = x__aio_post__mutmut_10 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_11'] = x__aio_post__mutmut_11 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_12'] = x__aio_post__mutmut_12 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_13'] = x__aio_post__mutmut_13 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_14'] = x__aio_post__mutmut_14 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_15'] = x__aio_post__mutmut_15 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_16'] = x__aio_post__mutmut_16 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_17'] = x__aio_post__mutmut_17 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_18'] = x__aio_post__mutmut_18 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_19'] = x__aio_post__mutmut_19 # type: ignore # mutmut generated
mutants_x__aio_post__mutmut['x__aio_post__mutmut_20'] = x__aio_post__mutmut_20 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x__aio_get__mutmut)
async def _aio_get(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_orig(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_1(url: str, headers: dict = None, timeout: int = 6) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_2(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = None
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_3(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                None, headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_4(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=None, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_5(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=None, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_6(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, timeout=None
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_7(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_8(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_9(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_10(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_11(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers and {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_12(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=None)
            ) as r:
                return r.status
    except Exception:
        return 0


async def x__aio_get__mutmut_13(url: str, headers: dict = None, timeout: int = 5) -> int:
    """Non-blocking GET status check via aiohttp."""

    import aiohttp

    _ssl = get_ssl_context()
    try:
        async with aiohttp.ClientSession() as sess:
            async with sess.get(
                url, headers=headers or {}, ssl=_ssl, timeout=aiohttp.ClientTimeout(total=timeout)
            ) as r:
                return r.status
    except Exception:
        return 1

mutants_x__aio_get__mutmut['_mutmut_orig'] = x__aio_get__mutmut_orig # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_1'] = x__aio_get__mutmut_1 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_2'] = x__aio_get__mutmut_2 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_3'] = x__aio_get__mutmut_3 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_4'] = x__aio_get__mutmut_4 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_5'] = x__aio_get__mutmut_5 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_6'] = x__aio_get__mutmut_6 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_7'] = x__aio_get__mutmut_7 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_8'] = x__aio_get__mutmut_8 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_9'] = x__aio_get__mutmut_9 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_10'] = x__aio_get__mutmut_10 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_11'] = x__aio_get__mutmut_11 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_12'] = x__aio_get__mutmut_12 # type: ignore # mutmut generated
mutants_x__aio_get__mutmut['x__aio_get__mutmut_13'] = x__aio_get__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁOllamaClientǁis_available__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOllamaClientǁagenerate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOllamaClientǁgenerate__mutmut: MutantDict = {}  # type: ignore


class OllamaClient(LocalLLMClient):
    """Ollama local LLM client — fully async (no blocking urllib)."""

    @_mutmut_mutated(mutants_xǁOllamaClientǁ__init____mutmut)
    def __init__(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.rstrip("/")
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_orig(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.rstrip("/")
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_1(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = None
        self.url = url.rstrip("/")
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_2(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = None
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_3(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.rstrip(None)
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_4(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.lstrip("/")
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_5(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.rstrip("XX/XX")
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_6(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.rstrip("/")
        self._available: Optional[bool] = ""
        self._last_check = 0.0

    def xǁOllamaClientǁ__init____mutmut_7(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.rstrip("/")
        self._available: Optional[bool] = None
        self._last_check = None

    def xǁOllamaClientǁ__init____mutmut_8(self, model: str = LOCAL_LLM_MODEL, url: str = LOCAL_LLM_URL):
        self.model = model
        self.url = url.rstrip("/")
        self._available: Optional[bool] = None
        self._last_check = 1.0

    @_mutmut_mutated(mutants_xǁOllamaClientǁis_available__mutmut)
    def is_available(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_orig(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_1(self) -> bool:
        now = None
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_2(self) -> bool:
        now = time.time()
        if self._available is not None or (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_3(self) -> bool:
        now = time.time()
        if self._available is None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_4(self) -> bool:
        now = time.time()
        if self._available is not None and (now + self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_5(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) <= 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_6(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 61:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_7(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = None
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_8(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split(None)[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_9(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace(None, "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_10(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", None).split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_11(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_12(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", ).split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_13(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace(None, "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_14(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", None).replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_15(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_16(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", ).replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_17(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("XXhttp://XX", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_18(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("HTTP://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_19(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "XXXX").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_20(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("XXhttps://XX", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_21(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("HTTPS://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_22(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "XXXX").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_23(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("XX/XX")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_24(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[1]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_25(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = None
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_26(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(None)
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_27(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.rpartition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_28(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition("XX:XX")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_29(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = None
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_30(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(None) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_31(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (444 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_32(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "XXhttpsXX" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_33(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "HTTPS" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_34(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" not in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_35(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 81)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_36(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = None
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_37(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(None, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_38(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, None)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_39(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_40(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, )
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_41(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(None)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_42(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(3)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_43(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = None
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_44(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex(None)
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_45(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = None
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_46(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result != 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_47(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 1
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_48(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = None
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_49(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = True
        self._last_check = now
        return self._available

    def xǁOllamaClientǁis_available__mutmut_50(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Non-blocking socket probe — avoids blocking urllib.request.urlopen
        # in the asyncio event loop thread. Socket connect with 2s timeout
        # only touches the TCP layer; no HTTP round-trip needed for availability.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 80)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = None
        return self._available

    @_mutmut_mutated(mutants_xǁOllamaClientǁagenerate__mutmut)
    async def agenerate(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_orig(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_1(self, prompt: str, system_prompt: str = "XXXX") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_2(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = None
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_3(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "XXmodelXX": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_4(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "MODEL": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_5(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "XXpromptXX": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_6(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "PROMPT": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_7(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "XXsystemXX": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_8(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "SYSTEM": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_9(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "XXstreamXX": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_10(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "STREAM": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_11(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": True,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_12(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "XXoptionsXX": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_13(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "OPTIONS": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_14(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"XXtemperatureXX": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_15(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"TEMPERATURE": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_16(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 1.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_17(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "XXtop_pXX": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_18(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "TOP_P": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_19(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 1.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_20(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = None
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_21(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(None, payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_22(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", None, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_23(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=None)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_24(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(payload, timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_25(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", timeout=30)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_26(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, )
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_27(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=31)
        return result.get("response", "")

    async def xǁOllamaClientǁagenerate__mutmut_28(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get(None, "")

    async def xǁOllamaClientǁagenerate__mutmut_29(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", None)

    async def xǁOllamaClientǁagenerate__mutmut_30(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("")

    async def xǁOllamaClientǁagenerate__mutmut_31(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", )

    async def xǁOllamaClientǁagenerate__mutmut_32(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("XXresponseXX", "")

    async def xǁOllamaClientǁagenerate__mutmut_33(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("RESPONSE", "")

    async def xǁOllamaClientǁagenerate__mutmut_34(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "prompt": prompt,
            "system": system_prompt,
            "stream": False,
            "options": {"temperature": 0.1, "top_p": 0.9},
        }
        result = await _aio_post(f"{self.url}/api/generate", payload, timeout=30)
        return result.get("response", "XXXX")

    @_mutmut_mutated(mutants_xǁOllamaClientǁgenerate__mutmut)
    def generate(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_orig(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_1(self, prompt: str, system_prompt: str = "XXXX") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_2(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = None
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_3(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=None) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_4(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=2) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_5(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(None, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_6(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, None).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_7(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_8(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, ).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_9(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(None, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_10(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, None)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_11(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_12(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, )).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_13(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(None)

    def xǁOllamaClientǁgenerate__mutmut_14(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(None, system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_15(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, None))

    def xǁOllamaClientǁgenerate__mutmut_16(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(system_prompt))

    def xǁOllamaClientǁgenerate__mutmut_17(self, prompt: str, system_prompt: str = "") -> str:
        """Sync wrapper — only for legacy callers outside asyncio context."""
        import asyncio as _aio

        try:
            loop = _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, ))

mutants_xǁOllamaClientǁ__init____mutmut['_mutmut_orig'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_1'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_2'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_3'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_4'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_5'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_6'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_7'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁ__init____mutmut['xǁOllamaClientǁ__init____mutmut_8'] = OllamaClient.xǁOllamaClientǁ__init____mutmut_8 # type: ignore # mutmut generated

mutants_xǁOllamaClientǁis_available__mutmut['_mutmut_orig'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_1'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_2'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_3'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_4'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_5'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_6'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_7'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_8'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_9'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_10'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_11'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_12'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_13'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_14'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_15'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_16'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_17'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_18'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_19'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_20'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_21'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_22'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_23'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_24'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_25'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_26'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_27'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_28'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_29'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_30'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_31'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_32'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_33'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_34'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_35'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_36'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_37'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_37 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_38'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_38 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_39'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_39 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_40'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_40 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_41'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_41 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_42'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_42 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_43'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_43 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_44'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_44 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_45'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_45 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_46'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_46 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_47'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_47 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_48'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_48 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_49'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_49 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁis_available__mutmut['xǁOllamaClientǁis_available__mutmut_50'] = OllamaClient.xǁOllamaClientǁis_available__mutmut_50 # type: ignore # mutmut generated

mutants_xǁOllamaClientǁagenerate__mutmut['_mutmut_orig'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_1'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_2'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_3'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_4'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_5'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_6'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_7'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_8'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_9'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_10'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_11'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_12'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_13'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_14'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_15'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_16'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_17'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_18'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_19'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_20'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_21'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_22'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_23'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_24'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_25'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_26'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_27'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_28'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_29'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_30'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_31'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_32'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_33'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁagenerate__mutmut['xǁOllamaClientǁagenerate__mutmut_34'] = OllamaClient.xǁOllamaClientǁagenerate__mutmut_34 # type: ignore # mutmut generated

mutants_xǁOllamaClientǁgenerate__mutmut['_mutmut_orig'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_1'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_2'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_3'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_4'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_5'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_6'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_7'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_8'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_9'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_10'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_11'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_12'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_13'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_14'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_15'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_16'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOllamaClientǁgenerate__mutmut['xǁOllamaClientǁgenerate__mutmut_17'] = OllamaClient.xǁOllamaClientǁgenerate__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenAICompatibleClientǁis_available__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut: MutantDict = {}  # type: ignore
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut: MutantDict = {}  # type: ignore


class OpenAICompatibleClient(LocalLLMClient):
    """OpenAI-compatible local LLM client (LM Studio, etc.) — fully async."""

    @_mutmut_mutated(mutants_xǁOpenAICompatibleClientǁ__init____mutmut)
    def __init__(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_orig(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_1(self, model: str = LOCAL_LLM_MODEL, url: str = "XXhttp://localhost:1234/v1XX", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_2(self, model: str = LOCAL_LLM_MODEL, url: str = "HTTP://LOCALHOST:1234/V1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_3(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = None
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_4(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = None
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_5(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip(None)
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_6(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.lstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_7(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("XX/XX")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_8(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = None
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_9(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key and "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_10(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "XXnot-neededXX"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_11(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "NOT-NEEDED"
        self._available: Optional[bool] = None
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_12(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = ""
        self._last_check = 0.0

    def xǁOpenAICompatibleClientǁ__init____mutmut_13(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = None

    def xǁOpenAICompatibleClientǁ__init____mutmut_14(self, model: str = LOCAL_LLM_MODEL, url: str = "http://localhost:1234/v1", api_key: str = None):
        self.model = model
        self.url = url.rstrip("/")
        self.api_key = api_key or "not-needed"
        self._available: Optional[bool] = None
        self._last_check = 1.0

    @_mutmut_mutated(mutants_xǁOpenAICompatibleClientǁis_available__mutmut)
    def is_available(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_orig(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_1(self) -> bool:
        now = None
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_2(self) -> bool:
        now = time.time()
        if self._available is not None or (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_3(self) -> bool:
        now = time.time()
        if self._available is None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_4(self) -> bool:
        now = time.time()
        if self._available is not None and (now + self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_5(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) <= 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_6(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 61:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_7(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = None
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_8(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split(None)[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_9(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace(None, "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_10(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", None).split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_11(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_12(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", ).split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_13(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace(None, "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_14(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", None).replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_15(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_16(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", ).replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_17(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("XXhttp://XX", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_18(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("HTTP://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_19(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "XXXX").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_20(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("XXhttps://XX", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_21(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("HTTPS://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_22(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "XXXX").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_23(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("XX/XX")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_24(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[1]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_25(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = None
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_26(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(None)
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_27(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.rpartition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_28(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition("XX:XX")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_29(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = None
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_30(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(None) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_31(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (444 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_32(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "XXhttpsXX" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_33(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "HTTPS" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_34(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" not in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_35(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1235)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_36(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = None
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_37(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(None, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_38(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, None)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_39(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_40(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, )
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_41(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(None)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_42(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(3)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_43(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = None
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_44(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex(None)
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_45(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = None
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_46(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result != 0
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_47(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 1
        except Exception:
            self._available = False
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_48(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = None
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_49(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = True
        self._last_check = now
        return self._available

    def xǁOpenAICompatibleClientǁis_available__mutmut_50(self) -> bool:
        now = time.time()
        if self._available is not None and (now - self._last_check) < 60:
            return self._available
        # Same non-blocking TCP socket probe as OllamaClient — avoids blocking
        # urllib.request in the event loop thread.
        try:
            import socket as _socket

            parsed = self.url.replace("http://", "").replace("https://", "").split("/")[0]
            host, _, port_str = parsed.partition(":")
            port = int(port_str) if port_str else (443 if "https" in self.url else 1234)
            s = _socket.socket(_socket.AF_INET, _socket.SOCK_STREAM)
            s.settimeout(2)
            result = s.connect_ex((host, port))
            s.close()
            self._available = result == 0
        except Exception:
            self._available = False
        self._last_check = None
        return self._available

    @_mutmut_mutated(mutants_xǁOpenAICompatibleClientǁagenerate__mutmut)
    async def agenerate(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_orig(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_1(self, prompt: str, system_prompt: str = "XXXX") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_2(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = None
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_3(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "XXmodelXX": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_4(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "MODEL": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_5(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "XXmessagesXX": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_6(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "MESSAGES": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_7(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"XXroleXX": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_8(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"ROLE": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_9(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "XXsystemXX", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_10(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "SYSTEM", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_11(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "XXcontentXX": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_12(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "CONTENT": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_13(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"XXroleXX": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_14(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"ROLE": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_15(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "XXuserXX", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_16(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "USER", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_17(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "XXcontentXX": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_18(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "CONTENT": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_19(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "XXtemperatureXX": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_20(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "TEMPERATURE": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_21(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 1.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_22(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "XXmax_tokensXX": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_23(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "MAX_TOKENS": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_24(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 257,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_25(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = None
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_26(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"XXAuthorizationXX": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_27(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_28(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"AUTHORIZATION": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_29(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = None
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_30(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(None, payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_31(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", None, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_32(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=None, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_33(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=None)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_34(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_35(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", headers=headers, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_36(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, timeout=30)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_37(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, )
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_38(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=31)
        return result["choices"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_39(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["XXchoicesXX"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_40(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["CHOICES"][0]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_41(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][1]["message"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_42(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["XXmessageXX"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_43(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["MESSAGE"]["content"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_44(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["XXcontentXX"]

    async def xǁOpenAICompatibleClientǁagenerate__mutmut_45(self, prompt: str, system_prompt: str = "") -> str:
        """Async generate — does not block the FastAPI event loop."""
        payload = {
            "model": self.model,
            "messages": [{"role": "system", "content": system_prompt}, {"role": "user", "content": prompt}],
            "temperature": 0.1,
            "max_tokens": 256,
        }
        headers = {"Authorization": f"Bearer {self.api_key}"}
        result = await _aio_post(f"{self.url}/chat/completions", payload, headers=headers, timeout=30)
        return result["choices"][0]["message"]["CONTENT"]

    @_mutmut_mutated(mutants_xǁOpenAICompatibleClientǁgenerate__mutmut)
    def generate(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_orig(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_1(self, prompt: str, system_prompt: str = "XXXX") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_2(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=None) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_3(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=2) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_4(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(None, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_5(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, None).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_6(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_7(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, ).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_8(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(None, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_9(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, None)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_10(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_11(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, )).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_12(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(None)

    def xǁOpenAICompatibleClientǁgenerate__mutmut_13(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(None, system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_14(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, None))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_15(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(system_prompt))

    def xǁOpenAICompatibleClientǁgenerate__mutmut_16(self, prompt: str, system_prompt: str = "") -> str:
        import asyncio as _aio

        try:
            _aio.get_running_loop()
            import concurrent.futures

            with concurrent.futures.ThreadPoolExecutor(max_workers=1) as ex:
                return ex.submit(_aio.run, self.agenerate(prompt, system_prompt)).result()
        except RuntimeError:
            return _aio.run(self.agenerate(prompt, ))

mutants_xǁOpenAICompatibleClientǁ__init____mutmut['_mutmut_orig'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_1'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_2'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_3'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_4'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_5'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_6'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_7'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_8'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_9'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_10'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_11'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_12'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_13'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁ__init____mutmut['xǁOpenAICompatibleClientǁ__init____mutmut_14'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁ__init____mutmut_14 # type: ignore # mutmut generated

mutants_xǁOpenAICompatibleClientǁis_available__mutmut['_mutmut_orig'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_1'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_2'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_3'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_4'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_5'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_6'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_7'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_8'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_9'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_10'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_11'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_12'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_13'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_14'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_15'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_16'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_17'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_18'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_19'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_20'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_21'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_22'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_23'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_24'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_25'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_26'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_27'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_28'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_29'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_30'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_31'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_32'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_33'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_34'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_35'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_36'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_37'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_37 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_38'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_38 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_39'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_39 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_40'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_40 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_41'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_41 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_42'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_42 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_43'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_43 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_44'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_44 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_45'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_45 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_46'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_46 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_47'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_47 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_48'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_48 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_49'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_49 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁis_available__mutmut['xǁOpenAICompatibleClientǁis_available__mutmut_50'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁis_available__mutmut_50 # type: ignore # mutmut generated

mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['_mutmut_orig'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_1'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_2'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_3'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_4'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_5'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_6'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_7'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_8'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_9'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_10'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_11'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_12'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_13'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_14'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_15'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_16'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_16 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_17'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_17 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_18'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_18 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_19'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_19 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_20'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_20 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_21'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_21 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_22'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_22 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_23'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_23 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_24'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_24 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_25'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_25 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_26'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_26 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_27'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_27 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_28'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_28 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_29'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_29 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_30'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_30 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_31'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_31 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_32'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_32 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_33'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_33 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_34'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_34 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_35'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_35 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_36'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_36 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_37'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_37 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_38'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_38 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_39'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_39 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_40'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_40 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_41'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_41 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_42'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_42 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_43'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_43 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_44'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_44 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁagenerate__mutmut['xǁOpenAICompatibleClientǁagenerate__mutmut_45'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁagenerate__mutmut_45 # type: ignore # mutmut generated

mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['_mutmut_orig'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_orig # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_1'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_1 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_2'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_2 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_3'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_3 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_4'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_4 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_5'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_5 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_6'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_6 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_7'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_7 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_8'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_8 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_9'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_9 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_10'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_10 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_11'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_11 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_12'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_12 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_13'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_13 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_14'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_14 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_15'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_15 # type: ignore # mutmut generated
mutants_xǁOpenAICompatibleClientǁgenerate__mutmut['xǁOpenAICompatibleClientǁgenerate__mutmut_16'] = OpenAICompatibleClient.xǁOpenAICompatibleClientǁgenerate__mutmut_16 # type: ignore # mutmut generated


# Global client instance
_llm_client: Optional[LocalLLMClient] = None
mutants_x_get_llm_client__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_llm_client__mutmut)
def get_llm_client() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_orig() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_1() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_2() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_3() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info(None)
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_4() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("XXLocal LLM disabled by configurationXX")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_5() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("local llm disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_6() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("LOCAL LLM DISABLED BY CONFIGURATION")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_7() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER != "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_8() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "XXollamaXX":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_9() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "OLLAMA":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_10() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = None
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_11() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER != "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_12() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "XXopenai-compatibleXX":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_13() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "OPENAI-COMPATIBLE":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_14() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = None
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_15() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = None  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_16() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = None
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_17() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(None)
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_18() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(None)
        _llm_client = None

    return _llm_client


def x_get_llm_client__mutmut_19() -> Optional[LocalLLMClient]:
    """Get or create the LLM client."""
    global _llm_client
    if _llm_client is not None:
        return _llm_client

    if not LOCAL_LLM_ENABLED:
        logger.info("Local LLM disabled by configuration")
        return None

    if LOCAL_LLM_PROVIDER == "ollama":
        client = OllamaClient()
    elif LOCAL_LLM_PROVIDER == "openai-compatible":
        client = OpenAICompatibleClient()
    else:
        client = OllamaClient()  # Default

    if client.is_available():
        _llm_client = client
        logger.info(f"Local LLM connected: {LOCAL_LLM_PROVIDER}/{LOCAL_LLM_MODEL}")
    else:
        logger.warning(f"Local LLM not available at {LOCAL_LLM_URL}, using fallback analysis")
        _llm_client = ""

    return _llm_client

mutants_x_get_llm_client__mutmut['_mutmut_orig'] = x_get_llm_client__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_1'] = x_get_llm_client__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_2'] = x_get_llm_client__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_3'] = x_get_llm_client__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_4'] = x_get_llm_client__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_5'] = x_get_llm_client__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_6'] = x_get_llm_client__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_7'] = x_get_llm_client__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_8'] = x_get_llm_client__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_9'] = x_get_llm_client__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_10'] = x_get_llm_client__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_11'] = x_get_llm_client__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_12'] = x_get_llm_client__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_13'] = x_get_llm_client__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_14'] = x_get_llm_client__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_15'] = x_get_llm_client__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_16'] = x_get_llm_client__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_17'] = x_get_llm_client__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_18'] = x_get_llm_client__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_llm_client__mutmut['x_get_llm_client__mutmut_19'] = x_get_llm_client__mutmut_19 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_analyze_news_sentiment__mutmut)
def analyze_news_sentiment(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_orig(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_1(news_items: list[dict], ticker: str = "XXXX") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_2(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = None

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_3(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None and not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_4(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is not None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_5(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_6(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(None)
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_7(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(None)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_8(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = None

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_9(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        None  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_10(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "XX\n\nXX".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_11(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get(None, '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_12(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', None)}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_13(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_14(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', )}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_15(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('XXheadlineXX', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_16(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('HEADLINE', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_17(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', 'XXXX')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_18(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get(None, '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_19(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', None)}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_20(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_21(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', )}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_22(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('XXsummaryXX', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_23(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('SUMMARY', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_24(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', 'XXXX')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_25(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:11]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_26(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = None

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_27(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = None

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_28(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker and "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_29(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "XXthis stockXX"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_30(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "THIS STOCK"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_31(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = None

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_32(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(None, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_33(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, None)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_34(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_35(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, )

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_36(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = None
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_37(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(None)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_38(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = None
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_39(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(None, response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_40(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", None, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_41(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, None)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_42(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_43(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_44(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, )
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_45(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"XXREASONING:\s*(.+)XX", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_46(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"reasoning:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_47(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = None

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_48(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(None).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_49(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(2).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_50(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:101]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_51(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = None
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_52(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "XXbullishXX" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_53(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "BULLISH" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_54(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score >= 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_55(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 1.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_56(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "XXbearishXX" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_57(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "BEARISH" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_58(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score <= -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_59(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < +0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_60(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -1.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_61(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "XXneutralXX"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_62(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "NEUTRAL"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_63(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = None

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_64(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(None, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_65(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, None)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_66(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_67(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, )

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_68(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(1.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_69(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 - abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_70(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 1.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_71(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) / 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_72(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(None) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_73(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 1.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_74(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=None,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_75(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=None,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_76(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=None,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_77(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=None,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_78(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source=None,
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_79(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_80(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_81(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_82(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_83(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_84(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="XXllmXX",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_85(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="LLM",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_86(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(None)
        return _analyze_sentiment_fallback(news_items)


def x_analyze_news_sentiment__mutmut_87(news_items: list[dict], ticker: str = "") -> SentimentResult:
    """
    Analyze sentiment of news items using local LLM or fallback.

    Args:
        news_items: List of news items with 'headline' and 'summary' fields
        ticker: Optional ticker symbol for context

    Returns:
        SentimentResult with score, confidence, and reasoning
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(f"Using fallback sentiment analysis for {ticker}")
        return _analyze_sentiment_fallback(news_items)

    # Prepare news text
    news_text = "\n\n".join(
        f"Headline: {item.get('headline', '')}\nSummary: {item.get('summary', '')}"
        for item in news_items[:10]  # Limit to 10 items
    )

    system_prompt = """You are a financial sentiment analyst. Analyze news headlines and summaries to determine market sentiment for a stock.
Respond with a sentiment score between -1.0 (very bearish) and +1.0 (very bullish), followed by a brief explanation.
Format: SCORE: <number> | REASONING: <explanation>"""

    prompt = f"""Analyze the following news for {ticker or "this stock"}:

{news_text}

Provide a sentiment score and reasoning."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        score = _parse_sentiment_score(response)
        reasoning_match = re.search(r"REASONING:\s*(.+)", response, re.IGNORECASE)
        reasoning = reasoning_match.group(1).strip() if reasoning_match else response[:100]

        label = "bullish" if score > 0.2 else "bearish" if score < -0.2 else "neutral"
        confidence = min(0.9, 0.5 + abs(score) * 0.4)

        return SentimentResult(
            score=score,
            confidence=confidence,
            label=label,
            reasoning=reasoning,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM sentiment analysis failed for {ticker}: {e}, using fallback")
        return _analyze_sentiment_fallback(None)

mutants_x_analyze_news_sentiment__mutmut['_mutmut_orig'] = x_analyze_news_sentiment__mutmut_orig # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_1'] = x_analyze_news_sentiment__mutmut_1 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_2'] = x_analyze_news_sentiment__mutmut_2 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_3'] = x_analyze_news_sentiment__mutmut_3 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_4'] = x_analyze_news_sentiment__mutmut_4 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_5'] = x_analyze_news_sentiment__mutmut_5 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_6'] = x_analyze_news_sentiment__mutmut_6 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_7'] = x_analyze_news_sentiment__mutmut_7 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_8'] = x_analyze_news_sentiment__mutmut_8 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_9'] = x_analyze_news_sentiment__mutmut_9 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_10'] = x_analyze_news_sentiment__mutmut_10 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_11'] = x_analyze_news_sentiment__mutmut_11 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_12'] = x_analyze_news_sentiment__mutmut_12 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_13'] = x_analyze_news_sentiment__mutmut_13 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_14'] = x_analyze_news_sentiment__mutmut_14 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_15'] = x_analyze_news_sentiment__mutmut_15 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_16'] = x_analyze_news_sentiment__mutmut_16 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_17'] = x_analyze_news_sentiment__mutmut_17 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_18'] = x_analyze_news_sentiment__mutmut_18 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_19'] = x_analyze_news_sentiment__mutmut_19 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_20'] = x_analyze_news_sentiment__mutmut_20 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_21'] = x_analyze_news_sentiment__mutmut_21 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_22'] = x_analyze_news_sentiment__mutmut_22 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_23'] = x_analyze_news_sentiment__mutmut_23 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_24'] = x_analyze_news_sentiment__mutmut_24 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_25'] = x_analyze_news_sentiment__mutmut_25 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_26'] = x_analyze_news_sentiment__mutmut_26 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_27'] = x_analyze_news_sentiment__mutmut_27 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_28'] = x_analyze_news_sentiment__mutmut_28 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_29'] = x_analyze_news_sentiment__mutmut_29 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_30'] = x_analyze_news_sentiment__mutmut_30 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_31'] = x_analyze_news_sentiment__mutmut_31 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_32'] = x_analyze_news_sentiment__mutmut_32 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_33'] = x_analyze_news_sentiment__mutmut_33 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_34'] = x_analyze_news_sentiment__mutmut_34 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_35'] = x_analyze_news_sentiment__mutmut_35 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_36'] = x_analyze_news_sentiment__mutmut_36 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_37'] = x_analyze_news_sentiment__mutmut_37 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_38'] = x_analyze_news_sentiment__mutmut_38 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_39'] = x_analyze_news_sentiment__mutmut_39 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_40'] = x_analyze_news_sentiment__mutmut_40 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_41'] = x_analyze_news_sentiment__mutmut_41 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_42'] = x_analyze_news_sentiment__mutmut_42 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_43'] = x_analyze_news_sentiment__mutmut_43 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_44'] = x_analyze_news_sentiment__mutmut_44 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_45'] = x_analyze_news_sentiment__mutmut_45 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_46'] = x_analyze_news_sentiment__mutmut_46 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_47'] = x_analyze_news_sentiment__mutmut_47 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_48'] = x_analyze_news_sentiment__mutmut_48 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_49'] = x_analyze_news_sentiment__mutmut_49 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_50'] = x_analyze_news_sentiment__mutmut_50 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_51'] = x_analyze_news_sentiment__mutmut_51 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_52'] = x_analyze_news_sentiment__mutmut_52 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_53'] = x_analyze_news_sentiment__mutmut_53 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_54'] = x_analyze_news_sentiment__mutmut_54 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_55'] = x_analyze_news_sentiment__mutmut_55 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_56'] = x_analyze_news_sentiment__mutmut_56 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_57'] = x_analyze_news_sentiment__mutmut_57 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_58'] = x_analyze_news_sentiment__mutmut_58 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_59'] = x_analyze_news_sentiment__mutmut_59 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_60'] = x_analyze_news_sentiment__mutmut_60 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_61'] = x_analyze_news_sentiment__mutmut_61 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_62'] = x_analyze_news_sentiment__mutmut_62 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_63'] = x_analyze_news_sentiment__mutmut_63 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_64'] = x_analyze_news_sentiment__mutmut_64 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_65'] = x_analyze_news_sentiment__mutmut_65 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_66'] = x_analyze_news_sentiment__mutmut_66 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_67'] = x_analyze_news_sentiment__mutmut_67 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_68'] = x_analyze_news_sentiment__mutmut_68 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_69'] = x_analyze_news_sentiment__mutmut_69 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_70'] = x_analyze_news_sentiment__mutmut_70 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_71'] = x_analyze_news_sentiment__mutmut_71 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_72'] = x_analyze_news_sentiment__mutmut_72 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_73'] = x_analyze_news_sentiment__mutmut_73 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_74'] = x_analyze_news_sentiment__mutmut_74 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_75'] = x_analyze_news_sentiment__mutmut_75 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_76'] = x_analyze_news_sentiment__mutmut_76 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_77'] = x_analyze_news_sentiment__mutmut_77 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_78'] = x_analyze_news_sentiment__mutmut_78 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_79'] = x_analyze_news_sentiment__mutmut_79 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_80'] = x_analyze_news_sentiment__mutmut_80 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_81'] = x_analyze_news_sentiment__mutmut_81 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_82'] = x_analyze_news_sentiment__mutmut_82 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_83'] = x_analyze_news_sentiment__mutmut_83 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_84'] = x_analyze_news_sentiment__mutmut_84 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_85'] = x_analyze_news_sentiment__mutmut_85 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_86'] = x_analyze_news_sentiment__mutmut_86 # type: ignore # mutmut generated
mutants_x_analyze_news_sentiment__mutmut['x_analyze_news_sentiment__mutmut_87'] = x_analyze_news_sentiment__mutmut_87 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_assess_macro_risk__mutmut)
def assess_macro_risk(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_orig(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_1(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = None

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_2(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None and not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_3(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is not None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_4(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_5(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug(None)
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_6(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("XXUsing fallback macro risk assessmentXX")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_7(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_8(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("USING FALLBACK MACRO RISK ASSESSMENT")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_9(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(None)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_10(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = None

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_11(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_12(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "XX\nXX".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_13(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_14(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = None

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_15(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = None

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_16(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = None

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_17(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(None, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_18(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, None)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_19(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_20(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, )

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_21(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = None
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_22(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(None, response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_23(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", None, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_24(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, None)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_25(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_26(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_27(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, )
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_28(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"XXRISK:\s*(\w+)XX", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_29(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"risk:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_30(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = None
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_31(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(None, response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_32(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", None, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_33(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, None)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_34(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_35(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_36(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, )
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_37(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"XXCONFIDENCE:\s*([\d.]+)XX", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_38(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"confidence:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_39(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = None
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_40(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(None, response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_41(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", None, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_42(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, None)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_43(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_44(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_45(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, )
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_46(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"XXFACTORS:\s*(.+?)(?:\||$)XX", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_47(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"factors:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_48(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = None

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_49(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(None, response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_50(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", None, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_51(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, None)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_52(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_53(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_54(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, )

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_55(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"XXRECOMMENDATION:\s*(\w+)XX", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_56(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"recommendation:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_57(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = None
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_58(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).upper() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_59(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(None).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_60(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(2).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_61(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "XXmediumXX"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_62(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "MEDIUM"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_63(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_64(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("XXlowXX", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_65(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("LOW", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_66(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "XXmediumXX", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_67(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "MEDIUM", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_68(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "XXhighXX", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_69(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "HIGH", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_70(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "XXextremeXX"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_71(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "EXTREME"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_72(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = None

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_73(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "XXmediumXX"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_74(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "MEDIUM"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_75(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = None
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_76(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(None) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_77(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(None)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_78(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(2)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_79(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 1.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_80(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = None

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_81(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(None, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_82(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, None)

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_83(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_84(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, )

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_85(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(1.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_86(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(None, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_87(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, None))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_88(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_89(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, ))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_90(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(1.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_91(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = None
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_92(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = None

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_93(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(None)]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_94(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(None).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_95(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(2).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_96(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split("XX,XX")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_97(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = None
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_98(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).upper() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_99(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(None).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_100(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(2).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_101(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "XXcautionXX"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_102(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "CAUTION"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_103(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_104(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("XXproceedXX", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_105(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("PROCEED", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_106(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "XXcautionXX", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_107(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "CAUTION", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_108(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "XXreduce_sizeXX", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_109(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "REDUCE_SIZE", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_110(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "XXavoidXX"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_111(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "AVOID"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_112(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = None

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_113(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "XXcautionXX"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_114(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "CAUTION"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_115(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=None,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_116(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=None,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_117(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=None,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_118(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=None,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_119(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source=None,
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_120(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_121(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_122(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_123(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_124(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_125(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="XXllmXX",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_126(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="LLM",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_127(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(None)
        return _risk_gate_fallback(macro_data)


def x_assess_macro_risk__mutmut_128(macro_data: dict) -> RiskGateResult:
    """
    Assess macro risk level using local LLM or fallback.

    Args:
        macro_data: Dictionary with macro indicators (VIX, yield curve, breadth, etc.)

    Returns:
        RiskGateResult with risk level, factors, and recommendation
    """
    client = get_llm_client()

    if client is None or not client.is_available():
        logger.debug("Using fallback macro risk assessment")
        return _risk_gate_fallback(macro_data)

    # Format macro data for LLM
    macro_text = "\n".join(f"- {k}: {v}" for k, v in macro_data.items() if v is not None)

    system_prompt = """You are a macro risk analyst. Assess the overall market risk level based on provided indicators.
Consider: volatility (VIX), yield curve, market breadth, credit spreads, economic data.
Respond with: RISK: <low/medium/high/extreme> | CONFIDENCE: <0-1> | FACTORS: <key factors> | RECOMMENDATION: <proceed/caution/reduce_size/avoid>"""

    prompt = f"""Assess the macro risk level based on these indicators:

{macro_text}

Provide risk assessment and trading recommendation."""

    try:
        response = client.generate(prompt, system_prompt)

        # Parse response
        risk_match = re.search(r"RISK:\s*(\w+)", response, re.IGNORECASE)
        conf_match = re.search(r"CONFIDENCE:\s*([\d.]+)", response, re.IGNORECASE)
        factors_match = re.search(r"FACTORS:\s*(.+?)(?:\||$)", response, re.IGNORECASE)
        rec_match = re.search(r"RECOMMENDATION:\s*(\w+)", response, re.IGNORECASE)

        risk_level = risk_match.group(1).lower() if risk_match else "medium"
        if risk_level not in ("low", "medium", "high", "extreme"):
            risk_level = "medium"

        confidence = float(conf_match.group(1)) if conf_match else 0.5
        confidence = max(0.1, min(0.95, confidence))

        factors = []
        if factors_match:
            factors = [f.strip() for f in factors_match.group(1).split(",")]

        recommendation = rec_match.group(1).lower() if rec_match else "caution"
        if recommendation not in ("proceed", "caution", "reduce_size", "avoid"):
            recommendation = "caution"

        return RiskGateResult(
            risk_level=risk_level,
            confidence=confidence,
            factors=factors,
            recommendation=recommendation,
            source="llm",
        )
    except Exception as e:
        logger.warning(f"LLM macro risk assessment failed: {e}, using fallback")
        return _risk_gate_fallback(None)

mutants_x_assess_macro_risk__mutmut['_mutmut_orig'] = x_assess_macro_risk__mutmut_orig # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_1'] = x_assess_macro_risk__mutmut_1 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_2'] = x_assess_macro_risk__mutmut_2 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_3'] = x_assess_macro_risk__mutmut_3 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_4'] = x_assess_macro_risk__mutmut_4 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_5'] = x_assess_macro_risk__mutmut_5 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_6'] = x_assess_macro_risk__mutmut_6 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_7'] = x_assess_macro_risk__mutmut_7 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_8'] = x_assess_macro_risk__mutmut_8 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_9'] = x_assess_macro_risk__mutmut_9 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_10'] = x_assess_macro_risk__mutmut_10 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_11'] = x_assess_macro_risk__mutmut_11 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_12'] = x_assess_macro_risk__mutmut_12 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_13'] = x_assess_macro_risk__mutmut_13 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_14'] = x_assess_macro_risk__mutmut_14 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_15'] = x_assess_macro_risk__mutmut_15 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_16'] = x_assess_macro_risk__mutmut_16 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_17'] = x_assess_macro_risk__mutmut_17 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_18'] = x_assess_macro_risk__mutmut_18 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_19'] = x_assess_macro_risk__mutmut_19 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_20'] = x_assess_macro_risk__mutmut_20 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_21'] = x_assess_macro_risk__mutmut_21 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_22'] = x_assess_macro_risk__mutmut_22 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_23'] = x_assess_macro_risk__mutmut_23 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_24'] = x_assess_macro_risk__mutmut_24 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_25'] = x_assess_macro_risk__mutmut_25 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_26'] = x_assess_macro_risk__mutmut_26 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_27'] = x_assess_macro_risk__mutmut_27 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_28'] = x_assess_macro_risk__mutmut_28 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_29'] = x_assess_macro_risk__mutmut_29 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_30'] = x_assess_macro_risk__mutmut_30 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_31'] = x_assess_macro_risk__mutmut_31 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_32'] = x_assess_macro_risk__mutmut_32 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_33'] = x_assess_macro_risk__mutmut_33 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_34'] = x_assess_macro_risk__mutmut_34 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_35'] = x_assess_macro_risk__mutmut_35 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_36'] = x_assess_macro_risk__mutmut_36 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_37'] = x_assess_macro_risk__mutmut_37 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_38'] = x_assess_macro_risk__mutmut_38 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_39'] = x_assess_macro_risk__mutmut_39 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_40'] = x_assess_macro_risk__mutmut_40 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_41'] = x_assess_macro_risk__mutmut_41 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_42'] = x_assess_macro_risk__mutmut_42 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_43'] = x_assess_macro_risk__mutmut_43 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_44'] = x_assess_macro_risk__mutmut_44 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_45'] = x_assess_macro_risk__mutmut_45 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_46'] = x_assess_macro_risk__mutmut_46 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_47'] = x_assess_macro_risk__mutmut_47 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_48'] = x_assess_macro_risk__mutmut_48 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_49'] = x_assess_macro_risk__mutmut_49 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_50'] = x_assess_macro_risk__mutmut_50 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_51'] = x_assess_macro_risk__mutmut_51 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_52'] = x_assess_macro_risk__mutmut_52 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_53'] = x_assess_macro_risk__mutmut_53 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_54'] = x_assess_macro_risk__mutmut_54 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_55'] = x_assess_macro_risk__mutmut_55 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_56'] = x_assess_macro_risk__mutmut_56 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_57'] = x_assess_macro_risk__mutmut_57 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_58'] = x_assess_macro_risk__mutmut_58 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_59'] = x_assess_macro_risk__mutmut_59 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_60'] = x_assess_macro_risk__mutmut_60 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_61'] = x_assess_macro_risk__mutmut_61 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_62'] = x_assess_macro_risk__mutmut_62 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_63'] = x_assess_macro_risk__mutmut_63 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_64'] = x_assess_macro_risk__mutmut_64 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_65'] = x_assess_macro_risk__mutmut_65 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_66'] = x_assess_macro_risk__mutmut_66 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_67'] = x_assess_macro_risk__mutmut_67 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_68'] = x_assess_macro_risk__mutmut_68 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_69'] = x_assess_macro_risk__mutmut_69 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_70'] = x_assess_macro_risk__mutmut_70 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_71'] = x_assess_macro_risk__mutmut_71 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_72'] = x_assess_macro_risk__mutmut_72 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_73'] = x_assess_macro_risk__mutmut_73 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_74'] = x_assess_macro_risk__mutmut_74 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_75'] = x_assess_macro_risk__mutmut_75 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_76'] = x_assess_macro_risk__mutmut_76 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_77'] = x_assess_macro_risk__mutmut_77 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_78'] = x_assess_macro_risk__mutmut_78 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_79'] = x_assess_macro_risk__mutmut_79 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_80'] = x_assess_macro_risk__mutmut_80 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_81'] = x_assess_macro_risk__mutmut_81 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_82'] = x_assess_macro_risk__mutmut_82 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_83'] = x_assess_macro_risk__mutmut_83 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_84'] = x_assess_macro_risk__mutmut_84 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_85'] = x_assess_macro_risk__mutmut_85 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_86'] = x_assess_macro_risk__mutmut_86 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_87'] = x_assess_macro_risk__mutmut_87 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_88'] = x_assess_macro_risk__mutmut_88 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_89'] = x_assess_macro_risk__mutmut_89 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_90'] = x_assess_macro_risk__mutmut_90 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_91'] = x_assess_macro_risk__mutmut_91 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_92'] = x_assess_macro_risk__mutmut_92 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_93'] = x_assess_macro_risk__mutmut_93 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_94'] = x_assess_macro_risk__mutmut_94 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_95'] = x_assess_macro_risk__mutmut_95 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_96'] = x_assess_macro_risk__mutmut_96 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_97'] = x_assess_macro_risk__mutmut_97 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_98'] = x_assess_macro_risk__mutmut_98 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_99'] = x_assess_macro_risk__mutmut_99 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_100'] = x_assess_macro_risk__mutmut_100 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_101'] = x_assess_macro_risk__mutmut_101 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_102'] = x_assess_macro_risk__mutmut_102 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_103'] = x_assess_macro_risk__mutmut_103 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_104'] = x_assess_macro_risk__mutmut_104 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_105'] = x_assess_macro_risk__mutmut_105 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_106'] = x_assess_macro_risk__mutmut_106 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_107'] = x_assess_macro_risk__mutmut_107 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_108'] = x_assess_macro_risk__mutmut_108 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_109'] = x_assess_macro_risk__mutmut_109 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_110'] = x_assess_macro_risk__mutmut_110 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_111'] = x_assess_macro_risk__mutmut_111 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_112'] = x_assess_macro_risk__mutmut_112 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_113'] = x_assess_macro_risk__mutmut_113 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_114'] = x_assess_macro_risk__mutmut_114 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_115'] = x_assess_macro_risk__mutmut_115 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_116'] = x_assess_macro_risk__mutmut_116 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_117'] = x_assess_macro_risk__mutmut_117 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_118'] = x_assess_macro_risk__mutmut_118 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_119'] = x_assess_macro_risk__mutmut_119 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_120'] = x_assess_macro_risk__mutmut_120 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_121'] = x_assess_macro_risk__mutmut_121 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_122'] = x_assess_macro_risk__mutmut_122 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_123'] = x_assess_macro_risk__mutmut_123 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_124'] = x_assess_macro_risk__mutmut_124 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_125'] = x_assess_macro_risk__mutmut_125 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_126'] = x_assess_macro_risk__mutmut_126 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_127'] = x_assess_macro_risk__mutmut_127 # type: ignore # mutmut generated
mutants_x_assess_macro_risk__mutmut['x_assess_macro_risk__mutmut_128'] = x_assess_macro_risk__mutmut_128 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut: MutantDict = {}  # type: ignore


@_mutmut_mutated(mutants_x_get_llm_status__mutmut)
def get_llm_status() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_orig() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_1() -> dict:
    """Get the current LLM status."""
    client = None
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_2() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "XXenabledXX": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_3() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "ENABLED": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_4() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": False,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_5() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "XXproviderXX": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_6() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "PROVIDER": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_7() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "XXmodelXX": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_8() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "MODEL": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_9() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "XXavailableXX": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_10() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "AVAILABLE": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_11() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "XXurlXX": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_12() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "URL": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_13() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "XXenabledXX": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_14() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "ENABLED": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_15() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "XXproviderXX": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_16() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "PROVIDER": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_17() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "XXmodelXX": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_18() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "MODEL": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_19() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "XXavailableXX": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_20() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "AVAILABLE": False,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_21() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": True,
        "fallback_mode": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_22() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "XXfallback_modeXX": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_23() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "FALLBACK_MODE": True,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_24() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": False,
        "note": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_25() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "XXnoteXX": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_26() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "NOTE": "Using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_27() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "XXUsing rule-based analysis fallbackXX",
    }


def x_get_llm_status__mutmut_28() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "using rule-based analysis fallback",
    }


def x_get_llm_status__mutmut_29() -> dict:
    """Get the current LLM status."""
    client = get_llm_client()
    if client:
        return {
            "enabled": True,
            "provider": LOCAL_LLM_PROVIDER,
            "model": LOCAL_LLM_MODEL,
            "available": client.is_available(),
            "url": LOCAL_LLM_URL,
        }
    return {
        "enabled": LOCAL_LLM_ENABLED,
        "provider": LOCAL_LLM_PROVIDER,
        "model": LOCAL_LLM_MODEL,
        "available": False,
        "fallback_mode": True,
        "note": "USING RULE-BASED ANALYSIS FALLBACK",
    }

mutants_x_get_llm_status__mutmut['_mutmut_orig'] = x_get_llm_status__mutmut_orig # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_1'] = x_get_llm_status__mutmut_1 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_2'] = x_get_llm_status__mutmut_2 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_3'] = x_get_llm_status__mutmut_3 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_4'] = x_get_llm_status__mutmut_4 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_5'] = x_get_llm_status__mutmut_5 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_6'] = x_get_llm_status__mutmut_6 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_7'] = x_get_llm_status__mutmut_7 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_8'] = x_get_llm_status__mutmut_8 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_9'] = x_get_llm_status__mutmut_9 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_10'] = x_get_llm_status__mutmut_10 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_11'] = x_get_llm_status__mutmut_11 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_12'] = x_get_llm_status__mutmut_12 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_13'] = x_get_llm_status__mutmut_13 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_14'] = x_get_llm_status__mutmut_14 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_15'] = x_get_llm_status__mutmut_15 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_16'] = x_get_llm_status__mutmut_16 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_17'] = x_get_llm_status__mutmut_17 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_18'] = x_get_llm_status__mutmut_18 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_19'] = x_get_llm_status__mutmut_19 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_20'] = x_get_llm_status__mutmut_20 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_21'] = x_get_llm_status__mutmut_21 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_22'] = x_get_llm_status__mutmut_22 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_23'] = x_get_llm_status__mutmut_23 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_24'] = x_get_llm_status__mutmut_24 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_25'] = x_get_llm_status__mutmut_25 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_26'] = x_get_llm_status__mutmut_26 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_27'] = x_get_llm_status__mutmut_27 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_28'] = x_get_llm_status__mutmut_28 # type: ignore # mutmut generated
mutants_x_get_llm_status__mutmut['x_get_llm_status__mutmut_29'] = x_get_llm_status__mutmut_29 # type: ignore # mutmut generated
